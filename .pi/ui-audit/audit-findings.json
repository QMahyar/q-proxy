[
 {
  "consolidations": [
   "login.html:128-129 inline dict should merge with src/ui/panel/dict.js into one catalog (the build already injects head.js/css/js into shell.html; login can consume the same or a generated subset), eliminating the 12 duplicated keys and the drift risk",
   "warp.js AMZ (line 66) and AMZK (line 81) arrays should merge into one shared constant with dict-keyed labels under a warp.amnezia.* family (warp.amnezia.i1 already exists as the pattern)",
   "The four orphaned key families (checker.*, ports.*, proxyip.*, tabs.settings.ports/proxyip) should consolidate into one deletion: remove all 70 keys from dict.js AND add their names to the dead-key guard at test/ui/assets.spec.ts:179 so they cannot return",
   "err.* handling should consolidate to a single contract: either server validate.ts emits err.* codes that the client translates via its existing bridge, or the client emits all client-side messages and the bridge plus the 4 dead err.* keys are removed \u2014 currently two half-implementations coexist"
  ],
  "dimension": "i18n census \u2014 dict coverage (EN/FA), unused keys, used-but-missing keys, hardcoded strings outside dict.js",
  "findings": [
   {
    "effort": "S",
    "evidence": "dict.js:3 `en:{'app.name':'Q Proxy','nav.home':'Home',...}` (523 keys on one line) and dict.js:20 `fa:{'app.name':'Q Proxy','nav.home':'\u062e\u0627\u0646\u0647',...}` (523 keys). Programmatic diff (brace-balanced extraction + eval, key-by-key membership) returns EN-only=0 and FA-only=0. Fallback is safe by design: dict.js:39 `const t=(k,p)=>{let s=(DICT[LANG]&&DICT[LANG][k])||(DICT.en[k]||k);...}`.",
    "location": "src/ui/panel/dict.js:3 (EN), src/ui/panel/dict.js:20 (FA)",
    "problem": "None \u2014 this is the one dimension of the panel i18n that is genuinely healthy. Every FA user sees a fully translated surface (as far as the dict reaches).",
    "recommendation": "Keep the 1:1 key invariant; add a 3-line unit test asserting Object.keys(DICT.en) equals Object.keys(DICT.fa) so parity can never regress silently.",
    "severity": "low",
    "title": "EN/FA dictionary parity is 100% \u2014 zero missing keys in either direction"
   },
   {
    "effort": "S",
    "evidence": "Usage scan of all panel sources (t('...'), t('prefix.'+x) dynamic families, FL() descriptors, confirmDialog args, chip/select opts, card titles) against dict keys finds 70 keys never referenced. Families: checker.* \u00d719 ('checker.title':'Proxy IP checker' \u2026 'checker.tooMany':'Maximum 100 targets.'), ports.* \u00d717 ('ports.domains.label':'Custom domains' \u2026 'ports.cdn.sni.label':'CDN SNI'), proxyip.* \u00d75\u2026",
    "location": "src/ui/panel/dict.js:3-40 (dead keys distributed through both language lines)",
    "problem": "Roughly one key in seven is unreachable: ~6.8KB of dead EN+FA text shipped to every panel load inside a 65.2KB dict, and \u2014 worse \u2014 the dict reads as the source of truth for UI surface when a sixth of it describes features (IP checker, Ports & Domains tab, Proxy IP tab) that no longer exist. Any contributor grepping the dict to understand current UI will be misled.",
    "recommendation": "Delete all 70 keys (both languages) and extend the existing dead-key guard (test/ui/assets.spec.ts:179 \"drops dead ids, rules and dictionary keys\") with a computed assertion that every DICT key is referenced, so new corpses are caught at CI instead of accumulating.",
    "severity": "medium",
    "title": "70 of 523 panel dict keys (13.4%, ~6.8KB) are dead code from removed features"
   },
   {
    "effort": "L",
    "evidence": "settings.js:510-512: `function showFieldError(path,msg){...const err=fw.querySelector('.field__error');if(err)err.textContent=/\\s/.test(msg)||!DICT[LANG][msg]&&!DICT.en[msg]?msg:t(msg);` \u2014 the intent is that the server sends a short key and t() localizes it. But src/settings/validate.ts emits prose at 50 fail() sites (~77 English message strings), e.g. `fail(fields, key, \\`must be an integer betwe\u2026",
    "location": "src/ui/panel/settings.js:510-515 (bridge), src/settings/validate.ts (prose emitters), src/ui/panel/dict.js:3 (dead err.* keys)",
    "problem": "The known gap 'Server-side validation messages are English-only even in the FA UI' is confirmed and quantified: every invalid save (bad address, out-of-range port, bad URI, >64 addresses, invalid pbk\u2026) shows raw English in a Persian-default panel, and 4 dict keys exist as dead witnesses of a contract that was never wired. This is the single largest untranslated surface in the product.",
    "recommendation": "Decide the contract once: either (a) server emits err.*-style codes and validate.ts maps each of the ~50 messages to a dict key with a new FA translation batch, or (b) accept English validation prose permanently and delete the dead err.* keys plus the showFieldError dict-bridge so the code stops pretending. Option (a) is the professional choice; it is also the L-effort item blocking 'bilingual pan\u2026",
    "severity": "high",
    "title": "err.* dict contract is vestigial: 4 of 10 err.* keys dead, server sends English prose the translation bridge can never fire on"
   },
   {
    "effort": "M",
    "evidence": "login.html:128-129 defines its own `const DICT={en:{...43 keys...},fa:{...43 keys...}}` including 'common.reveal':'Reveal', 'common.hide':'Hide', 'common.close':'Close', 'common.theme':'Theme', 'common.theme_light'/'theme_dark'/'theme_toggle', 'toast.networkError':'Network error \u2014 check your connection.', 'app.name':'Q Proxy', 'lang.en'/'lang.fa', 'onboarding.setup.expired' \u2014 all 12 also present w\u2026",
    "location": "src/ui/login.html:128-129 vs src/ui/panel/dict.js:3,20",
    "problem": "Two hand-maintained catalogs with overlapping keys and no shared source: any wording fix or new language must be applied twice, and the copies will drift (login already has app.tagline/login.* keys the panel will never see, while panel carries onboarding.setup.expired for a page it isn't shown on). The build already assembles assets (src/ui/assets.ts), so a shared catalog is trivially achievable.",
    "recommendation": "Extract the ~12 common keys into one source consumed by both pages at build time, or have login.html import a trimmed slice of the panel dict. Remove the panel's dead onboarding.setup.expired copy if login keeps its own.",
    "severity": "medium",
    "title": "Two separate hand-maintained dicts (panel dict.js + login.html inline) duplicate 12 keys verbatim"
   },
   {
    "effort": "S",
    "evidence": "`const WARP_F=[['wireguard-conf','WireGuard .conf (ZIP)','grad-1'],...['throne','Throne wg://','grad-5'],...['v2rayn','V2RayN (base64)','grad-3'],...['singbox-legacy','sing-box legacy','grad-2'],...['clash','Clash / mihomo','grad-5'],...['egern','Egern','grad-6']];` \u2014 17 labels, zero dict keys. Rendered via `'<span class=\"fmt-label\">'+esc(label)+'</span>'` in renderWarpDetail (warp.js:96) and warp\u2026",
    "location": "src/ui/panel/warp.js:2 (WARP_F array)",
    "problem": "For an admin panel that defaults to Persian, the WARP account detail page and the home 'WARP subscriptions' block show 17 English strings. Most are product names (Xray, Surge, Loon \u2014 untranslatable), but 'Amnezia (ZIP)', 'legacy', '(base64)' are ordinary English words with no reason to be outside the dict, and the array is a third place (after dict.js and src/warp/formats/registry.ts) where format\u2026",
    "recommendation": "Move the translatable suffixes ('legacy', 'Amnezia', '(ZIP)') into dict keys (warp.fmt.* family) or accept and document WARP_F as format identifiers. Also consider generating WARP_F from src/warp/formats/registry.ts instead of hand-copying 17 rows.",
    "severity": "low",
    "title": "17 WARP output-format labels are hardcoded English in warp.js, untranslated in FA"
   },
   {
    "effort": "S",
    "evidence": "warp.js:66 `const AMZ=[['Jc','Jc (0-128)'],['Jmin','Jmin (0-1280)'],['Jmax','Jmax (0-1280)'],['S1','S1 (0-65535)'],...['H4','H4']];` and warp.js:81 `const AMZK=[['Jc','Jc (0-128)'],...['H4','H4']];` \u2014 identical 11-entry arrays in renderWarpSection and renderWarpDetail, while I1 uses the dict: `esc(t('warp.amnezia.i1'))` ('I1 (init packet)' / '\u0628\u0633\u062a\u0647\u0654 \u0622\u063a\u0627\u0632\u06cc\u0646').",
    "location": "src/ui/panel/warp.js:66 (AMZ) and src/ui/panel/warp.js:81 (AMZK)",
    "problem": "Inconsistent by one field: I1 gets a Persian label, Jc/Jmin/Jmax/S1\u2013S4/H1\u2013H4 stay English. The duplication also means adding an Amnezia parameter requires editing two arrays in two functions. (Numeric-range labels like 'Jc (0-128)' are borderline-translatable; the real sin is having two copies of the array.)",
    "recommendation": "Hoist a single AMZ_FIELDS constant to module scope with dict-keyed labels (warp.amnezia.jc etc., or compose 'Jc' + t('warp.amnezia.range') suffixes), and render both sections from it.",
    "severity": "low",
    "title": "Amnezia parameter labels hardcoded twice in warp.js (AMZ and AMZK arrays) while the identical I1 field IS dict-keyed"
   },
   {
    "effort": "S",
    "evidence": "Client filters: home.js:90 `...(S.subs||[]).filter(u=>u.format!=='base64'||u.label!=='Panel info').length...` and actions.js:544 `const firstSub=(S.subs||[]).find(u=>u.format==='base64'&&u.label!=='Panel info');` against the server producer src/handlers/api/status.ts:54 `{ format: \"base64\", label: \"Panel info\", url: ... }`. The label is displayed untranslated on Home via `esc(entry.label||entry.fo\u2026",
    "location": "src/handlers/api/status.ts:54; consumers src/ui/panel/home.js:90 and src/ui/panel/actions.js:544",
    "problem": "Home's subscription list shows a magic English string ('Panel info') even in FA, and two JS files hardcode the literal to filter it out. The test makes this safe, but it is still a user-visible untranslated string plus a server\u2192UI string contract where a stable format id would be cleaner.",
    "recommendation": "Either key it ('home.subs.panelinfo') with the server sending a stable format discriminator the client maps to a label, or leave as-is given the parity test \u2014 but then FA users will keep reading 'Panel info' on the home screen; a third option is hiding that entry's label and rendering t('home.subs.title') style text instead.",
    "severity": "low",
    "title": "\"Panel info\" subscription label is a hardcoded EN literal shared between server and two client files (test-guarded, but untranslated for FA)"
   },
   {
    "effort": "S",
    "evidence": "shell.html:19 `<a class=\"visually-hidden skip-link\" href=\"#main\">Skip to content</a>`; shell.html:53 `<div class=\"seg\" role=\"group\" aria-label=\"Language\" id=\"langseg\"></div>`; shell.html:67 `<div class=\"subtabs\" role=\"tablist\" aria-label=\"Settings sections\" id=\"subtabs\"></div>`; shell.html:83 `<noscript>...JavaScript is required.</noscript>`. Contrast: nav ('Navigation'), swatches ('Accent color')\u2026",
    "location": "src/ui/panel/shell.html:19,53,67,83; src/ui/login.html:121",
    "problem": "In FA/RTL the panel's landmark announcements and the (focusable) skip link are English \u2014 exactly the kind of mixed-language a11y debt professional dashboards don't ship. The noscript case is minor; the two group aria-labels are one-line fixes each.",
    "recommendation": "Add dict keys (a11y.skip, a11y.lang_group, a11y.settings_sections, a11y.noscript) and set them in buildShell()/login applyLang(), or hardcode bilingual strings. Effort: one buildShell() addition.",
    "severity": "medium",
    "title": "Static accessibility strings in shell.html and login.html stay English in FA: skip link, two landmark aria-labels, noscript"
   },
   {
    "effort": "S",
    "evidence": "dict has 'home.status.total':'Requests total' (zero references in JS); home.js:120 renders `...'<span class=\"lbl\">'+esc(t('home.status.today'))+'</span><span class=\"mono\">'+esc(String(u.requestsToday...))+'</span><span class=\"mono\">'+esc(String(u.requestsTotal...))+'</span>...` \u2014 requestsTotal is emitted with no label element at all.",
    "location": "src/ui/panel/dict.js:3 (key), src/ui/panel/home.js:120 (stat-grid markup)",
    "problem": "Two bare numbers sit adjacent in the Worker status card; in a RTL Persian UI there is nothing telling the reader which is today's count and which is the lifetime total. The dict key for the missing label exists but was simply never wired after this markup was rewritten.",
    "recommendation": "Wire t('home.status.total') as a label (or a data-l column) for the requestsTotal cell \u2014 kills one dead key and removes the ambiguity simultaneously.",
    "severity": "low",
    "title": "home.status.total key is dead and its absence leaves the 'Requests total' number unlabeled in the status grid"
   },
   {
    "effort": "S",
    "evidence": "test/ui/assets.spec.ts:179-194 loops a hardcoded array of 16 keys (`expect(html).not.toContain('\\''+key+'\\':')` for home.stats.updated, common.on/off, login.*, ...) \u2014 a blocklist, not a coverage check. Nothing in test/ parses DICT or cross-references t() usage; meanwhile the server side has a real drift test (test/settings/fields.spec.ts enforces table/interface/registry/dict agreement per AGENTS.\u2026",
    "location": "test/ui/assets.spec.ts:179-194",
    "problem": "The 70 dead keys accumulated after that test was written precisely because the guard only remembers keys someone manually deleted. Without a computed 'every key is used' assertion, dict.js will keep accreting corpses with every IA change.",
    "recommendation": "Add a unit test that imports/extracts DICT from src/ui/panel/dict.js, scans src/ui/panel/*.js + shell.html for referenced keys (same approach as this census), and fails on unused keys; also assert EN/FA key-set equality. Cheap, deterministic, and it retro-catches everything listed here.",
    "severity": "low",
    "title": "No automated unused-key detection: the dead-key test only blocklists 16 historical keys"
   },
   {
    "effort": "S",
    "evidence": "dict.js:37 `const getLangCookie=()=>{const m=document.cookie.match(/(?:^|;\\s*)qp_lang=(en|fa)/);return m&&m[1]||'fa'};` runs at script eval; head.js does the same (`const l=m&&m[1]||'fa'`) for the pre-JS `<html lang=\"fa\" dir=\"rtl\">` in shell.html:1. No navigator.language lookup anywhere (grep for navigator.language in src/ui/panel = 0 hits). AGENTS.md documents 'settings.language seeds the qp_lang\u2026",
    "location": "src/ui/panel/dict.js:37; src/ui/panel/head.js:1; src/ui/panel/shell.html:1 (`<html lang=\"fa\" dir=\"rtl\">`)",
    "problem": "An EN-speaking admin who has never set a cookie sees Persian chrome (nav, shell, then full panel) until settings.language seeds the cookie \u2014 or forever if bootstrap fails. Given the sibling projects this is likely a deliberate owner-first choice, but it is the mirror image of what BPB/cmliu panels do (follow the client), and it is worth being a conscious decision rather than an accident of the ||'\u2026",
    "recommendation": "If the FA default is intentional, document it; if not, one line \u2014 `return m&&m[1]||(navigator.language&&navigator.language.startsWith('fa')?'fa':'en')` \u2014 in both dict.js and head.js makes the panel follow the browser while keeping fa as the fallback for fa-locale users.",
    "severity": "low",
    "title": "Default language is hardcoded 'fa' with no navigator.language detection \u2014 first paint is Persian for every new visitor"
   },
   {
    "effort": "S",
    "evidence": "All checked negatives are clean: `grep -o \"'err\\.[a-z_]*'\" src/ui/panel/*.js` shows settings.js uses only err.domain/duplicate/invalid_line/number/required/uuid; `grep checker\\.` / `nav.warp` / `home.status.total` / `common.theme'` outside dict.js = 0 hits. The 24 raw 'used-but-missing' candidates from the literal scan decompose into settings data-bind paths built at runtime (`data-bind=\"'+paths[0\u2026",
    "location": "src/ui/panel/dict.js:3,20 (scan subject); src/ui/panel/settings.js (FL descriptor-driven t() calls verified)",
    "problem": "None \u2014 this is the strongest part of the i18n implementation: descriptor-driven rendering (FL/t(f.label)/t(f.hint)/t(f.help)) plus dynamic families (tabs.settings.*, accent.*, users.status.*, home.kill.*, fragment.preset.*, toast.killOn/Off, lang.*) are all covered, and the esbuild-built dict ships every string it references.",
    "recommendation": "Nothing to fix; keep the descriptor pattern \u2014 it is what makes a computed unused-key test (see other finding) cheap to add.",
    "severity": "low",
    "title": "Census cross-check: zero genuine used-but-missing keys \u2014 every t() reference resolves"
   }
  ],
  "removals": [
   "70 unused panel dict keys (dict.js:3 EN + dict.js:20 FA, identical lists): app.name, nav.warp, nav.checker, common.theme, home.status.total, tabs.settings.ports, tabs.settings.proxyip, checker.title/desc/targets.label/targets.hint/run/stop/running/results/empty/unsupported_port/col.target/col.latency/col.rate/col.status/status.ok/status.partial/status.failed/summary/tooMany (19), ports.domains.label/hint, ports.hostnameOverride.label/hint, ports.matrix.legend_tls/legend_plain/hint, ports.policy.label/always/workersdev/never, ports.cleanAddrs.label, ports.remoteSubs.label/hint, ports.cdn.title/enabled.label/addresses.label/host.label/sni.label (17), ports.cdn.addresses.short/help, ports.cleanAddrs.short/help, proxyip.mode.label/list/nat64, proxyip.list.label, proxyip.nat64.label, proxyip.list.short/help, proxyip.nat64.short/help (8), proxyip.* family (5 total incl. above), warp.settings, warp.presets.inuse, warp.view.accounts, warp.view.settings, routing.title, toast.saveFailed, toast.langChanged, wizard.s1_title, onboarding.setup.expired (panel copy), err.url, err.ip_or_host, err.ipv6_prefix, err.port",
   "The vestigial dict-translation bridge in settings.js:511-512 (`/\\s/.test(msg)||!DICT[LANG][msg]&&!DICT.en[msg]?msg:t(msg)`) \u2014 dead weight as long as validate.ts sends prose; remove it or make it real by fixing the server contract, but not both-half",
   "Duplicate AMZ array in warp.js (either AMZ at line 66 or AMZK at line 81) once merged into a single module-scope constant"
  ],
  "summary": "Panel i18n coverage is technically excellent \u2014 523/523 EN/FA keys with zero gaps in either direction plus 43/43 on login \u2014 but 70 of the 523 keys (13.4%, ~6.8KB) are corpses from the removed IP-checker view and old Ports/ProxyIP settings tabs, and the test suite only blocklists 16 historical dead keys instead of detecting new ones. Used-but-missing is exactly zero; the real leakage is the inverse: ~30 user-visible strings are hardcoded outside any dict (17 WARP format labels, 10 Amnezia param labels duplicated across two arrays, the server-coupled 'Panel info' label, skip-link/aria/noscript), and all ~50 server-side validation errors arrive as English prose that the client's err.* translation bridge can never match \u2014 so the Persian-default UI still renders English on every failed save, in WARP format rows, and in the home subscription list. Two hand-maintained dictionaries (panel dict.js + login.html inline) duplicate 12 keys verbatim with no shared source, guaranteeing future drift."
 },
 {
  "consolidations": [
   "Format-row rendering: WARP_F's 17 rows are emitted by warp.js:88-89 (detail), warp.js:90-95 (home), and counted at home.js:90 \u2014 one renderFormatRows(container, baseUrl, idPrefix) helper should serve all copy surfaces (copyFieldHtml is already shared).",
   "Pool rendering: renderHomePool (home.js:159-172) and renderPool (warp.js:21-47) are two divergent renderers of the same api/proxy-pool payload \u2014 merge into one component with a maxRows/addActions option, used by both Home and Egress.",
   "Users data: loadHomeUsers (home.js:165-172) and loadUsers (users.js:26-34) both GET api/users and write S.users \u2014 single fetch + subscription so Home counts and the Users table stay consistent.",
   "Language switching: topbar #langseg (shell.html:46) and the General 'language' select (settings.js:36) are the same 2-value control \u2014 keep one (topbar), drop the other.",
   "Kill switch: Home card (home.js:105-107) and General instantbool (settings.js:36-39) are the same toggle synced by DOM-wide [data-kill] queries \u2014 keep the Home card, remove the settings copy.",
   "Save model: per-section Save row (settings.js:318), global applybar (shell.html:71), and Ctrl+S are three surfaces \u2014 keep applybar (with per-section chips) and Ctrl+S, drop the per-section buttons or make them scroll-to-applybar.",
   "Empty states: home.js:100 and warp.js:61 already share the .empty-card pattern \u2014 apply the same component to Users (users.js:29) instead of the bare hint row.",
   "'Panel info' magic string: replace label-string matching (home.js:90, actions.js:542-543) with a server-declared kind/flag on the SubUrlEntry so UI filtering and the wizard stop depending on a mutable display label."
  ],
  "dimension": "Inventory / information architecture of the Q Proxy admin panel UI",
  "findings": [
   {
    "effort": "S",
    "evidence": "shell.html:43-44: `<a class=\"tab\" role=\"tab\" id=\"tab-home\" href=\"#/home\" data-view=\"home\"></a><a class=\"tab\" role=\"tab\" id=\"tab-settings\" href=\"#/settings/general\" data-view=\"settings\"></a>` \u2014 that is the entire nav. dict.js:3 still defines `'nav.warp':'WARP','nav.checker':'IP Checker'` (and FA equivalents) but no element ever calls t('nav.warp') or t('nav.checker') (grep: 2 hits each = the dict l\u2026",
    "location": "src/ui/panel/shell.html:43-44 vs src/ui/panel/dict.js:3",
    "problem": "The dictionary preserves the shape of a 4-tab nav (Home / Settings / WARP / IP Checker) that was cut back to two tabs. Users and WARP \u2014 the two most product-like surfaces \u2014 became settings subtabs #9 and #10 (settings.js:124-125), which is exactly the burial the owner reports. The dead keys are the fossil of that decision.",
    "recommendation": "Either restore `#/warp` and `#/users` as top-level tabs (nav.warp string is already translated) or delete nav.warp/nav.checker. Tailscale-style: Machines, DNS, Settings \u2014 proxy panels get 3-4 top-level nouns.",
    "severity": "high",
    "source": "src/ui/panel/shell.html",
    "title": "WARP and Users live as subtabs #9/#10 of Settings behind a 2-tab topbar (owner pain 1 confirmed)"
   },
   {
    "effort": "S",
    "evidence": "users.js:29: `tb.innerHTML=S.users.length?S.users.map(userRowHtml).join(''):'<tr><td colspan=\"6\"><span class=\"field__hint\">'+esc(t('users.empty'))+'</span></td></tr>';` \u2014 the word for it is t('users.empty')='No users yet', one dim hint line inside a 6-column table that still shows a header row with a select-all checkbox. Contrast WARP: warp.js:61 renders a full `.empty-card` with icon, title, mess\u2026",
    "location": "src/ui/panel/users.js:29",
    "problem": "Three empty states, three different quality levels; the Users one is the weakest surface in the panel for the exact scenario a new admin hits first. Home compounds it: the users card just shows `0 / 0` counts (home.js:166-172) with a 'Manage' link into the same empty table.",
    "recommendation": "Reuse the .empty-card pattern for Users: icon, 'No users yet', one-line value proposition, primary 'Add user' CTA. Zero-code Users should also hide the bulk bar/select-all column rather than render dead affordances.",
    "severity": "high",
    "source": "src/ui/panel/users.js",
    "title": "Users empty state is a single hint row in a dead table \u2014 weakest empty state in the panel (owner pain 3 confirmed)"
   },
   {
    "effort": "S",
    "evidence": "dict.js:3: `'checker.title':'Proxy IP checker','checker.desc':'Tests reachability and latency\u2026','checker.targets.label':'Targets',\u2026,'checker.tooMany':'Maximum 100 targets.'` \u2014 20 keys \u00d7 2 languages. The only non-dict remnant is settings.js:564: `ta.id.indexOf('checker-targets')===0?' / '+MAX_TARGETS:''` and lib.js:3 `MAX_TARGETS=100`. No renderer, no route, no markup anywhere (grep panel.html: che\u2026",
    "location": "src/ui/panel/dict.js:3 (checker.*) and src/ui/panel/settings.js:564, src/ui/panel/lib.js:3",
    "problem": "A whole removed feature persists as ~40 dead strings plus a live code branch and a constant. Every future i18n audit pays for it, and the checker-targets special case in shared validation code is a trap for refactoring.",
    "recommendation": "Delete the checker.* namespaces, the checker-targets id check and MAX_TARGETS together (or resurrect the feature as a view \u2014 the strings are ready).",
    "severity": "medium",
    "source": "src/ui/panel/dict.js",
    "title": "An entire 'IP Checker' feature exists as dead i18n strings plus two code fossils"
   },
   {
    "effort": "S",
    "evidence": "settings.js:222: `case 'ports':{` renders a `port-matrix` fieldset with TLS/PLAIN checkboxes and a `[data-port-master]` select-all \u2014 but no SECTIONS field anywhere declares `type:'ports'` (grep `type:'ports'` in settings.js: 0 hits). `updatePortMasters` (settings.js:344) is called on every render/change against elements that can never exist. The matching `ports.*` dict namespace (ports.domains, po\u2026",
    "location": "src/ui/panel/settings.js:222-227, 344-352",
    "problem": "The old Ports & Domains card (port matrices per protocol family, CDN fronting, hostname override) was replaced by the per-address `addrList` cards (settings.js:136-159: address/port/label/host/sni/country/city per card), but the renderer, the master-toggle sync logic and ~30 dict strings were left behind.",
    "recommendation": "Delete `case 'ports'`, updatePortMasters and the ports.*/ports.cdn.* dict keys; the address card side column already renders the effective port preview (settings.js:154).",
    "severity": "medium",
    "source": "src/ui/panel/settings.js",
    "title": "Dead 'ports' field renderer: the port-matrix UI survives with no data model behind it"
   },
   {
    "effort": "S",
    "evidence": "dict.js:3-4: `'tabs.settings.ports':'Ports & Domains','tabs.settings.proxyip':'Proxy IP'` alongside `'proxyip.mode.label','proxyip.mode.list','proxyip.mode.nat64','proxyip.list.*','proxyip.nat64.*'`. The live section keys are `{key:'addresses',\u2026}` (settings.js:71) and `{key:'egress',\u2026}` (settings.js:80); buildSubtabs (home.js:74) looks up `t('tabs.settings.'+s.key)`, so ports/proxyip can never mat\u2026",
    "location": "src/ui/panel/dict.js:3-4 vs src/ui/panel/settings.js:71,80",
    "problem": "These are the old names of the addresses/egress sections before a split/rename. The repo enforces descriptor-table drift with test/settings/fields.spec.ts, but the dict has no equivalent drift test \u2014 dead keys accumulate silently and mask future renames.",
    "recommendation": "Purge tabs.settings.ports/proxyip and the proxyip.* namespace (egress.* covers the same concepts), and add a cheap test asserting every t('tabs.settings.<key>') referenced by SECTIONS exists in both languages.",
    "severity": "medium",
    "source": "src/ui/panel/dict.js",
    "title": "Stale subtab names in dict: 'Ports & Domains' and 'Proxy IP' match no section key"
   },
   {
    "effort": "M",
    "evidence": "home.js:90: the 'formats' chip is `(S.subs||[]).filter(u=>u.format!=='base64'||u.label!=='Panel info').length` \u2014 the UI identifies a server entry by its human label. status.ts:54 ships `{ format: \"base64\", label: \"Panel info\", url: `${base}?view=html` }`, and home.js:96-99 renders all 7 rows \u2014 including that HTML page URL \u2014 as identical copy+QR rows next to client-importable subscriptions.",
    "location": "src/ui/panel/home.js:90 vs src/handlers/api/status.ts:45-58",
    "problem": "If a server-side label ever changes, the chip count silently becomes wrong. Meanwhile the card gives 7 equal-weight rows (Base64/Mixed, Clash, sing-box, Surge, Loon, Quantumult X, Panel info) with no 'this is the one to import' hierarchy \u2014 the main links technically ARE on Home (pain point 2 is partially refuted: copy+QR per row, wizard re-surfaces the Base64 URL), but choosing between them is on \u2026",
    "recommendation": "Mark the info URL with a distinct format/kind server-side instead of matching a display label; visually separate or annotate the '?view=html' row; give the Base64/Mixed row hero treatment (larger, first, 'paste this into your client').",
    "severity": "medium",
    "source": "src/ui/panel/home.js",
    "title": "UI identifies the 'Panel info' row by magic-string label matching against server data"
   },
   {
    "effort": "S",
    "evidence": "home.js:59-60: `if(seg[1]==='warp'&&seg[2])return{view:'settings',sec:'warp',warpId:seg[2]};` and `return{view:'settings',sec:SECTIONS.some(s=>s.key===seg[1])?seg[1]:'general'};` \u2014 the detail route is entered only from warp.js:62 `<a class=\"acct-card\" href=\"#/settings/warp/'+esc(a.id)+'\">` or post-generate/import redirects (actions.js:461,467 `location.hash='#/settings/warp/'+d.account.id`). Unkno\u2026",
    "location": "src/ui/panel/home.js:59-60, src/ui/panel/warp.js:62, src/ui/panel/actions.js:461,467",
    "problem": "The richest single-entity screen in the panel (token, endpoints, 17 sub URLs, Amnezia overrides, delete) is reachable only by hover-and-click on a card or by a transient redirect \u2014 the owner's 'burrow into Settings' complaint applies doubly here. The silent general-fallback also turns a typo'd bookmark into a mystery navigation.",
    "recommendation": "Give the detail route a home: at minimum a 'WARP' breadcrumb in the subtab row, and surface unknown-section hashes with a small 'not found, showing General' notice instead of silent history.replaceState.",
    "severity": "medium",
    "source": "src/ui/panel/home.js",
    "title": "Route #/settings/warp/<id> exists with zero nav entries; unknown hash routes silently rewrite to General"
   },
   {
    "effort": "S",
    "evidence": "chrome.js:79: `if(mod&&e.key.toLowerCase()==='k'){e.preventDefault();const s=document.querySelector('#settings-search');if(s)s.focus();else location.hash='#/home';return}` \u2014 `#settings-search` exists nowhere (grep: only this line and its assembled copy in panel.html:1004). The shortcuts modal advertises it anyway: chrome.js:40 `['Ctrl / \u2318 + K','shortcuts.search']` with dict text 'Focus search, or \u2026",
    "location": "src/ui/panel/chrome.js:79,40",
    "problem": "A keyboard affordance that always degrades to 'go home' is worse than none: it teaches the admin that shortcuts lie. It also implies a settings-search feature that was planned or removed \u2014 same fossil pattern as checker.",
    "recommendation": "Either implement the filter input (a data-bind grep across SECTIONS fields would be ~40 lines) or remove the shortcut row and the handler.",
    "severity": "low",
    "source": "src/ui/panel/chrome.js",
    "title": "Ctrl+K targets a search field that does not exist \u2014 advertised in the shortcuts modal as a fallback"
   },
   {
    "effort": "M",
    "evidence": "Every panel gets a Save row prepended above its cards: settings.js:318 `h='<div class=\"section-actions\"><button type=\"button\" class=\"btn btn--primary btn--sm\" data-action=\"section-save\" data-sec=\"'+s.key+'\">\u2026'` \u2014 including users/warp whose cards contain no `[data-bind]` fields, where applySection then hits settings.js:477 `if(Object.keys(patch).length===0){markDirty();return}` and silently does no\u2026",
    "location": "src/ui/panel/settings.js:318,477 vs src/ui/panel/shell.html:71 and src/ui/panel/actions.js:53-55",
    "problem": "Two different save scopes with identical labels ('Save' vs 'Apply changes') and no explanation of which rows they cover; no-op Save buttons on non-form sections train the admin to ignore them.",
    "recommendation": "Pick one model: keep the applybar (per-section chips already show what is dirty \u2014 settings.js:404-409) and drop the per-section buttons, or keep section-save and make applybar show per-section rows. Never render a save control on a fieldless section.",
    "severity": "medium",
    "source": "src/ui/panel/settings.js",
    "title": "Two parallel save affordances with different scopes, one of which is a no-op on most sections"
   },
   {
    "effort": "S",
    "evidence": "sourcesCardHtml (warp.js:10-12) renders `data-action=\"pool-fetch\"` and `data-action=\"source-doh\"` \u2014 but loadPool writes into `const box=$('pool-list')` (warp.js:15) which only exists inside poolCardHtml in the egress panel; renderSettings pre-renders ALL panels hidden (settings.js:305-313). Clicking 'DoH resolve' in Sources updates a hidden panel \u2014 zero visible feedback in Sources. 'source-doh' (a\u2026",
    "location": "src/ui/panel/warp.js:10-12,15 vs src/ui/panel/warp.js:8-9 and src/ui/panel/actions.js:210-218",
    "problem": "The Sources tab's entire helper block is functionally decorative: two buttons doing one thing, with output landing in another subtab. This is the kind of dead-end that makes a page feel broken even when the API call succeeds.",
    "recommendation": "Either render pool results inline in Sources (its own list container) or remove the helper buttons and keep Sources purely as the sourceUrls editor; if kept, delete the duplicate action.",
    "severity": "medium",
    "source": "src/ui/panel/warp.js",
    "title": "Sources tab's 'Helpers' buttons write results into a hidden panel \u2014 invisible output, duplicated action"
   },
   {
    "effort": "M",
    "evidence": "renderHomePool (home.js:159-172) renders `pool.slice(0,8)` with no source/updated meta and no action; renderPool (warp.js:21-47) renders `sorted.slice(0,16)` sorted by probe latency with an add-to-proxyIps button (warp.js:43). Both call `api('api/proxy-pool\u2026',{fresh:true})` via separate loaders (loadHomePool home.js:152 vs loadPool warp.js:13).",
    "location": "src/ui/panel/home.js:159-172 vs src/ui/panel/warp.js:21-47",
    "problem": "Copy-paste divergence: an admin comparing Home's pool to Egress's pool sees different entries in different order with different capabilities. Two code paths to fix on every API change.",
    "recommendation": "One renderer, parameterized (rows, showAdd, showMeta); Home reuses it with rows=8.",
    "severity": "low",
    "source": "src/ui/panel/home.js",
    "title": "Proxy pool rendered twice by two divergent copy-paste renderers"
   },
   {
    "effort": "S",
    "evidence": "loadHomeUsers/renderHomeUsers (home.js:165-172, `api('api/users',{fresh:true})`) duplicate loadUsers/renderUserRows (users.js:26-34, same call) \u2014 both write the shared S.users array, and Home's card only extracts `all.length` and active count. WARP's 17-format list (WARP_F, warp.js:2) is likewise triple-rendered: warp detail subs (warp.js:88-89), home warpSubsHtml `<details>` per account (warp.js:\u2026",
    "location": "src/ui/panel/home.js:165-172 vs src/ui/panel/users.js:26-34",
    "problem": "Same data, two fetch caches (lib.js api() caches per path but home uses fresh:true), two renderers, one shared mutable array \u2014 classic drift setup, and with N WARP accounts the Home subscriptions card DOM balloons to 7+17\u00d7N copy fields.",
    "recommendation": "Single users loader; for WARP rows on Home render collapsed per-account links (jump to detail) instead of duplicating all 17 URLs, or virtualize.",
    "severity": "low",
    "source": "src/ui/panel/home.js",
    "title": "Users data fetched and rendered by two independent code paths"
   },
   {
    "effort": "S",
    "evidence": "home.js:120: `'<span class=\"lbl\">'+esc(t('home.status.today'))+'</span><span class=\"mono\">'+\u2026requestsToday\u2026+'</span><span class=\"mono\">'+\u2026requestsTotal\u2026+'</span>'` \u2014 one label, two number cells. dict.js:3 defines `'home.status.total':'Requests total'` but grep shows it appears only inside the dict (en+fa).",
    "location": "src/ui/panel/home.js:120 vs src/ui/panel/dict.js:3",
    "problem": "The 'requests total' number renders with no label (a reader cannot tell today's requests from lifetime), and the translation key intended for it is dead \u2014 a live display bug caused by a dead key, in a panel where counters are already documented as estimates.",
    "recommendation": "Use t('home.status.total') for the second cell (stat-grid already supports the label/value pattern used two rows above).",
    "severity": "low",
    "source": "src/ui/panel/home.js",
    "title": "'Requests total' renders with no label \u2014 its dict key is dead"
   },
   {
    "effort": "S",
    "evidence": "shell.html:46 `<div class=\"seg\" role=\"group\" aria-label=\"Language\" id=\"langseg\"></div>` (populated in buildShell, home.js:57-58, switching calls setLangCookie+reload) AND General's field list includes `FL('language','lang','general.language.label',null)` (settings.js:36) rendering a `<select data-lang-sel>` (settings.js:253-255) whose change handler is also `setLangCookie(e.target.value);location.\u2026",
    "location": "src/ui/panel/shell.html:46 and src/ui/panel/settings.js:36,253-255 vs src/ui/panel/actions.js:436-438",
    "problem": "A two-value enum with two full controls plus a third copy in boot()'s qp_lang cookie re-seeding (actions.js:527-530). Wasted pixels in General and one more thing to keep in sync for a future language addition.",
    "recommendation": "Keep the topbar segment; delete the General field (it is not a settings leaf worth round-tripping to the server on every save \u2014 it only re-sets the same cookie).",
    "severity": "low",
    "source": "src/ui/panel/settings.js",
    "title": "Language switcher exists twice: topbar segment and General section field"
   },
   {
    "effort": "S",
    "evidence": "settings.js:30-126: `const SECTIONS=[{key:'general',cards:[\u20266 cards\u2026]}, {key:'protocols',\u2026}, \u2026 {key:'warp',cards:[{warpAccounts:true,\u2026},{warpPresets:true,\u2026},{warpAmnezia:true,\u2026}]}];` \u2014 62 `FL(\u2026)` descriptors (settings.js:37 onward) rendered through one bindHtml switch, plus securityCardHtml (settings.js:273), totpCardHtml+TOTP b32/HOTP helpers (settings.js:277-300, hand-rolled base32/HMAC in the b\u2026",
    "location": "src/ui/panel/settings.js:30-126, 4-29, 273-300, 362-470",
    "problem": "The descriptor table is the right core, but it is welded to four unrelated subsystems (field renderers, TOTP enrollment crypto, the user modal, section IO). The seams are already visible \u2014 users/warp/pool/sources renderers were pushed out to users.js/warp.js, leaving settings.js as the residue.",
    "recommendation": "Split at existing joints: sections.js (SECTIONS + FL), fields-render.js (bindHtml), totp.js, section-io.js. Purely mechanical given the named-function style; the drift test (test/settings/fields.spec.ts) then reads the split table unchanged.",
    "severity": "medium",
    "source": "src/ui/panel/settings.js",
    "title": "settings.js is a 48KB/620-line monolith: descriptor registry, all renderers, TOTP crypto, user modal, validation and section IO in one file"
   },
   {
    "effort": "M",
    "evidence": "dict.js:3/20 dead namespaces confirmed by grep against assembled panel.html: `checker.*` (20 keys \u00d72), `ports.*`+`ports.cdn.*` (~15 \u00d72), `proxyip.*` (~6 \u00d72), `tabs.settings.ports`/`tabs.settings.proxyip`, `home.status.total`, `warp.view.accounts`/`warp.view.settings`, `nav.warp`/`nav.checker` \u2014 all appear exactly twice (en+fa dict lines) and in no `t(\u2026)` call. dict.js is 65,245 bytes concatenated \u2026",
    "location": "src/ui/panel/dict.js:3-4,19-20",
    "problem": "Roughly 100 dead keys ride along with a dictionary that is not per-language-split; EN users download FA strings and vice versa. More insidiously, dead keys mask real drift: when something is renamed, the old strings stay green in tests because nothing asserts dict\u2194UI usage.",
    "recommendation": "Add a dict-drift test (scan panel JS for t('key') literals, assert bidirectional coverage in both languages), then prune. Consider emitting only the active language into the assembled HTML \u2014 the build already splices strings.",
    "severity": "low",
    "source": "src/ui/panel/dict.js",
    "title": "dict.js ships ~100 dead keys (\u22482KB \u00d7 2 languages) that no t() call can ever reach"
   }
  ],
  "removals": [
   "checker.* dict namespace (20 keys \u00d7 2 langs), the `ta.id.indexOf('checker-targets')===0` branch in validateOneEditor (settings.js:564), and MAX_TARGETS in lib.js:3 \u2014 the IP Checker view does not exist.",
   "'ports' case in bindHtml (settings.js:222-227), updatePortMasters (settings.js:344-352) and its call sites \u2014 no field of type 'ports' exists.",
   "ports.* / ports.cdn.* / proxyip.* / tabs.settings.ports / tabs.settings.proxyip dict keys (dict.js:3-4,19-20) \u2014 replaced by the addresses/egress sections and per-address cards.",
   "nav.warp and nav.checker dict keys (dict.js:3,20) \u2014 no such tabs; restore as real tabs or delete.",
   "'source-doh' action (actions.js:214-218) \u2014 identical to 'pool-fetch' and writes into a hidden panel either way.",
   "warp.view.accounts / warp.view.settings / home.status.total dict keys (dict.js:3,19-20) \u2014 no t() references; fix the unlabeled Requests-total cell first.",
   "Ctrl+K handler in globalKeys (chrome.js:79) plus the 'Ctrl / \u2318 + K' row in renderShortcuts (chrome.js:40) and the shortcuts.search dict strings \u2014 #settings-search does not exist."
  ],
  "summary": "SCREEN MAP \u2014 Q Proxy admin panel (3 surfaces, all routes/sections verified against source)\n\nLOGIN src/ui/login.html \u2014 states: login(passphrase) | TOTP(6-digit \u21c4 recovery swap) | setup(claim passphrase, strength meter) | setup-expired | redirecting. Own DICT/theme/toasts, auto-redirect if already authed (login.html tail: fetch api/settings \u2192 goPanel).\n\nCAMO src/ui/camo.html \u2014 static \"Daily Notes\" deco page, zero UI logic.\n\nPANEL src/ui/panel.html (built from panel/*; assembly order in panel/README.md: dict\u2192lib\u2192qr\u2192home\u2192warp\u2192users\u2192chrome\u2192settings\u2192actions)\n\nROUTES (parseRoute home.js:55-61): #/home \u00b7 #/settings/{general,protocols,addresses,egress,fragment,chain,advanced,sources,users,warp} \u00b7 #/settings/warp/<id> \u00b7 unknown sec \u2192 silent rewrite to general; anything else \u2192 home. Topbar nav = only 2 tabs (shell.html:43-44).\n\nGLOBAL chrome: #backup-banner (30d-no-export reminder, 7d dismiss \u2014 chrome.js:27-35) \u00b7 #applybar fixed-bottom dirty bar (shell.html:71) \u00b7 #force-change fullscreen bootstrap-password takeover reusing securityCardHtml (actions.js:296-319) \u00b7 toasts (\u22643, pause-on-hover) \u00b7 Ctrl+S apply, Ctrl+Z/\u21e7Z undo-redo, g-h home, Ctrl+K\u2192ghost search (chrome.js:74-84).\n\nHOME #/home (renderHome home.js:85-129; grid 3fr/2fr)\n\u251c L: chips row (protocols-on, formats \u2014 home.js:89-90)\n\u251c L: Subscriptions card (:92-104): normal|fragment mode seg; 7 URL rows copy+QR (Base64/Mixed, Clash/mihomo, sing-box, Surge, Loon, Quantumult X, Panel-info HTML view \u2014 status.ts:45-58); WARP subs <details> per account \u00d717 formats (warpSubsHtml warp.js:90-95); empty-card CTA\u2192#/settings/protocols only when zero subs AND zero WARP accounts (:100)\n\u2514 R: Kill switch card (:105-107) \u00b7 My IP & egress card (:108-110) \u00b7 Egress pool card top-8 (:111-113, renderHomePool :159-172) \u00b7 Users card n/active + Manage link (:114-116) \u00b7 Worker status card (version/colo/requests today+total/traffic sparkline/check-update, :117-121)\n\nSETTINGS #/settings/<sec> \u2014 10 subtabs (SECTIONS settings.js:30-125), 27 cards, 62 FL descriptors; section-actions Save row PREPENDED above cards of every section (settings.js:318)\n\u251c general (6): [securePath\u00b7profileTitle\u00b7debugLogging\u00b7allowedIps] | [killSwitch(instantbool)\u00b7language] | Danger reset | Backup export/import | Security password change (securityCardHtml :273) | TOTP setup/verify/recovery (totpCardHtml :297)\n\u251c protocols (5): VLESS[enable,uuid,path,flow] \u00b7 VMess[enable,uuid,path] \u00b7 Trojan[enable,password,path] \u00b7 SS[enable,password,cipher,path,ssDirect] \u00b7 Common[earlyData\u00d72,fingerprint,sniCase,ECH\u00d73,alpn] \u2014 disabled protos dim via applyProtoDim\n\u251c addresses (3): Addresses[addrList cards: address/port/label/host/sni/country/city + enable toggle + probe dots; defaultPort; nameTemplate] \u00b7 Remote subs[remoteSubUrls] \u00b7 Remote nodes[remoteList reality/hy2, max 20]\n\u251c egress (2): Fallback[proxyIpMode chips; proxyIps|nat64Prefixes showIf; poolUrl] \u00b7 Proxy pool[poolCardHtml warp.js:8; fetch/test; top-16 sorted; add\u2192proxyIps]\n\u251c fragment (1): preset chips off/low/medium/high/severe/custom; packets; length/delay/split ranges (presets lock fields)\n\u251c chain (1): enabled + upstream URI\n\u251c advanced (4): DoH[upstream,remoteDns,localDns,udp53,__privateDoh copy-only] \u00b7 Telegram[enable,token,chatId + webhook setup/remove] \u00b7 Behavior[urlTest,subInterval,maxNodes] \u00b7 Camouflage+routing[mode,url,speedtest,bypassLan,blockQuic,blockAds,blockMalware,customBypass,customBlock]\n\u251c sources (1): sourceUrls textarea + Helpers[pool-fetch, source-doh] \u2014 output renders into hidden egress panel\n\u251c users (1): table[select-all,name,full token URL copy+QR,status chip,enabled switch,copy/QR/edit/regen/delete] + bulk bar[enable/disable/extend/delete]; empty state = ONE hint row \"No users yet\" (users.js:29)\n\u2514 warp (3): Accounts[stat chips; empty-card w/ Generate+Import; acct-cards\u2192detail] \u00b7 Presets[rows, edit/del] \u00b7 Amnezia defaults[11 fields]\n   \u2514 DETAIL #/settings/warp/<id> (renderWarpDetail warp.js:68-89): token panel(+regen) \u00b7 name/dns/preset card \u00b7 custom endpoints card \u00b7 17-format subscription card \u00b7 per-account Amnezia overrides(11) \u00b7 delete card\n\nMODALS (shell.html:72-80): m-qr(canvas+PNG download) \u00b7 m-confirm \u00b7 m-warp-generate \u00b7 m-warp-import \u00b7 m-warp-preset \u00b7 m-user(name, protocols all/4, daily limit, expiry, addressOverride address/port/label+datalist) \u00b7 m-rot(new user token after regen) \u00b7 m-keys(shortcuts) \u00b7 m-wizard(3 steps, once, qp_wizard_done)\n\nFIELD SYNC: 62 FL descriptors + sourceUrls(sources card, outside table) + totp.* \u21d2 exactly covers src/settings/fields.ts user-facing paths (74 minus 5 server-internal) \u2014 field layer is in sync; the DRIFT is all in dict.js dead keys and dead renderer code."
 },
 {
  "consolidations": [
   "Merge F2+F3 (census diff sets) into the inventory summary \u2014 they are the core deliverable, not defects; the same numbers back F4's zombie-field analysis.",
   "Merge F6 (vtype:'url' dead) into F5's dead-code list \u2014 both are register-metadata orphaned by a removed/changed feature and should be swept in one pass.",
   "Merge F7 (language cookie-only) and F8 (killSwitch dual-path) into a single 'controls outside settings persistence' finding \u2014 same root cause: renderer emits no data-bind, so diffSection never sees them; killSwitch is by-design, language is a gap.",
   "F10's three concrete client/server regex divergences (echServerName, proxyIpPoolUrl, https-only) plus the no-client-check list are one 'client-side validation parity' work item."
  ],
  "dimension": "Field census: UI settings fields vs src/settings/fields.ts descriptor table",
  "findings": [
   {
    "effort": "S",
    "evidence": "Descriptor table holds 76 rows: 52 top-level + 24 nested (fragment x8, chainProxy x2, camouflage x2, routingRules x6, telegram x3, totp x3), no duplicates, exactly covering every DEFAULT_SETTINGS settable leaf (drift-tested). The FL() registry (settings.js `const FL=(path,type,label,hint,extra)=>...` / `const SECTIONS=[...]`) issues 65 FL() calls -> 68 unique paths (3 'range' calls carry two paths\u2026",
    "location": "src/settings/fields.ts:44-147 vs src/ui/panel/settings.js:32-127",
    "problem": "The diff itself is healthy \u2014 the census diff is: {UI-only: __privateDoh} vs {descriptor-without-UI: 9}. Nothing silently diverges, which is better than rival panels (BPB's settings blob has no descriptor table at all).",
    "recommendation": "Keep as-is. Optionally: replace AGENTS.md's stale 'validate (66 leaf fields)' claim with the true 76-path count.",
    "severity": "high",
    "source": "src/settings/fields.ts",
    "title": "Census headline: 76 descriptor paths vs 68 FL-registry paths \u2014 the only structural diff is 1 documented pseudo-field and 9 descriptor rows outside the registry"
   },
   {
    "effort": "M",
    "evidence": "The General section renders `FL('securePath','secret','general.securePath.label','general.securePath.short',{gen:'hex12',copy:true,maxLen:64,help:'general.securePath.help'})` (settings.js:33) and applySection wires a full change flow: `if(patch.securePath!==undefined&&!(await confirmDialog('confirm.securepath_title','confirm.securepath_body',true)))return;` (settings.js:478) and on success `if(pat\u2026",
    "location": "src/ui/panel/settings.js:33,478-485 vs src/handlers/api/settings.ts:63,90",
    "problem": "A top-of-first-card field with a generate button, copy button, confirm dialog and success redirect is a complete write-path ghost. This is exactly the class of drift the drift test cannot see: it only asserts FL-path strings exist, not that the server honors them. Rival panels either truly rotate the path or hide it.",
    "recommendation": "Pick one: (a) honor it \u2014 add a dedicated securePath mutation endpoint that re-issues sessions and rewrites sub-URL caches, keeping the confirm dialog; or (b) delete FL('securePath'), the confirm keys (confirm.securepath_*), the redirect branch, and show the current path as a read-only copy widget instead (it is already exposed via /{securePath}/sub URLs). (b) is 30 minutes and removes a phantom co\u2026",
    "severity": "critical",
    "source": "src/handlers/api/settings.ts:63",
    "title": "securePath is a dead control: UI renders, confirms and redirects as if it saves, but handleSaveSettings strips it \u2014 an admin changing it gets dumped on a dead URL"
   },
   {
    "effort": "S",
    "evidence": "`FL('language','lang','general.language.label',null)` (settings.js:39) renders case 'lang' which emits a select with `data-lang-sel` \u2014 NOT `data-bind` (settings.js bindHtml case 'lang'). The change handler is `if(e.target.closest('[data-lang-sel]')){setLangCookie(e.target.value);location.reload();return}` (actions.js:398), i.e. cookie-only. collectSection only reads `[data-bind]` elements (setting\u2026",
    "location": "src/ui/panel/settings.js:39 vs src/ui/panel/actions.js:398",
    "problem": "It is a zombie registry entry: the drift test counts the FL('language') path as 'bound' because it matches a descriptor, but the renderer produces no data-bind, so the save pipeline never sees it. Same shape as killSwitch (FL('killSwitch','instantbool') at settings.js:38 emits `data-kill` and goes through the dedicated /api/killswitch endpoint \u2014 that one is intentional instant-apply), but for lang\u2026",
    "recommendation": "Either include language in the general-section patch (switch the renderer to data-bind and let save persist it), or drop FL('language') from the registry and treat language as pure cookie state with an explicit comment in the drift test's exception list.",
    "severity": "medium",
    "source": "src/ui/panel/actions.js:398",
    "title": "language is a zombie registry entry: rendered in General, but the control only writes a cookie \u2014 settings.language is never persisted from the panel"
   },
   {
    "effort": "M",
    "evidence": "Hard client rules come only from SCALAR_RULES, which registers exactly: num fields with min/max (earlyDataMaxBytes 0-8192, urlTestIntervalSec 60-86400, subUpdateIntervalHours 1-168, maxNodesPerFormat 1-2000 \u2014 4 paths), `f.type==='secret'&&f.gen==='uuid'` (vlessUuid, vmessUuid \u2014 2 paths), `f.vtype==='domain'` (echServerName \u2014 1 path), and `SCALAR_RULES.securePath={kind:'required'}` (settings.js:573\u2026",
    "location": "src/ui/panel/settings.js:573-576 (SCALAR_RULES builder)",
    "problem": "The panel never blocks or previews: every one of these fields saves first and reports 'must be an integer between 1 and 1500'-style English strings afterwards \u2014 painful in the FA locale where showFieldError falls through to the raw English message (settings.js:510 `...?msg:t(msg)`). Rival panels (sing-box subscribers, BPB) do the same, so it is not unusual \u2014 but this repo's descriptor table makes \u2026",
    "recommendation": "Generate client rules from the descriptor table: extend the drift test to assert FL min/max/vtype/maxLen match SETTING_FIELD_DESCRIPTORS, then add pattern/char-count for the four protocol paths and a blur rule for echServerName that mirrors ECH_SERVER_NAME_RE. Effort M because it is a generator + test change, not 30 hand edits.",
    "severity": "medium",
    "source": "src/settings/validate.ts",
    "title": "Client-side pre-validation covers 8 of ~55 editable paths; the other ~30 fields rely on server round-trip with English-only errors"
   },
   {
    "effort": "M",
    "evidence": "The identically-rendered line editors fail differently server-side: proxyIps (`FL('proxyIps','list',...,{validate:'host_port'...})`, settings.js:82) \u2014 server-side `sanitizeStrArray` SILENTLY DROPS lines that fail HOST_TOKEN_RE (validate.ts strArrayField/sanitizeStrArray: `if (opts.pattern && !opts.pattern.test(item)) continue;`), while remoteSubUrls/sourceUrls (validate:'url') hit urlListField whi\u2026",
    "location": "src/settings/validate.ts (sanitizeStrArray vs urlListField) vs src/ui/panel/settings.js:82-84,229",
    "problem": "An admin pasting 64 proxy IPs where 3 are malformed gets no error and loses 3 entries with no indication; the same admin pasting 16 sub URLs with one http:// line gets a whole-save rejection. Neither behavior is discoverable from the UI's identical rendering (red 'bad lines' hint + Save).",
    "recommendation": "Pick one policy per family: reject-with-field-error for all of proxyIps/nat64Prefixes (aligning with urlListField), or surface dropped-line counts in the save response and toast. Also relax the client url validator to accept http:// to match the server.",
    "severity": "medium",
    "source": "src/settings/validate.ts",
    "title": "Same-looking list fields have three different invalid-line behaviors: silent drop (proxyIps, nat64Prefixes), hard fail (remoteSubUrls), or validate-anything (allowedIps, alpn)"
   },
   {
    "effort": "S",
    "evidence": "`case 'ports':{...}` in bindHtml (settings.js:222) renders a fieldset of TLS_PORTS/PLAIN_PORTS checkboxes with a data-port-master select-all, and readBind/writeBind both have data-type==='ports' branches (settings.js:405,429), plus updatePortMasters() runs on every render (settings.js:340,326,506) \u2014 but no FL() call anywhere declares type 'ports' (grep `,'ports',` = 0 hits), so the branch is unrea\u2026",
    "location": "src/ui/panel/settings.js:222-227,340,405,429 vs src/ui/panel/dict.js:3,20",
    "problem": "Roughly 44 unique dead keys x2 languages (~88 entries, dict.js is 65KB total) and one dead renderer branch. The drift test only checks that keys REFERENCED BY FL exist \u2014 it never checks the reverse, so removed features leave permanent sediment that misleads contributors (and inflates the bilingual bundle in every panel load).",
    "recommendation": "Delete the 'ports' bind case + branches + ports-master helpers, add a reverse dict-drift assertion (every key referenced outside dict.js must be referenced somewhere, or moved to a dead-key allowlist), then sweep ports.*/checker.*/nav.warp/nav.checker/tabs.settings.ports/tabs.settings.proxyip.",
    "severity": "medium",
    "source": "src/ui/panel/settings.js:222",
    "title": "Dead 'ports' renderer branch and ~44 orphaned dictionary keys (x2 languages) from removed port-matrix and IP-checker features"
   },
   {
    "effort": "S",
    "evidence": "`FL('proxyIpPoolUrl','str','egress.poolUrl.label','egress.poolUrl.hint',{mono:true,vtype:'url'})` (settings.js:85) \u2014 but the SCALAR_RULES builder only registers `f.type==='num'`, `f.type==='secret'&&f.gen==='uuid'`, and `f.vtype==='domain'` (settings.js:573-575); there is no 'url' branch and blurValidateEl has no url kind (settings.js:578-589). validateLine's 'url' kind applies only to textareas v\u2026",
    "location": "src/ui/panel/settings.js:85 vs settings.js:573-575",
    "problem": "The metadata suggests a client check that does not exist \u2014 an easy future bug when someone assumes vtype:'url' works like vtype:'domain'.",
    "recommendation": "Either register `{kind:'url'}` in SCALAR_RULES for f.vtype==='url' (3 lines) or remove the dead annotation.",
    "severity": "low",
    "source": "src/ui/panel/settings.js:85",
    "title": "Dead validation metadata: FL('proxyIpPoolUrl',...,{vtype:'url'}) is consumed by nothing"
   },
   {
    "effort": "S",
    "evidence": "TOTP rows exist in the table (`{ path: \"totp.enabled\", spec: { kind: \"bool\" } }`, `{ path: \"totp.secret\", spec: { kind: \"custom\" } }`, `{ path: \"totp.recoveryCodes\", spec: { kind: \"custom\" } }`, fields.ts:144-146) and are excluded from the FL registry by design (fields.spec.ts: 'keeps totp out of the generic panel registry behind the custom card'). The UI's totpCardHtml/settings.js TOTP machinery \u2026",
    "location": "src/settings/fields.ts:144-146 vs src/ui/panel/settings.js (totpCardHtml) and src/ui/panel/actions.js:268",
    "problem": "None functionally \u2014 coverage is complete and intentional. This entry exists so the census is not read as '8 fields missing from the UI'.",
    "recommendation": "None; optionally document the API_MANAGED_PATHS list in the settings README so future contributors do not 'fix' the drift test exceptions.",
    "severity": "low",
    "source": "test/settings/fields.spec.ts:12",
    "title": "Descriptors with no UI are all justified: 8 API-managed (password/seed/totp) verified against their dedicated flows"
   },
   {
    "effort": "S",
    "evidence": "count descriptor rows = 76 (52 top-level + 24 nested). AGENTS.md states: 'src/settings/ ... validate (66 leaf fields)'. The descriptor table (and therefore validate.ts) actually processes 76 paths including the totp and telegram blocks added later.",
    "location": "AGENTS.md (Architecture Map, src/settings/ entry)",
    "problem": "Stale documentation understates the validated surface by 10 paths; agents relying on AGENTS.md will under-test new fields.",
    "recommendation": "Update AGENTS.md to '76 descriptor paths' or phrase it as 'descriptor-table-driven' without a hard count.",
    "severity": "low",
    "source": "src/settings/fields.ts",
    "title": "Census byproduct: AGENTS.md claims '66 leaf fields' but the descriptor table has 76"
   }
  ],
  "removals": [
   "src/ui/panel/settings.js:222-227 `case 'ports'` renderer branch (no FL field ever instantiates it)",
   "src/ui/panel/settings.js:405-410 and 429-433 data-type==='ports' branches in readBind/writeBind",
   "src/ui/panel/settings.js:340 updatePortMasters + its three call sites (326, 506) \u2014 operate on an element class nothing renders",
   "src/ui/panel/dict.js dead i18n keys x2 languages: ports.* (23 keys), checker.* (19 keys), nav.warp, nav.checker, tabs.settings.ports, tabs.settings.proxyip",
   "src/ui/panel/settings.js:564 dead checker-targets conditional (the textarea it guards no longer exists; MAX_TARGETS kept alive only for it)",
   "src/ui/panel/settings.js:85 vtype:'url' annotation on proxyIpPoolUrl (never consumed by SCALAR_RULES builder)"
  ],
  "summary": "The descriptor-table discipline is real: 76 descriptor paths in fields.ts, 68 unique FL-registry paths in settings.js (67 real + 1 documented pseudo-field __privateDoh), with only sourceUrls bound outside the registry \u2014 the repo's own drift test (test/settings/fields.spec.ts, 19/19 passing) enforces exactly this diff, so rivals like BPB's untyped settings blob are beaten here. But structural sync is not semantic sync: the census exposed two zombie controls (language writes a cookie and never persists settings.language; killSwitch is registry-declared yet saved via a separate endpoint) and one genuinely broken field \u2014 securePath is rendered, confirmed and 'saved' while handleSaveSettings strips it server-side and the UI then redirects to a URL that never becomes real. Client-side validation covers only 8 of ~55 editable paths, and the remaining ~30 fields lean on server round-trips with English-only error text in a bilingual panel, plus ~44 dead dictionary keys and a dead 'ports' renderer left behind by removed features."
 },
 {
  "consolidations": [
   "The two one-time share surfaces \u2014 the create-time auto-copy + #m-qr auto-open (actions.js:493) and the rotation modal #m-rot (shell.html:78) \u2014 should merge into a single ShareSheet component (URL text + copy + QR + 'shown once' warning) used by both create and rotate.",
   "The dead u.token branch in userRowHtml and the never-rendered tokenHint field must resolve to ONE design (hint-based rows + explicit re-issue flow) instead of coexisting as half-implementations of the same contract.",
   "Home's Users card (home.js:173) and the Users table (users.js:3-6) each independently recompute 'active user' status \u2014 extract one shared status/summary helper derived from the same PublicUser fields.",
   "Reuse the established empty-state primitive (.empty-card with icon/title/message/CTA, already used for subscriptions in home.js and WARP's empty_title/empty_msg) for the Users empty state instead of a bespoke field__hint table row."
  ],
  "dimension": "users-page-purpose-audit",
  "findings": [
   {
    "effort": "M",
    "evidence": "users.js:10-15: `const url=u.token?userSubUrl(u.token):'';` then `clip=url?(...copy+QR buttons...):''` and the cell `'<td data-l=\"'+esc(t('users.col.token'))+'\">'+(url?'<code ...>'+esc(url)+'</code>':'<span class=\"field__hint\">'+esc(t('users.toast.regen'))+'</span>')+'</td>'`. But the API contract provably never returns `token` on list rows: test/users/api.spec.ts:115 'create exposes the full toke\u2026",
    "location": "src/ui/panel/users.js:10-15 + src/handlers/api/users.ts:188-201 + test/users/api.spec.ts:115-134",
    "problem": "The column literally headed 'Subscription URL' (users.col.token) can never display a URL and its copy/QR buttons are dead code; instead every row permanently shows the string 'New token ready' \u2014 a toast message reused as a table cell, semantically false (nothing was regenerated). The primary artifact of the whole feature \u2014 the shareable link \u2014 is visible exactly once, at creation, and unrecoverabl\u2026",
    "recommendation": "Render `tokenHint` in the token column with an explicit state ('link shown once at creation \u2014 regenerate to re-issue'); delete the dead u.token branch; make rotate a first-class 're-issue link' action whose modal (the existing #m-rot) is the canonical place a link is displayed. If showing live links is a hard requirement, that is an architecture decision (store encrypted plaintext) to escalate, no\u2026",
    "severity": "critical",
    "source": "src/ui/panel/users.js",
    "title": "Token column can never show a URL \u2014 dead branch against the hashed-token API contract; per-row copy/QR never render; cell shows toast text 'New token ready' forever"
   },
   {
    "effort": "S",
    "evidence": "renderUserRows: `tb.innerHTML=S.users.length?S.users.map(userRowHtml).join(''):'<tr><td colspan=\"6\"><span class=\"field__hint\">'+esc(t('users.empty'))+'</span></td></tr>'` \u2014 t('users.empty') = 'No users yet' / '\u0647\u0646\u0648\u0632 \u06a9\u0627\u0631\u0628\u0631\u06cc \u0646\u06cc\u0633\u062a'. The only Add affordance is the small btn--sm in the card head. Meanwhile home.js builds a real `.empty-card` with icon/title/message/primary CTA for subscriptions, and WAR\u2026",
    "location": "src/ui/panel/users.js:29 (renderUserRows)",
    "problem": "The page answers 'no users' with the lowest-effort empty state in the app: a muted hint line inside an empty 6-column table, no illustration, no explanation of what creating a user gives you (scoped link, protocol filter, quota, expiry), no primary CTA. A first-time admin landing here learns nothing and feels the feature is unfinished. Confirms owner pain point 3.",
    "recommendation": "Reuse the existing .empty-card pattern: icon, 'No users yet', one sentence ('Give each person their own subscription link \u2014 you control protocols, daily limits and expiry'), and a primary 'Create first user' button that opens the modal. Mention the 50-user capacity.",
    "severity": "high",
    "source": "src/ui/panel/users.js",
    "title": "Zero-user state is one muted line ('No users yet') in a bare table skeleton \u2014 no CTA, no explanation, while the repo already has a proper empty-card component used elsewhere"
   },
   {
    "effort": "M",
    "evidence": "withHits returns `{...sanitizeUser(u), todayHits: await getUserHits(env, u.tokenHash)}`; sanitizeUser includes expiresAt, dailyReqLimit, protocols, addressOverride, tokenHint, createdAt. userChip (users.js:3-6) collapses it to 4 states: `key=!u.enabled?'users.status.disabled':expired?'users.status.expired':(u.dailyReqLimit!=null&&(u.todayHits||0)>=u.dailyReqLimit)?'users.status.limited':'users.sta\u2026",
    "location": "src/handlers/api/users.ts:188-191 vs src/ui/panel/users.js:3-15 (userChip, userRowHtml)",
    "problem": "The admin cannot answer 'when does this user expire?' or 'how close is this user to their cap?' without opening the edit modal per user; todayHits is fetched from KV/D1 for every user on every list call and then used only in one >= comparison, never displayed. Protocol scoping and address overrides are likewise write-only. The Status chip and the Enabled toggle column also encode overlapping state\u2026",
    "recommendation": "Add expiry countdown column (relative + absolute on hover), '23/100 today' progress (bar or text) using todayHits/dailyReqLimit, protocol chips (or 'All'), an override badge, and drop or merge the redundant Enabled column into the chip.",
    "severity": "high",
    "source": "src/handlers/api/users.ts",
    "title": "The table withholds nearly everything the API already sends \u2014 no expiry date, no limit value, no today count, no protocol scope, no override badge; a 4-state chip is the entire read surface"
   },
   {
    "effort": "S",
    "evidence": "`const { allowed, hits, total } = await consumeUserHit(env, token, user.dailyReqLimit);` \u2014 the only consumer of consumeUserHit in src/ (grep: users-sub.ts:77 only; tunnel handlers never touch it). Enforcement: `if (!allowed) return plain(429, \"daily quota exceeded\", { \"Retry-After\": String(secondsUntilUtcMidnight(now)) })`. Dict: 'users.limit':'Daily request limit', 'users.limit_none':'Empty means\u2026",
    "location": "src/handlers/users-sub.ts:77-84 vs src/ui/panel/dict.js (users.limit / users.status.limited)",
    "problem": "An admin reading 'Daily request limit' will assume it caps the user's proxy traffic. It actually caps how many times per day the client may download its subscription file \u2014 a client auto-refreshing every hour burns 24 of the budget, while a user streaming through the tunnel consumes zero. 'Limit reached' on a user row therefore means 'this person's client can't refresh configs until UTC midnight',\u2026",
    "recommendation": "Relabel to 'Daily subscription fetch limit' (or localize an explicit explanation: 'counts subscription downloads, not traffic; resets 00:00 UTC') in both EN and FA; surface the Retry-After/reset semantics in the edit modal hint.",
    "severity": "high",
    "source": "src/handlers/users-sub.ts",
    "title": "'Daily request limit' is a quota on subscription downloads, not proxy traffic \u2014 the UI copy never says so"
   },
   {
    "effort": "M",
    "evidence": "Route: `if (rest.length === 2 && rest[1] === \"activity\" && method === \"GET\") return jsonOk({ activity: await getUserActivity(env, user.tokenHash, days) });`. Grep for 'activity' in src/ui/panel/*.js \u2192 no matches. Grep for recordUserActivity outside store.ts \u2192 no matches; the only activity write is `await bufferActivity(env, h, { requests: 1 });` (store.ts:818, consumeUserHitKv) and the D1 upsert i\u2026",
    "location": "src/handlers/api/users.ts:175-180, src/users/store.ts:818, D1_SCHEMA user_activity table",
    "problem": "The store maintains a whole per-user, per-day telemetry subsystem (user_activity table, KV keys, buffers, migration logic) whose byte counters are never populated by anything and whose read endpoint the panel never calls. Users of a Users page want exactly this: 'is this person actually using their link? how much this week?'. Currently the data is unfetchable-by-UI and the byte half is unfetchable\u2026",
    "recommendation": "Either wire it (per-user detail view with a 7/30-day sparkline of requests; drop the always-zero bytes columns from the response or populate them from relay accounting) or delete the dead byte plumbing. Keeping dormant schema + endpoint + no consumer is the worst of the three.",
    "severity": "medium",
    "source": "src/users/store.ts",
    "title": "Per-user activity feature is triple-dead: /activity endpoint has zero UI callers, bytesUp/bytesDown are never written by anything, and the activity sparkline data is unusable"
   },
   {
    "effort": "S",
    "evidence": "`else{const d=await api('api/users',{method:'POST',body});const tok=d&&d.user&&d.user.token;closeModal('m-user');toast(t('users.toast.created'),'ok');if(tok){const url=userSubUrl(tok);copyText(url);openQr(url)}}` \u2014 openQr targets #m-qr (shell.html:72) which contains only a canvas, hint text and Download PNG; no URL string. By contract (api.spec.ts:119) this POST response is the last time the plain\u2026",
    "location": "src/ui/panel/actions.js:493 (mu-go handler), src/ui/panel/shell.html:72 (#m-qr), src/ui/panel/shell.html:78 (#m-rot)",
    "problem": "copyText() is best-effort; on a denied clipboard permission or non-secure context it fails without feedback, and the admin who closes the QR modal without scanning can never retrieve the URL \u2014 the table shows 'New token ready' instead (see token-column finding). The correct pattern already exists in-repo (#m-rot: full URL text + copy + QR) but is gated behind destroying the current token. Neither \u2026",
    "recommendation": "Route creation through the same one-time share sheet as rotation: URL as text + copy button + QR + explicit 'you will not see this link again \u2014 copy it now'. Show a failure toast if clipboard write fails.",
    "severity": "medium",
    "source": "src/ui/panel/actions.js",
    "title": "Create flow shows the token exactly once with no URL text and no 'shown once' warning; the good pattern exists only behind token destruction"
   },
   {
    "effort": "S",
    "evidence": "parseAddressOverride accepts `host` and `sni` fields with full hostname validation; the modal's handler collects only `ovAddr/ovPort/ovLabel` and builds `{address, port?, label?}`. The override block reuses generic labels (`addresses.field.address`, `addresses.field.port`, `addresses.field.label`) and its hint ('Optional: pin this user to a specific endpoint (address, port).') never mentions host/\u2026",
    "location": "src/ui/panel/shell.html:77 (#m-user override fields) + src/ui/panel/actions.js:489-492 vs src/handlers/api/users.ts:90-113",
    "problem": "A capability exists in the API (pin a user to a specific connect host/SNI, e.g. a cleaner IP with its own SNI) that the panel silently does not expose; an admin editing an override created out-of-band would also see host/sni vanish on save since the UI PUT sends addressOverride as a full object. Feature/UI drift in the exact area the descriptor table is supposed to keep in sync.",
    "recommendation": "Add host/SNI inputs to the override block (reuse the addr-card fields already used in the addresses section) or explicitly drop them from the API.",
    "severity": "medium",
    "source": "src/handlers/api/users.ts",
    "title": "Address-override drift: UI sends a strict subset of the API \u2014 host and sni override fields are API-only and invisible in the panel"
   },
   {
    "effort": "L",
    "evidence": "SECTIONS: `{key:'users',cards:[{title:'users.title',users:true,fields:[]}]}` is the 9th of 10 subtabs (general\u2026users,warp). showSection handles `if(sec==='warp')loadWarpIfNeeded().then(()=>{if(warpId)renderWarpDetail(warpId)...})` \u2014 no users equivalent; no `#/settings/users/<id>` route exists. Home card: `uc.innerHTML='...<a ... href=\"#/settings/users\">Manage</a>...<div id=\"home-users\">...'` rende\u2026",
    "location": "src/ui/panel/settings.js (SECTIONS, users key) + src/ui/panel/home.js:71-76,115,169-174",
    "problem": "The product's multi-user capability \u2014 arguably its differentiator over BPB/edgetunnel, which are single-admin single-link \u2014 is presented as a configuration section rather than an object space. WARP accounts get deep-linkable detail pages with copy-fields; users get a modal with no URL, no usage, no route. Home's Users card is a dead-end stat pair that duplicates the table's own status computation.",
    "recommendation": "Promote Users to a first-class view (top-level tab or Home section) with per-user detail route mirroring the WARP pattern (list \u2192 detail with link/token state, usage, activity, actions). If IA change is out of scope, at minimum give user rows a deep link and the home card real content (recent activity, expiring-soon warnings).",
    "severity": "medium",
    "source": "src/ui/panel/settings.js",
    "title": "IA: Users is settings subtab #9 of 10 with no per-user detail route, while WARP accounts got deep links (#/settings/warp/<id>) and a detail renderer"
   },
   {
    "effort": "S",
    "evidence": "`h='<div class=\"section-actions\"><button type=\"button\" class=\"btn btn--primary btn--sm\" data-action=\"section-save\" data-sec=\"'+s.key+'\">'+esc(t('common.save'))+'</button>...'` is applied unconditionally; the users card is `{key:'users',cards:[{title:'users.title',users:true,fields:[]}]}` with zero data-bind fields, and applySection's diffSection collects only [data-bind] elements.",
    "location": "src/ui/panel/settings.js (renderSettings section-actions injection) + SECTIONS users entry",
    "problem": "The Users tab shows a primary 'Apply changes'/'Save' button plus an unsaved-changes hint system that can never apply to it \u2014 user mutations are instant API calls. Clicking it silently no-ops (empty patch \u2192 return). Same applies to the WARP and Sources tabs. Furniture that does nothing undermines trust in the sections that do need it.",
    "recommendation": "Skip the section-actions bar for card-based sections (users/warp/sources) or hide it when a section has no bindable fields.",
    "severity": "low",
    "source": "src/ui/panel/settings.js",
    "title": "Dead 'Apply changes' bar on the Users tab \u2014 an injected section-save button that silently does nothing"
   },
   {
    "effort": "S",
    "evidence": "`'users-bulk-extend'(){const pick=$('users-bulk-expiry');const v=pick?pick.value:'';if(!v){toast(t('users.bulk.empty_expiry'),'err');return}bulkUsers({expiresAt:new Date(v).getTime()})}` \u2014 v is always required; parseBulkOp would accept `expiresAt: null` (parseExpiry returns null for null) but the UI can't produce it. parseBulkOp rejects any patch keys besides enabled/expiresAt/delete.",
    "location": "src/ui/panel/actions.js:153 + src/handlers/api/users.ts (parseBulkOp)",
    "problem": "Bulk can enable/disable/delete/set-expiry but cannot clear an expiry or batch-change protocol scopes/daily limits \u2014 so a common 'extend everyone 30 days' or 'revoke everyone's override' workflow requires per-user edits. Small n (\u226450) limits the damage, but the bulk bar advertises more power than it delivers.",
    "recommendation": "Add a 'Never (clear)' option to bulk extend; consider bulk limit/protocol patches server-side already forbids \u2014 either extend parseBulkOp or document why.",
    "severity": "low",
    "source": "src/ui/panel/actions.js",
    "title": "Bulk bar can set but never clear expiry; no bulk protocol/limit edits; server would accept clearing but UI never sends it"
   },
   {
    "effort": "S",
    "evidence": "POST handler: `if (fresh.length >= MAX_USERS) throw new ValidationError({ limit: `at most ${MAX_USERS} users` });` \u2014 surfaced via `$('mu-error').textContent=Object.values(err.fields)[0]` in the modal, English in both locales (known repo-wide gap, but acute here where the limit is invisible). The table has no search input, no sort, no status filter; the select-all/bulk machinery is the only navigat\u2026",
    "location": "src/handlers/api/users.ts (POST, MAX_USERS check) + src/ui/panel/users.js (usersCardHtml, no search/capacity UI)",
    "problem": "For a list that can hold 50 rows the panel offers no way to find a user, filter to 'expired' or 'limit reached', or see how close the deployment is to its cap before a failed create explains it in English inside a Persian UI.",
    "recommendation": "Show '7/50 users' near the Add button; add a status filter chip row (All/Active/Expired/Limit reached/Disabled) and a name search \u2014 all client-side over the already-loaded S.users array.",
    "severity": "low",
    "source": "src/handlers/api/users.ts",
    "title": "No search/sort/status filter for up to 50 rows and no capacity indicator; the 50-user cap surfaces only as a raw English error string"
   },
   {
    "effort": "S",
    "evidence": "mu-go builds `const picked=[...document.querySelectorAll('#mu-protocols input[type=checkbox]:checked')].map(i=>i.value);const body={...protocols:picked.includes('all')?'all':picked};` \u2014 empty pick submits [] and server throws `ValidationError({ protocols: \"must include at least one protocol\" })`; `$('mu-error').textContent=Object.values(err.fields)[0]`. parseLimit rejects n<1 and n>10000 with Engl\u2026",
    "location": "src/ui/panel/actions.js:482-496 (mu-go) + src/handlers/api/users.ts (parseProtocols, parseLimit)",
    "problem": "The mutual-exclusion wiring (All unchecks individuals, individuals uncheck All) is correct, but the form has no client guard for the two most likely mistakes (no protocol picked, limit 0/10001+), so users of the FA panel get English engineering strings where a localized hint belongs. Known repo gap ('validation messages are English-only'), but this modal composes the body client-side and could pre\u2026",
    "recommendation": "Client-side guard: disable the submit button (or show a localized error) when no protocol is checked; clamp/validate the limit against 1..10000 before submit.",
    "severity": "low",
    "source": "src/ui/panel/actions.js",
    "title": "Modal validation gaps: zero protocols selected submits [] and surfaces raw English server text even in FA; limit field min=1 but 0/10001+ also surface raw English"
   }
  ],
  "removals": [
   "users.js:10-11,15 \u2014 the dead `u.token?userSubUrl(u.token)` branch, its never-rendered copy/QR `clip` markup, and the `users.toast.regen` toast-string-as-placeholder swap in the token cell.",
   "user_activity bytesUp/bytesDown write/read plumbing (D1 columns, KV parseActivityValue fields, API response fields) if there is no near-term plan to populate them \u2014 they are permanently zero and misrepresent the endpoint's data.",
   "The section-actions Apply/Save bar (and dirty '*' subtab marker) injected onto the users/warp/sources panels where no data-bind fields exist and the button is a silent no-op."
  ],
  "summary": "The Users page is a write-only CRUD table that defeats its own purpose: the column headed \"Subscription URL\" can never render a URL (a dead `u.token` branch against the API's documented hashed-token contract, test/users/api.spec.ts:115), so a user's link exists exactly once \u2014 auto-copied at creation with no URL text and no warning \u2014 and is unrecoverable afterwards without destroying it, while quota, expiry, protocol scope, token hint, and the entire /activity endpoint are fetched or built server-side and never displayed. The zero-user state is a single muted \"No users yet\" line inside an empty table skeleton, in an app that already ships a proper icon/title/CTA empty-card for subscriptions and WARP and simply didn't use it here. As built this is settings furniture, not the identity hub (create \u2192 hand link \u2192 watch usage) the feature needs; nearly every fix is presentation work because the backend already returns every number the page should be showing."
 },
 {
  "consolidations": [
   "Sources subtab merges into Egress: both answer 'where do relay IPs come from' (sourcesCardHtml in warp.js:53 feeds pool tools; egress already hosts the pool card) \u2014 one subtab, two cards.",
   "Fragment + Chain proxy merge into a single 'Tunnel' subtab: each is a lone single-card section (settings.js:87-97) and together they strip-bloat the 10-pill row; the fpreset chip row already behaves like a mini control panel.",
   "Home users summary card and the Users view merge once Users is top-level: card becomes stats + 'Add user' primary + 'View all' link to #/users, eliminating the duplicate Manage ghost button (home.js:115).",
   "WARP account list and its subscription URLs consolidate: instead of management in Settings\u2192WARP and URLs duplicated in collapsed <details> on Home, one accounts grid where each account row exposes top-3 formats inline + 'All formats' to the detail page.",
   "Kill switch controls consolidate conceptually but keep both surfaces (Home instant + Settings instantbool) \u2014 they already share setKillSwitch/syncKillUI; only ensure they stay in sync when promoting tabs (they do, via document-level [data-kill] queries, home.js:130)."
  ],
  "dimension": "Information Architecture & Navigation \u2014 Q Proxy admin panel",
  "findings": [
   {
    "effort": "S",
    "evidence": "chrome.js:79: if(mod&&e.key.toLowerCase()==='k'){e.preventDefault();const s=document.querySelector('#settings-search');if(s)s.focus();else location.hash='#/home';return} \u2014 grep over all panel sources finds zero occurrences of 'settings-search' outside this handler; chrome.js:40 advertises ['Ctrl / \u2318 + K','shortcuts.search'] and dict.js defines 'shortcuts.search':'Focus search, or go home'.",
    "location": "src/ui/panel/chrome.js:79 (handler), chrome.js:40 (advertised shortcut row)",
    "problem": "The only fast way to traverse a 10-subtab / 66-field settings surface does not exist: the shortcut is listed in the shortcuts modal, the handler is live, and the target input was never rendered. First-time admins press Ctrl+K expecting a palette and get teleported to Home with no feedback.",
    "recommendation": "Either build a minimal command palette (input id=\"settings-search\" in shell.html that filters SECTIONS + field labels and jumps hash routes), or delete the Ctrl+K row from renderShortcuts (chrome.js:40) and the handler (chrome.js:79) plus the 'shortcuts.search' dict key. Wired palette is the higher-value option given ten subtabs.",
    "severity": "high",
    "source": "src/ui/panel/chrome.js",
    "title": "Advertised Ctrl+K search is a dead affordance \u2014 the element it focuses does not exist anywhere"
   },
   {
    "effort": "M",
    "evidence": "settings.js:124-125: {key:'users',cards:[{title:'users.title',users:true,fields:[]}]},{key:'warp',cards:[{warpAccounts:true,fields:[]},{warpPresets:true,fields:[]},{warpAmnezia:true,fields:[]}]}. Users card has fields:[] and renders an immediate-write CRUD table (users.js:8); WARP cards are zero-field entity cards with instant-PUT actions (actions.js warp-save/warp-regen). The subtab strip is over\u2026",
    "location": "src/ui/panel/settings.js:124-125 (SECTIONS tail), src/ui/panel/shell.html:42-44 (nav), src/ui/panel/users.js:8 + warp.js:52-56 (immediate-write cards)",
    "problem": "The panel's two entity domains (user subscription links, WARP accounts) are disguised as configuration. They bypass the Settings snapshot/dirty/apply contract entirely (users.js bulkUsers and actions.js warp-* fire api PUT immediately + toast), so the container is mislabeled, and their position (subtabs 9-10) makes the highest-traffic destinations the hardest to reach \u2014 first WARP config costs 5 a\u2026",
    "recommendation": "Promote both to top-level tabs: shell.html gains tab-users (#/users) and tab-warp (#/warp) between Home and Settings; parseRoute (home.js:55-61) gains {view:'users'}/{view:'warp'} branches with history.replaceState redirects from #/settings/users and #/settings/warp* for backward compatibility; SECTIONS drops to 8 config-only subtabs; buildSubtabs/showSection split into per-view renderers. Update \u2026",
    "severity": "high",
    "source": "src/ui/panel/settings.js",
    "title": "Users and WARP are settings subtabs 9\u201310 of 10: entity CRUD wearing a settings costume, past the mobile fade mask"
   },
   {
    "effort": "S",
    "evidence": "users.js:29: tb.innerHTML=S.users.length?S.users.map(userRowHtml).join(''):'<tr><td colspan=\"6\"><span class=\"field__hint\">'+esc(t('users.empty'))+'</span></td></tr>' ('No users yet'). Contrast warp.js:63-65 which inserts a full .empty-card with icon, title, message and two CTAs (warp-generate-open / warp-import-open), and home.js:100 which builds an .empty-card whose CTA deep-links '#/settings/pro\u2026",
    "location": "src/ui/panel/users.js:29; contrast src/ui/panel/warp.js:63-65 and home.js:100",
    "problem": "Users is the only major surface with no designed empty state \u2014 no add button on the Home card, no CTA in the empty table row \u2014 which is exactly why the owner reports it as 'meaningless when there are no users'. The codebase already contains the correct pattern twice; it was simply not applied here.",
    "recommendation": "In renderUserRows, when S.users.length===0 render an .empty-card (icon, 'No users yet', one-line explanation, primary 'Add User' button wired to data-action=\"users-add\"); on the Home users card, add a primary Add-User button next to the Manage ghost link so the card is actionable at zero users.",
    "severity": "high",
    "source": "src/ui/panel/users.js",
    "title": "Users empty state is a bare table string \u2014 the only major surface without an empty-card CTA"
   },
   {
    "effort": "M",
    "evidence": "home.js:98: if(S.warp&&S.warp.accounts.length)sh+=warpSubsHtml(); \u2014 with zero accounts the Home subscriptions card shows nothing WARP-related. Account creation lives only in the settings subtab cards (warp.js:52 Generate/Import buttons) and both flows hard-jump into Settings: actions.js:461 closeModal('m-warp-generate');...location.hash='#/settings/warp/'+d.account.id and actions.js:467 (import). \u2026",
    "location": "src/ui/panel/home.js:98; src/ui/panel/actions.js:461,467; src/ui/panel/warp.js:52,99-107",
    "problem": "The owner's pain point quantified: grabbing a first WARP config costs 5 taps plus a settings detour while every other primary job costs 1-3. Home hides the WARP affordance until state exists, so a new admin has no signal WARP is even offered from the dashboard; the dict literally still contains 'nav.warp':'WARP' (dict.js:3) from a cut top-level tab.",
    "recommendation": "With a top-level WARP tab: Home subscriptions card renders a compact WARP group with account chips linking to #/warp/:id (replace the <details> collapse) plus a 'Set up WARP' empty-state CTA when accounts.length===0; after generate/import, land on the WARP view (#/warp/:id) instead of #/settings/warp/:id (actions.js:461,467).",
    "severity": "medium",
    "source": "src/ui/panel/home.js",
    "title": "First WARP config costs 5 actions + burrow; Home shows zero WARP affordance until an account exists"
   },
   {
    "effort": "S",
    "evidence": "settings.js:481: if(sec==='general'||sec==='addresses')await refreshSubUrls() \u2014 enabling VLESS under the protocols subtab saves without refreshing S.subs; grep shows S.subs is written only from api/bootstrap in boot() (actions.js:523) and refreshSubUrls (settings.js:470). No settings subtab displays any subscription URL (grep for subUrls in panel sources: only bootstrap/refresh paths), so the only\u2026",
    "location": "src/ui/panel/settings.js:481; grep: S.subs written only by api/bootstrap + refreshSubUrls",
    "problem": "An admin who enables a protocol (the exact empty-state CTA path home.js:100 pushes them down) returns to Home to a subscriptions card that may still say 'No subscriptions yet' or list stale URLs until a manual reload; nothing inside Settings ever shows the URL they just unlocked, reinforcing pain point #2.",
    "recommendation": "Refresh sub URLs whenever a protocols toggle changes S.subs \u2014 extend the condition to sec==='protocols' (cheap: refreshSubUrls re-renders Home), and add a collapsed 'Your subscription links' copy-list to the bottom of the protocols subtab so the payoff is visible in context.",
    "severity": "medium",
    "source": "src/ui/panel/settings.js",
    "title": "Home subscription URLs go stale after a protocol-only Apply; zero sub-URL surface inside Settings"
   },
   {
    "effort": "S",
    "evidence": "status.ts:93-101: buildSubUrls returns [...formats..., { format: 'base64', label: 'Panel info', url: `${base}?view=html` }] \u2014 last of 7; home.js:97 renders each entry as an identical label+copyField row sorted base64-first, so the info page sits at the bottom visually identical to the 6 format URLs.",
    "location": "src/handlers/api/status.ts:93-101 (buildSubUrls); render src/ui/panel/home.js:97",
    "problem": "The single most useful link for a novice (a browser-rendered page listing every format) is visually indistinguishable from the 'Base64/Mixed' duplicate format above it and ranked last; admins copy URLs they do not need and never discover the info page.",
    "recommendation": "Render the ?view=html entry as a distinct link row ('Open info page \u2197', anchor, not copy-field) pinned to the top or bottom of the subscriptions card with its own style, so the 6 machine formats and the human page are separable at a glance.",
    "severity": "medium",
    "source": "src/handlers/api/status.ts",
    "title": "'Panel info' HTML page mislabeled as a subscription format and ranked last of 7 rows"
   },
   {
    "effort": "S",
    "evidence": "actions.js:493 (create): const tok=d&&d.user&&d.user.token;...copyText(url);openQr(url) \u2014 auto-copy + QR. actions.js:144 (regen): const tok=d&&d.token;toast(t('users.toast.regen'),'ok');if(tok)showRotation(tok);await loadUsers() \u2014 rotation modal only, with users.js:31-37 showing the URL once and dict rotation.hint stating 'Copy it now \u2014 it is shown only once'.",
    "location": "src/ui/panel/actions.js:493 vs actions.js:144; modal src/ui/panel/users.js:31-37",
    "problem": "The two flows produce the same artifact (a fresh user sub URL) but the more destructive one (regen, which kills all existing links per users.confirm_regen_body) is the one that does not pre-copy, so a mis-tap between confirm and modal leaves the admin with a dead client and a one-shot URL they must read from a modal code field.",
    "recommendation": "In users-regen (actions.js:144), call copyText(userSubUrl(tok)) before showRotation(tok) so the new URL is on the clipboard the instant it exists, matching the create flow; keep the rotation modal as the visible confirmation.",
    "severity": "medium",
    "source": "src/ui/panel/actions.js",
    "title": "Regenerate-token does not auto-copy the new URL while user-create does \u2014 asymmetric one-shot secret handling"
   },
   {
    "effort": "S",
    "evidence": "dict.js:3/20 still define 'nav.warp':'WARP', 'nav.checker':'IP Checker', 'tabs.settings.ports', 'tabs.settings.proxyip', the full checker.* block (checker.title\u2026checker.tooMany), ports.* and proxyip.* families, 'warp.settings', 'warp.view.accounts', 'warp.view.settings' \u2014 grep for t('checker./t('ports./t('proxyip./t('nav.checker/t('warp.view. across panel JS returns no usages; shell.html has only \u2026",
    "location": "src/ui/panel/dict.js:3 (en), dict.js:20 (fa)",
    "problem": "The dictionary is archaeology of previous IA iterations (a flat nav with WARP and IP Checker top tabs, a ports/proxyip settings layout). It inflates the 65KB dict, and the orphaned nav.warp proves the intended IA existed and was cut without cleanup \u2014 misleading future contributors about what the shell supports.",
    "recommendation": "Delete the verified-unused key families (checker.*, ports.* except keys actually referenced by SECTIONS, proxyip.*, tabs.settings.ports/proxyip, nav.warp, nav.checker, warp.view.*, warp.settings) from both en and fa in dict.js; if the WARP top tab is adopted, re-harvest nav.warp instead of inventing a new key.",
    "severity": "low",
    "source": "src/ui/panel/dict.js",
    "title": "~40 dead i18n keys from prior IA iterations survive in both dictionaries"
   },
   {
    "effort": "S",
    "evidence": "home.js:55-61 parseRoute: return{view:'settings',sec:SECTIONS.some(s=>s.key===seg[1])?seg[1]:'general'} and terminal return{view:'home'} \u2014 no not-found handling; navigate() (home.js:63-69) silently history.replaceState's the coerced hash. shell.html:44: <a class=\"tab\" role=\"tab\" id=\"tab-settings\" href=\"#/settings/general\" data-view=\"settings\"> \u2014 the href is fixed to general.",
    "location": "src/ui/panel/home.js:55-61 (parseRoute), src/ui/panel/shell.html:44",
    "problem": "A typo'd deep link (or a stale bookmark after a section rename) lands you on Home or General with no signal anything was wrong; and because the Settings tab always targets general, any section-remembering must come from hash history \u2014 clicking away and back loses your place.",
    "recommendation": "Preserve last-visited settings section in sessionStorage and point the Settings tab href at it on render (or keep #/settings and let navigate() substitute); optionally toast 'Unknown section' when coercion fires. Low priority vs F2.",
    "severity": "low",
    "source": "src/ui/panel/home.js",
    "title": "Silent route coercion: unknown subtab falls back to General, unknown hash falls back to Home, Settings tab hardcodes #/settings/general"
   },
   {
    "effort": "S",
    "evidence": "actions.js:545: '<a class=\"btn btn--primary btn--sm\" href=\"#/settings/protocols\" onclick=\"document.getElementById(\\'wiz-skip\\').click()\">' \u2014 the CTA dismisses the wizard as it navigates, so step 2 (which displays the base64 sub URL, actions.js:546) is only ever rendered if the user reopens the panel later and steps forward manually; actions.js:549: $('wiz-skip').onclick=()=>{...localStorage.setIte\u2026",
    "location": "src/ui/panel/actions.js:545,549",
    "problem": "The onboarding teaches the settings-burrow as the canonical path (wizard.s3_body: 'Generate WARP accounts from Settings \u2192 WARP any time') and then evaporates: a first-time admin who follows step 1's CTA never sees the panel hand them their subscription URL, and the wizard cannot be recalled without clearing localStorage.",
    "recommendation": "Remove the inline onclick so step 1's CTA navigates while keeping the wizard open on step 2; add a 'Replay setup guide' entry (e.g. in the shortcuts modal or General) that deletes qp_wizard_done and calls maybeWizard(); update s3 copy when Users/WARP become top-level tabs.",
    "severity": "low",
    "source": "src/ui/panel/actions.js",
    "title": "Wizard is one-shot, un-reopenable, and its step-1 CTA closes the wizard while navigating \u2014 step 2 is only ever seen on the next session"
   }
  ],
  "removals": [
   "Dead dictionary families in dict.js en+fa (verified unreferenced across panel JS): checker.* block, ports.* block, proxyip.* block, tabs.settings.ports, tabs.settings.proxyip, nav.warp, nav.checker, warp.view.accounts, warp.view.settings, warp.settings.",
   "The <details>-collapsed per-account WARP URL list on Home (warp.js:99-107 / warpSubsHtml) once accounts are link-chips on Home or a top-level WARP tab \u2014 it duplicates the detail page and adds a hidden two-step copy path.",
   "The phantom 'shortcuts.search' row in renderShortcuts (chrome.js:40) and its Ctrl+K handler (chrome.js:79) if the command palette is not built \u2014 advertising dead affordances is worse than not advertising.",
   "The inline onclick=\"document.getElementById('wiz-skip').click()\" in the wizard step-1 CTA (actions.js:545) \u2014 it silently skips onboarding steps the CTA should lead into."
  ],
  "summary": "The interaction layer is genuinely excellent \u2014 copy/QR on everything, a debounced optimistic kill switch, per-subtab dirty markers, and real ARIA tab semantics \u2014 but it hangs on an inverted skeleton: a two-tab shell (Home/Settings, shell.html:43-44) that hides the panel's two entity domains, Users and WARP, as subtabs 9\u201310 of a 10-pill overflow strip with a mobile fade mask. The owner's three pain points are confirmed in code: WARP management is Settings-only (5 actions for a first config), the primary sub URL is actually the best-served object in the panel (1 action) while everything around it (stale refresh, mislabeled 'Panel info' row, no in-settings surface) rots, and Users is the only major surface shipped without the empty-card onboarding pattern the same codebase uses for WARP and subscriptions. Worst of all, the shortcuts modal advertises a Ctrl+K search that was never built (chrome.js:79 queries a #settings-search element that exists nowhere), silently dumping users on Home."
 },
 {
  "consolidations": [
   "Findings 1, 2 and 3 are one work item: a single 'light-theme token completion' pass adding dark-remapped --success/--warning/--danger, distinct light values for --text-faint/--text-ghost, and tokenizing .empty-title/.amz-tag/.swatch borders \u2014 after which every light contrast failure closes together.",
   "Findings 4 and 5 are one accent rework: either (a) delete the swatch picker and freeze one brand accent, or (b) restructure so each html[data-accent] block defines --accent/--accent-bright/--accent-pale per theme (dark+light), swatch gradients read from those tokens, and status tokens are renamed/shifted out of collision. Do not ship (a) and (b) separately.",
   "Findings 6 and 13 are one token-hygiene commit: define --line:var(--border), delete truly dead tokens (--ease-bounce, --accent-hover, --accent-down, --shadow-sm), adopt --ring in the three focus rules, and either adopt or delete --fs-xl/--r-sm/--r-lg.",
   "Findings 8 and 9 (type scale and spacing scale) should be one 'design-token completion' sweep with the visual-regression check, since both touch every component block in app.css.",
   "Findings 7 and 15 (hardcoded light-invisible chrome + hardcoded blue blob/logo) are one 'de-hardcode theme-bound values' pass mapping rgba(255,255,255,x) chrome and blue decorations onto theme tokens."
  ],
  "dimension": "visual-design-light-dark-english",
  "findings": [
   {
    "effort": "S",
    "evidence": "Light block (app.css:5) remaps only neutrals and shadows: \"html[data-theme=\"light\"]{--bg:#f8fafc;...--text:#0f172a;--text-mid:#334155;--text-dim:#64748b;--text-faint:#94a3b8;--text-ghost:#94a3b8...}\" \u2014 no --success/--warning/--danger anywhere in it. Meanwhile :root keeps dark-tuned values: \"--success:#34d399;--warning:#fbbf24;--danger:#f87171\". Consumers: app.css:210 \".chip-status.ok{background:va\u2026",
    "location": "src/ui/panel/app.css:5 (html[data-theme=\"light\"] token block), consumers at app.css:91,201,206,210,211,270",
    "problem": "Computed WCAG ratios in light mode: #34d399 on white \u22481.9:1, #fbbf24 \u22481.7:1, #f87171 \u22482.8:1 \u2014 all below the 4.5:1 (normal) and even 3:1 (large-text) floors. The \"Unsaved changes\" applybar label, per-field error text, kill-switch status chips ('Traffic serving/paused'), pool \u2713/\u2717 glyphs and section hint warnings are functionally invisible on a white card. Dark mode passes (~5\u201310:1); the failure is 1\u2026",
    "recommendation": "Add a light status ramp to the app.css:5 block: --success:#059669, --warning:#d97706, --danger:#dc2626 (keep the *-bg tints as-is), plus optionally light variants of --danger-strong. This is a ~4-line token fix covering every consumer.",
    "severity": "critical",
    "source": "E:/code/Q Proxy/src/ui/panel/app.css",
    "title": "Light theme never re-derives semantic status colors \u2014 success/warning/danger text fails WCAG AA everywhere"
   },
   {
    "effort": "S",
    "evidence": "app.css:5 sets both to the identical gray: \"--text-faint:#94a3b8;--text-ghost:#94a3b8\" (dark :root has two distinct steps: --text-faint:#94a3b8, --text-ghost:#76808f). Downstream: app.css:90 \".field__hint{font-size:var(--fs-sm);color:var(--text-faint)}\", app.css:203 \".tbl th{...color:var(--text-ghost)}\", app.css:104 char-count, app.css:173 addr-card field labels, app.css:190 pool-status, app.css:2\u2026",
    "location": "src/ui/panel/app.css:5; consumers at app.css:90,104,173,190,203,229,244,298",
    "problem": "#94a3b8 on #f8fafc \u22482.5:1 \u2014 fails AA for normal text (4.5:1) and even large text (3:1). Field hints are the highest-frequency text in the panel (every settings field has one); table column headers and the users 'No users yet' message also ride on these tokens. The two-step text hierarchy is additionally collapsed to one level in light mode, flattening metadata vs body distinction.",
    "recommendation": "In the light block set --text-ghost:#64748b (\u22484.8:1) and --text-faint:#475569 (\u22487:1) to restore both AA compliance and the two-step hierarchy. One-line fix.",
    "severity": "critical",
    "source": "E:/code/Q Proxy/src/ui/panel/app.css",
    "title": "Light theme collapses --text-faint and --text-ghost into the same #94a3b8 (~2.5:1 on white) \u2014 hints, table headers and metadata all fail AA"
   },
   {
    "effort": "S",
    "evidence": "app.css:228: \".empty-title{font-size:1rem;font-weight:600;letter-spacing:-.01em;color:#e5e7eb;margin-bottom:.375rem}\". The light override block (app.css:17) fixes only the container: \"html[data-theme=\"light\"] .empty-card{border-color:rgba(0,0,0,.09);background:rgba(0,0,0,.015)}\" \u2014 .empty-title and .empty-icon color are untouched. home.js renders these for 'No subscriptions yet' (home.js renderHome\u2026",
    "location": "src/ui/panel/app.css:228 (no html[data-theme=light] override; container fixed at app.css:17)",
    "problem": "#e5e7eb on white \u22481.1:1. In light mode every empty state shows a title you cannot see; the icon tint (accent rgba) is the only thing left. This directly degrades the owner's pain point: an empty Users page in light theme communicates nothing at first glance.",
    "recommendation": "Change to color:var(--text) (and give .empty-icon a light-mode color var) so both themes inherit correct contrast.",
    "severity": "critical",
    "source": "E:/code/Q Proxy/src/ui/panel/app.css",
    "title": "Empty-state title is hardcoded #e5e7eb \u2014 near-invisible (\u22481.1:1) in light mode, and empty states are exactly the pages with nothing else to look at"
   },
   {
    "effort": "M",
    "evidence": "app.css:2-4: \"html[data-accent=\"violet\"]{--accent-rgb:167,139,250;...--cyan:#a78bfa;--cyan-bright:#c4b5fd;--cyan-pale:#ddd6fe}\" (same pattern for green/amber). Then app.css:6, later in source with equal specificity (0,1,1): \"html[data-theme=\"light\"]{--cyan:#0891b2;--cyan-pale:#0e7490;--cyan-bright:#06b6d4}\" \u2014 it does not touch --accent-rgb. Solid fills read --cyan (app.css:114 .btn--primary, app.c\u2026",
    "location": "src/ui/panel/app.css:2-4 (data-accent blocks) vs app.css:6 (html[data-theme=\"light\"] cyan override), cascade applied at app.css:50,114,148,153,212",
    "problem": "CSS cascade: both selectors match the same html element at equal specificity; the later light block wins, so in light mode --cyan is ALWAYS #0891b2 regardless of chosen accent. Result: pick Violet in light mode and you get a cyan primary button, cyan switch tracks and cyan selected-tab text sitting inside violet accent-bg tints, violet focus rings and violet glows. Half the personalization feature\u2026",
    "recommendation": "Either derive the solid ramp from --accent-rgb once (color-mix or per-accent light values inside each data-accent block, e.g. html[data-accent=\"violet\"],html[data-theme=\"light\"] ... or a second block per accent), or scope the light cyan override so it only applies when data-accent is absent/unset.",
    "severity": "high",
    "source": "E:/code/Q Proxy/src/ui/panel/app.css",
    "title": "Accent switching is silently half-broken in light mode: cascade bug forces the cyan ramp over every chosen accent"
   },
   {
    "effort": "S",
    "evidence": ":root has \"--success:#34d399;--warning:#fbbf24\" (app.css:1); app.css:4 sets \"html[data-accent=\"amber\"]{--accent-rgb:251,191,36;...--cyan:#fbbf24;...}\" and app.css:3 green sets \"--cyan:#34d399\" \u2014 identical hex to the status tokens. Swatch previews (app.css:56-59): \".swatch--amber{background:linear-gradient(135deg,#fbbf24,#f97316)}\" and \".swatch--cyan{background:linear-gradient(135deg,#22d3ee,#3b82f\u2026",
    "location": "src/ui/panel/app.css:1,3,4,56-59; collision surface app.css:206-212 (.glyph-*, .chip-status, .dot--*)",
    "problem": "With the green or amber accent active, success/warning chips, dots, glyphs and the kill-switch 'Traffic serving' state become the same hue as primary buttons, selected tabs and switch tracks \u2014 status color stops being a signal. The swatches also lie: the amber swatch previews orange (#f97316 blend) but the applied accent is yellow, and in light mode the cyan swatch shows #22d3ee while the applied \u2026",
    "recommendation": "Cut to one brand accent (delete the swatch group + the three data-accent blocks + accent.* dict keys), or if kept: pick accent hues that don't collide with status tokens (e.g. indigo/teal instead of exact green/amber hexes) and generate swatch gradients from the same tokens that get applied, per theme.",
    "severity": "high",
    "source": "E:/code/Q Proxy/src/ui/panel/app.css",
    "title": "The 4 accent swatches are noise with real semantic cost: green accent = --success exactly, amber accent = --warning exactly, and swatch previews misrepresent the applied color"
   },
   {
    "effort": "S",
    "evidence": "app.css:246: \".token-panel{position:relative;overflow:hidden;border-radius:1rem;border:1px solid var(--line);...}\". The token --line is defined nowhere; app.css:216 uses the guarded form \".skel-card{border:1px solid var(--line,rgba(255,255,255,.06))}\" but 246 has no fallback. An undefined var() makes border-color invalid at computed-value time \u2192 initial value \u2192 currentColor (i.e. var(--text): near\u2026",
    "location": "src/ui/panel/app.css:246 (.token-panel); --line only ever written with fallback at app.css:216 (.skel-card)",
    "problem": "The WARP subscription-token panel renders with a harsh full-contrast 1px border instead of the intended subtle hairline \u2014 a visible glitch on a security-sensitive surface, in both themes.",
    "recommendation": "Define --line:var(--border) in :root (and the light block inherits), or replace var(--line) with var(--border) at app.css:246.",
    "severity": "high",
    "source": "E:/code/Q Proxy/src/ui/panel/app.css",
    "title": ".token-panel borders with an undefined variable \u2014 border-color resolves to currentColor, producing a full-strength text-colored 1px border"
   },
   {
    "effort": "S",
    "evidence": "app.css:154: \".seg{display:inline-flex;background:rgba(255,255,255,.04);border:1px solid rgba(255,255,255,.05)...}\"; app.css:197: \".subtab{...background:rgba(255,255,255,.04)...}\"; app.css:78: \".stat-chip{...border:1px solid rgba(255,255,255,.05)...}\"; app.css:59-ish .swatch border rgba(255,255,255,.18). The light block (app.css:7-20) overrides .topbar, .tabs, .tab:hover, .view-title, .dotgrid, .a\u2026",
    "location": "src/ui/panel/app.css:154 (.seg), 197 (.subtab), 78 (.stat-chip), plus .swatch border at app.css:53; light overrides only at app.css:7-20",
    "problem": "In light mode the language segment, the 10 settings subtab pills and the home stat chips lose their container/background and read as floating text with no chrome; the active subtab is the only visible one. Pro light dashboards keep a visible gray-100 control track for segmented controls.",
    "recommendation": "Replace the hardcoded white-alphas with tokens (--surface / --surface-2 already exist and remap correctly per theme), which also deletes the need for the .tabs light override at app.css:8.",
    "severity": "medium",
    "source": "E:/code/Q Proxy/src/ui/panel/app.css",
    "title": "Hardcoded white-alpha chrome has no light-mode override: .seg, .subtab, .stat-chip (and .toast close hover) become invisible in light theme"
   },
   {
    "effort": "M",
    "evidence": "Defined: \"--fs-xl:18px;--r-sm:8px;--r-lg:16px\" (app.css:1) \u2014 var() usage counts: fs-xl 0, r-sm 0, r-lg 0. Meanwhile hardcoded: app.css:62 \".view-title{font-size:1.5rem...}\" (24px, skipping the 18px rung), app.css:63 \".card{...border-radius:1rem...}\", app.css:228 \".empty-title{font-size:1rem...}\", app.css:229 \".empty-msg{font-size:.875rem...}\", app.css:197 subtab radius .65rem, app.css:154 seg .75r\u2026",
    "location": "src/ui/panel/app.css:1 (dead tokens) vs app.css:62,63,197,228,229 (hardcoded); src/ui/login.html:50 (--fs-2xl)",
    "problem": "The hierarchy relies on bold-vs-regular within a 2px delta: card titles (15px/700) barely out-rank 13px body, and the page h1 (24px gradient) floats disconnected from a type ramp that claims to end at 18px. The dead --fs-xl/--r-sm/--r-lg tokens plus login's --fs-2xl prove the scale was designed and then not adopted \u2014 every new component invents its own values (0.55rem\u201320px radii; 6 distinct radii \u2026",
    "recommendation": "Add --fs-2xl:24px to both stylesheets; bind .view-title, .empty-title/.empty-msg and .acct-name to fs tokens; collapse component radii onto --r-sm/md/lg/full (1rem\u2192--r-lg, .75rem\u2192--r-md, the .55\u2013.7rem cluster\u2192one --r-sm); delete --fs-xl or actually use it (e.g. card titles at 16px+ would strengthen hierarchy).",
    "severity": "medium",
    "source": "E:/code/Q Proxy/src/ui/panel/app.css",
    "title": "Type/radius scales are defined but abandoned: --fs-xl, --r-sm, --r-lg unused while a dozen hardcoded rem/px values bypass tokens; card titles sit only 2px above body text"
   },
   {
    "effort": "L",
    "evidence": "app.css:40: \".topbar{...gap:var(--s-4,16px);...}\" \u2014 --s-4 appears nowhere else. Grep shows zero --space-*/--s-* definitions, while padding/gap values in app.css include 4,6,8,10,12,14,16,24px (e.g. .card 16\u219224, .field margin 16, .row padding 8, subtab padding-inline 14) and rem values (3rem/2rem in .empty-card at app.css:224, .375rem/.625rem in .stat-chip at app.css:78).",
    "location": "src/ui/panel/app.css:40 (only --s-* reference); magic numbers throughout app.css:63,78,90,197,224",
    "problem": "The spacing rhythm is actually decent (mostly 4/8-based), but it's accidental: nothing constrains the next contributor from a 14px here and a 3rem there, and the rem-based paddings (.empty-card 3rem 2rem) don't scale with user font-size settings while px ones do \u2014 an inconsistent zoom story. The half-remembered --s-4 is a fossil of an abandoned scale.",
    "recommendation": "Add --s-1\u2026--s-6 (4/8/12/16/24/32) to :root and sweep the common paddings/gaps onto them; keep px (not rem) for chrome spacing or go all-rem \u2014 but pick one.",
    "severity": "medium",
    "source": "E:/code/Q Proxy/src/ui/panel/app.css",
    "title": "No spacing scale exists at all \u2014 one orphan var(--s-4,16px) reference proves it was planned and dropped"
   },
   {
    "effort": "S",
    "evidence": "app.css:66: \".home-grid>.card:hover{border-color:rgba(var(--accent-rgb),.35);box-shadow:var(--shadow-hover);transform:translateY(-3px)}\" \u2014 applies to every card including the kill-switch, IP/egress and worker-status cards, none of which are clickable (only inner buttons/links are). Ambient layer: app.css:31 dotgrid, app.css:32 noise, app.css:33-35 .blob-a/.blob-b with 44s/56s drift animations (.bl\u2026",
    "location": "src/ui/panel/app.css:66 (.home-grid>.card:hover), 31-35 (dotgrid/noise/blobs), 41-43 (.logo-tile spinring), 62 (.view-title gradient), 116-118 (.btn--primary::after shine)",
    "problem": "Six simultaneous ambient/ornament systems (dotgrid, noise, two blobs, spinring, gradient title) plus a sheen animation and a lift-on-hover for non-interactive surfaces is consumer-app signal, not admin-console signal \u2014 it erodes trust, adds repaint cost on every pointer move over cards, and the fixed blue logo/blob actively fights non-cyan accents.",
    "recommendation": "Delete the .home-grid>.card:hover lift (keep it only for genuinely clickable .acct-card), delete the spinring and the button shine, tie .blob-b and .logo-tile to --accent-rgb or flatten the logo tile, and keep dotgrid+subtle blobs as the only ambient layer.",
    "severity": "medium",
    "source": "E:/code/Q Proxy/src/ui/panel/app.css",
    "title": "False hover affordance and ornament overload: static cards lift and glow on hover; five decorative effects stack (conic spinring, button shine, drifting blobs, noise, gradient title)"
   },
   {
    "effort": "S",
    "evidence": "Sprite: 18 symbols at shell.html:23-38; grep for use href shows zero references to #i-play (shell.html:31) and #i-stop (shell.html:32) across all panel JS/HTML \u2014 leftovers of the removed checker view (dict.js still ships checker.* strings, nav.checker key). Inconsistent strokes: shell.html:26 \"#i-check ... stroke-width=\"2.5\"\" and shell.html:29 \"#i-info ... stroke-width=\"2.5\"\" vs stroke-width=\"2\" e\u2026",
    "location": "src/ui/panel/shell.html:31-32 (i-play, i-stop), 26/29 (stroke-width 2.5); src/ui/panel/app.css:100 (9px help icon), 106 (data-URI chevron)",
    "problem": "Dead payload in every page load of the sprite; two icons render visibly bolder than their siblings at 16px; the select chevron can't follow theme/accent; the 9px help glyph is below legibility for a control that appears on most settings fields.",
    "recommendation": "Delete i-play/i-stop (and the orphaned checker.* dict keys); normalize stroke-width to 2; add the chevron as a sprite symbol or at minimum a currentColor data-URI; set .help-trigger svg to 10-11px with a 24px+ hit area.",
    "severity": "low",
    "source": "E:/code/Q Proxy/src/ui/panel/shell.html",
    "title": "Icon sprite drift: two dead symbols, mixed stroke widths, a duplicated data-URI chevron, and a 9px help glyph"
   },
   {
    "effort": "S",
    "evidence": "app.css:53: \".swatch{width:18px;height:18px;...}\" with no pointer:coarse enlargement (the coarse block at app.css:299 bumps .btn/.chip/.input but not .swatch). app.css:99: \".help-trigger{...width:14px;height:14px;...}\" \u2014 enlarged to 44px only under @media(pointer:coarse) (app.css:302).",
    "location": "src/ui/panel/app.css:53 (.swatch), 99 (.help-trigger)",
    "problem": "The four accent buttons and the ubiquitous '?' help triggers (present on most settings fields) are below even the 24px minimum target size for pointer users; mis-taps on adjacent swatches are likely, and help triggers sit directly next to labels and char counters.",
    "recommendation": "Wrap .swatch in padding or a ::after hit-expansion to reach 24px; give .help-trigger a padding-based 24px hit area on fine pointers too (visual glyph can stay 14px).",
    "severity": "low",
    "source": "E:/code/Q Proxy/src/ui/panel/app.css",
    "title": "18px swatch buttons and 14px help triggers fail WCAG 2.5.8 target size on fine pointers"
   },
   {
    "effort": "S",
    "evidence": "Defined but never referenced: \":root{...--ring:rgba(var(--accent-rgb),.65);...}\" and its light twin \"--ring:rgba(var(--accent-rgb),.45)\" (app.css:5), while the actual focus rules inline the value: app.css:25 \":focus-visible{outline:2px solid rgba(var(--accent-rgb),.7)...}\", app.css:143 switch, app.css:165 port-cell. Also dead: --accent-hover, --accent-down (0 var() references), --accent used once \u2026",
    "location": "src/ui/panel/app.css:1 (:root --ring), app.css:5 (light --ring); unused: --fs-xl, --r-sm, --r-lg, --shadow-sm, --ease-bounce, --accent-hover, --accent-down",
    "problem": "The ring color is maintained twice but consumed nowhere, so it can drift from the real focus ring with no test failing \u2014 the exact class of drift this token system exists to prevent. ~8 dead tokens add review noise in a 302-line file.",
    "recommendation": "Use var(--ring) in the three focus rules (and reconcile the .7/.65 mismatch), delete the remaining dead tokens, and add a drift-test like the repo's fields.spec.ts pattern if CSS tokens grow.",
    "severity": "low",
    "source": "E:/code/Q Proxy/src/ui/panel/app.css",
    "title": "--ring (and --accent-hover/--accent-down/--ease-bounce/--shadow-sm) are defined in both themes but never used \u2014 focus rings are hardcoded instead"
   },
   {
    "effort": "S",
    "evidence": ":root \"--bg:#05080f;--surface:rgba(255,255,255,.03);--border:rgba(255,255,255,.06)\" (app.css:1); app.css:63 \".card{...background:var(--surface);border:1px solid var(--border);...box-shadow:var(--shadow-pop)}\". The inset highlight is only 2% white (shadow-sm/pop). By comparison .copy-field inputs sit on --surface-2 (5%) inside the same card \u2014 nested elements are harder to distinguish from their par\u2026",
    "location": "src/ui/panel/app.css:1 (--surface 3%), app.css:63 (.card)",
    "problem": "On most monitors a 3% white overlay over #05080f is a ~2-3 RGB-step change \u2014 the panel reads as one flat dark canvas with hairlines rather than elevated cards, and nested surfaces (.copy-field, inputs) become the visual anchors instead of the cards. Tailscale/Cloudflare dark dashboards step ~2-4% per elevation with a slightly stronger border on the top layer.",
    "recommendation": "Raise --surface to ~.045 and --border to ~.08 in dark (keep light as-is), or add a faint top-to-bottom gradient to .card; verify .copy-field still reads inside by bumping --surface-2 to .07.",
    "severity": "low",
    "source": "E:/code/Q Proxy/src/ui/panel/app.css",
    "title": "Dark elevation is nearly flat: 3% white cards on #05080f with 6% borders make card vs page and nested vs parent hard to distinguish"
   }
  ],
  "removals": [
   "Dead sprite symbols i-play and i-stop (shell.html:31-32) \u2014 zero use href references across panel JS/HTML; likely remains of the removed IP-checker view.",
   "Dead CSS tokens --ease-bounce, --accent-hover, --accent-down, --shadow-sm (app.css:1) \u2014 0 var() references; either delete or wire up, but do not keep maintaining them in both themes.",
   "--ring as defined-but-unused (app.css:1 and :5) \u2014 replace the three inline focus rgba() values with var(--ring) or delete the token.",
   "The .home-grid>.card:hover translateY(-3px) lift (app.css:66) \u2014 static status/config cards are not clickable; the affordance is false.",
   "The .btn--primary ::after shine-sweep gradient (app.css:116-118) and the .logo-tile conic spinring (app.css:42-43) \u2014 pure ornament with animation cost; prefers-reduced-motion already has to neuter them.",
   "Undefined token references var(--s-4,16px) (app.css:40) and var(--line) (app.css:246) \u2014 replace with real tokens (--space scale once defined, --border) rather than leaving fossils.",
   "Orphaned checker.* i18n strings in dict.js if the checker card is confirmed gone from SECTIONS (icons i-play/i-stop already point that way)."
  ],
  "summary": "The dark theme is a genuinely competent, AA-clean foundation \u2014 better token discipline than any rival proxy panel \u2014 but the light theme is a 16-rule bolt-on that fails WCAG on every semantic color: success \u22481.9:1, warning \u22481.7:1, danger \u22482.8:1, hints/table headers \u22482.5:1, and empty-state titles are literally invisible at \u22481.1:1. The design-token system is half-finished: no spacing scale at all, eight dead tokens, one undefined token (--line) that turns the WARP token panel's border into full-strength currentColor, and a cascade bug that silently kills 3 of 4 accent swatches in light mode. Versus Cloudflare dash and Tailscale it wins on polish and loses on restraint \u2014 conic spinrings, button shine sweeps, drifting blobs, gradient titles, hover-lift on non-clickable cards, and four theme swatches whose green/amber hexes exactly collide with the success/warning status colors."
 },
 {
  "consolidations": [
   "Merge the two touch-target media queries \u2014 app.css:300 (@media(pointer:coarse) for .help-trigger) and app.css:302 (@media(pointer:coarse),(max-width:767px) for .btn/.chip/.input) \u2014 into one block and add the missing selectors (.swatch, .subtab, .tab, .back-btn, .toast__close) there, instead of scattering per-component patches.",
   "login.html:107 duplicates app.css:302's coarse-pointer rules (.btn 44px / .icon-btn 44 / .input 44+16px) as a hand-copied subset; both files are inlined into src/ui/assets.ts at build time, so a shared coarse-touch snippet injected by the assembler would keep the two surfaces from drifting.",
   "app.css:302 and app.css:303 both target max-width:767px with interleaved pointer:coarse logic; fold all <=767px rules into one ordered block (layout, then touch, then tables) so future breakpoints don't have to reason about two cascade positions.",
   "The QR modal (shell.html:72) and the token-rotation modal (shell.html:67, m-rot with copy+QR) solve the same 'get this URL onto a client' job with different affordances; unify them into one share sheet (URL text + copy + QR + download) used by both call sites."
  ],
  "dimension": "Mobile & Responsive",
  "findings": [
   {
    "effort": "M",
    "evidence": "shell.html:46-56 renders `.topbar__end` as `.swatches` (4x18px dot buttons) + `#langseg` (two seg buttons labeled 'English'/'\u0641\u0627\u0631\u0633\u06cc', dict.js:3/20) + three 36px icon buttons (theme/shortcuts/logout). The only mobile rule is `@media(max-width:767px){.topbar{flex-wrap:wrap;padding-block:4px 0}.tabs{order:3;width:100%}}` (app.css:303) \u2014 it moves the 2 nav tabs to their own row but never condenses the \u2026",
    "location": "src/ui/panel/app.css:40,51,53,303 + src/ui/panel/shell.html:31-49",
    "problem": "On the single most common phone width the sticky chrome consumes ~20% of viewport height and at 320px risks sideways scroll; four decorative accent swatches permanently occupy prime row space on a device where they will never be used, and the language seg competes with a logout button for the same 44px-wide strip. Rival panels (BPB) collapse everything non-essential into a single menu on narrow sc\u2026",
    "recommendation": "Under (max-width:767px) (or 500px): display:none the .swatches (accent choice belongs in Settings, it is pure decoration), and either shrink the lang seg to a single toggle or move theme/shortcuts/logout into a '...' overflow button, keeping row 1 = brand + menu + one icon.",
    "severity": "high",
    "title": "Topbar utility cluster (swatches + lang + 3 icon buttons) never reflows for phones; 375px overflows and 320px breaks"
   },
   {
    "effort": "S",
    "evidence": "app.css:53 `.swatch{width:18px;height:18px...}` \u2014 four accent buttons in the topbar (shell.html:48-51) stay 18x18 on touch; app.css:197 `.subtab{...height:32px...}` \u2014 the subtab rail is the ONLY navigation to 10 settings sections on a phone, yet is 32px tall and not listed in the 44px sweep; app.css:48 `.tab{...height:34px...}` likewise omitted; app.css:291 `.toast__close{...width:28px;height:28px\u2026",
    "location": "src/ui/panel/app.css:53,197,48,291,260,302",
    "problem": "The elements that ARE bumped to 44px (inputs, cards' buttons) are the easy ones; the ones missed are the permanent chrome a user touches most: section switcher, accent dots, toast dismissal. Mis-taps on an 18px swatch silently repaint the whole panel's accent color.",
    "recommendation": "Extend the app.css:302 block: .swatch to 32-44px hit area (padding or ::before hit expansion), .subtab/.tab to min-height 44px on coarse/narrow, .toast__close and .back-btn to 44px, .seg button to 44px, and give the table checkboxes `width/height:1.25rem` minimum with `accent-color`.",
    "severity": "high",
    "title": "44px touch-target sweep misses the panel's actual chrome: 18px swatches, 32px subtabs, 34px tabs, 28px toast close, 34px back button"
   },
   {
    "effort": "S",
    "evidence": "Desktop is `grid-template-columns:auto 1fr 1fr` (app.css:297); mobile becomes `@media(max-width:767px){.stat-grid{grid-template-columns:auto 1fr}.stat-grid .h-cell{display:none}}` (app.css:303). But the markup was authored in triplets including unlabeled spacer cells: home.js:120 emits `...<span class=\"lbl\">Version</span><span class=\"mono\">v\u2026</span><span></span><span class=\"lbl\">Colo</span><span c\u2026",
    "location": "src/ui/panel/home.js:120,174 + src/ui/panel/app.css:297,303",
    "problem": "The Worker status card \u2014 the only place version/colo/usage are visible \u2014 renders misaligned and partly unlabeled on phones. The h-cell technique was applied to one cell type but not to the spacer cells the same markup convention requires.",
    "recommendation": "Either mark spacers with a class hidden alongside .h-cell in the mobile rule, or restructure the status card to a definition list / 2-col-safe markup instead of positional grid triplets.",
    "severity": "medium",
    "title": "Worker status stat-grid mispairs on mobile: unlabeled spacer cells shift every label one cell right"
   },
   {
    "effort": "M",
    "evidence": "`<div class=\"modal\" id=\"m-qr\"...><div class=\"qr-wrap\"><canvas id=\"qr-canvas\" width=\"560\" height=\"560\" style=\"width:280px;height:280px;display:block;max-width:64vw;max-height:64vw\"></canvas></div>...<button ... id=\"qr-download\" data-action=\"download-qr\"></button><button ... id=\"qr-close\" data-action=\"close-modal\"></button>` \u2014 actions are download + close only; no copy button, no URL text (contrast \u2026",
    "location": "src/ui/panel/shell.html:72 + src/ui/panel/qr.js (render)",
    "problem": "The QR modal is the panel's most mobile-critical surface and the weakest one: no copy fallback, no share (navigator.share), no enlarge, and marginal module density exactly where the owner says 'getting subscription links' already hurts.",
    "recommendation": "Add a copy button + truncated URL text to #m-qr, add navigator.share when available, and render the canvas at min(displayed size, container)*3 device pixels instead of a fixed 280*dpr; consider a tap-to-fullscreen mode (100dvh overlay) for scanning from a second device.",
    "severity": "medium",
    "title": "QR modal is a dead end on a phone: no copy, no URL display, no enlarge, canvas clamped to 64vw"
   },
   {
    "effort": "S",
    "evidence": "shell.html:5 `<meta name=\"viewport\" content=\"width=device-width,initial-scale=1\">`. Meanwhile app.css:267 `.applybar{...bottom:calc(16px + env(safe-area-inset-bottom))...}`, app.css:273 `.modal__panel{...padding-bottom:max(24px,env(safe-area-inset-bottom))}`, app.css:280 `.toasts{position:fixed;top:calc(16px + env(safe-area-inset-top))...}`. Without viewport-fit=cover, Safari reports 0 for all env\u2026",
    "location": "src/ui/panel/shell.html:5 + src/ui/panel/app.css:267,273,280",
    "problem": "Three CSS rules were written to respect notches and never fire; on an iPhone the confirm/apply bar and modal action buttons butt directly against the gesture area.",
    "recommendation": "Add `viewport-fit=cover` to the viewport meta in shell.html (and login.html for parity), then confirm the fixed applybar/toasts still clear the browser chrome in normal (non-cover) rendering paths where env()=0.",
    "severity": "medium",
    "title": "env(safe-area-inset-*) handling is dead code: viewport meta lacks viewport-fit=cover"
   },
   {
    "effort": "S",
    "evidence": "Mobile table strategy is real and mostly good: `@media(max-width:767px){.tbl thead{display:none}.tbl td{display:block;...}.tbl td::before{content:attr(data-l) \": \";...}}` (app.css:303) and every users-table td carries data-l (users.js:11-17). But the select-all control is `<th><input type=\"checkbox\" id=\"users-select-all\" ...>` (users.js:8) \u2014 inside the thead that mobile CSS hides, so bulk mode on \u2026",
    "location": "src/ui/panel/users.js:8,11-17 + src/ui/panel/app.css:303",
    "problem": "Bulk operations (enable/disable/extend/delete) are half-usable on phones, and the tallest table in the panel gets taller per row; the URL cell relies on browser break-after-slash behavior only.",
    "recommendation": "Move select-all out of thead (e.g., into the bulkbar as a 'Select all' button rendered when rows exist), drop the data-l prefix for the checkbox cell (empty-label exception in the td::before pattern), and add `overflow-wrap:anywhere` + a max-height/line-clamp to the token cell.",
    "severity": "medium",
    "title": "Users table cardification works, but select-all lives in the hidden thead and every row stacks 6 labeled blocks"
   },
   {
    "effort": "S",
    "evidence": "`.subtabs{display:flex;gap:6px;overflow-x:auto;...}` / `.subtab{...height:32px...}` (app.css:192-197) \u2014 no position:sticky anywhere on this rail, while the topbar it sits under IS sticky (app.css:40). With 10 sections (settings.js:30 SECTIONS array) and long card lists per section, a phone user deep in 'Advanced' must scroll the entire section back to the top to reach another rail item.",
    "location": "src/ui/panel/app.css:192-197",
    "problem": "On mobile the subtab rail is the app's primary navigation but behaves like static page content; navigation cost grows linearly with section length.",
    "recommendation": "Make .subtabs sticky under the topbar on <=767px (`position:sticky;top:<topbar-height>;z-index:30;background:var(--bg)`) or inject the rail into the topbar as a dropdown on small screens.",
    "severity": "low",
    "title": "Settings subtab rail (10 sections) is the de-facto mobile nav but never sticks"
   },
   {
    "effort": "S",
    "evidence": "`.tabs{-webkit-mask-image:linear-gradient(to right,#000 calc(100% - 28px),transparent 100%);mask-image:...}` (app.css:194) and same for .subtabs (:195) are unconditional. .tabs is `width:fit-content` with exactly two tabs (shell.html:33-35), so nothing ever overflows it \u2014 yet the right 28px of the 'Settings' tab is permanently faded at every viewport width, in both LTR and RTL (:196).",
    "location": "src/ui/panel/app.css:194-196",
    "problem": "A scroll affordance is rendered where there is nothing to scroll, reading as a clipped/disabled tab rather than decoration; on the subtab rail (where it IS meaningful) it also never disappears when the rail is fully scrolled to the end.",
    "recommendation": "Apply the mask only when scrollWidth>clientWidth (one-line JS toggle, e.g. class on the container), or move the fade into a pseudo-element shown conditionally.",
    "severity": "low",
    "title": "Right-edge fade mask is always on, ghosting the last tab even when nothing overflows"
   },
   {
    "effort": "M",
    "evidence": "Complete inventory in app.css: :61 `@media(min-width:1024px)` (main padding, home-grid 3fr/2fr), :64/:73/:232 `@media(min-width:768px)` (card padding, grid2, warp-grid), :274 `@media(min-width:640px)` (modal centering), :300 `@media(max-width:500px)` (tighter padding, copy-field wrap, addr-card single column, bulkbar buttons flex), :300 pointer:coarse (help-trigger 44px), :302 combined coarse/narr\u2026",
    "location": "src/ui/panel/app.css:61,64,73,76,232,274,300-303",
    "problem": "The breakpoints that exist are sensible, but phones are used landscape too and hover styles on coarse pointers produce sticky-lift artifacts; tablets get a stretched phone layout on the home screen.",
    "recommendation": "Add one `@media(max-height:480px) and (orientation:landscape)` rule (collapse topbar utilities, allow modal to use more height), a `@media(hover:hover)` guard around hover transforms, and consider min-width:768px for the home-grid two-column split.",
    "severity": "low",
    "title": "Breakpoint inventory: solid foundation, but no landscape/short-viewport handling, no hover:none guards, tablets keep the phone home layout"
   },
   {
    "effort": "S",
    "evidence": "Base form controls are 13px (`--fs-md`, app.css :88-93 block) but the touch block sets `.input,.select{height:44px;font-size:16px}` (app.css:302) \u2014 16px is exactly the threshold that stops iOS Safari focus zoom, and every textarea in the codebase carries `class=\"input textarea...\"` (settings.js:229, warp.js:12,78; shell.html m-warp-import) so it inherits the 16px via .input; height collision resol\u2026",
    "location": "src/ui/panel/app.css:302,88-93,273-274 + src/ui/login.html:107",
    "problem": "None \u2014 these are the parts that already meet a professional bar; listed so the fixes above do not regress them.",
    "recommendation": "Keep. When extending app.css:302 (findings 2/9), preserve the 16px font rule and the textarea min-height interplay; do not 'simplify' the .input dual-class trick without adding an explicit .textarea font-size rule.",
    "severity": "low",
    "title": "What already works on mobile (credit where due): 16px input font kills iOS zoom, dvh bottom-sheets, data-l table labels, RTL variants"
   }
  ],
  "removals": [
   "On (max-width:767px), remove the .swatches group from the topbar entirely (shell.html:47-52 / app.css:53): it is decoration with zero task frequency on phones and is the single largest contributor to the topbar overflow; the accent setting remains reachable once via Settings.",
   "Remove the unconditional 28px mask-image from .tabs (app.css:194): with exactly two top-level tabs it can never scroll, so the rule only ever degrades the active tab's right edge; keep the mask solely on .subtabs and make it conditional on actual overflow.",
   "Remove the dead 'nav.checker' / 'checker.*' i18n strings from dict.js:3/:20 (no renderer references them anywhere in panel JS \u2014 only the orphaned checker-targets counter at settings.js:564 remains); unused copy inflates the 65KB dictionary that ships inline in every page load, which matters more on mobile data than desktop."
  ],
  "summary": "Q Proxy is rare for its genre in having any mobile layer at all \u2014 bottom-sheet modals, table cardification with data-l labels, 16px input fonts to kill iOS zoom, and RTL-aware scroll fades \u2014 and that layer is genuinely better than BPB's. But it is desktop-first with patches, not a designed small-screen layout: the topbar utility cluster (4 decorative swatches + lang seg + 3 icon buttons + brand) is never condensed, overflowing at 320px and stacking to a ~3-row sticky header at 375px; the 44px touch-target sweep (app.css:302) covers forms but skips the actual chrome (18px swatches, 32px subtabs, 34px tabs, 28px toast close); and the QR modal \u2014 the single most-used mobile action in a proxy panel \u2014 offers no copy, no URL, no enlarge, with the canvas clamped to 64vw. Two latent bugs compound it: env() safe-area insets are dead code because the viewport meta lacks viewport-fit=cover, and the mobile stat-grid mispairs labels with unlabeled spacer cells."
 },
 {
  "consolidations": [
   "Merge the digit-consistency fix (finding 5) with the date/time fix (finding 6): one small i18n utility module in the panel bundle (faNum(n), fmtTime(ts), fmtDay(iso)) used by home.js, warp.js, users.js, and actions.js toasts, instead of three separate patches.",
   "The four hardcoded-English shell strings (finding 4) and the .warp-acc marker (finding 8) are both one-line shell.html/app.css edits \u2014 do them in a single RTL-polish pass and re-verify with the FA locale in a browser screenshot.",
   "Fold the dead-key purge (finding 3) and the orphaned home.status.total fix (finding 2) into one dictionary-cleanup change: delete the dead blocks and simultaneously wire the total label into home.js:120, then regenerate dist and confirm the drift test/asset hash still passes.",
   "The microcopy terminology fixes (finding 9) and the ECH preview dir fix (finding 1) both touch dict.js FA strings around protocols.* \u2014 batch them so translators/reviewers see one coherent FA diff."
  ],
  "dimension": "Visual Design \u2014 Farsi/RTL",
  "findings": [
   {
    "effort": "S",
    "evidence": "const extra=f.preview==='ech'?'<p class=\"field__hint\" data-ech-preview dir=\"ltr\"></p>':'' \u2014 populated by updateEchPreview(): el.textContent=t('protocols.ech.preview_manual',{name:name}) / preview_auto / preview_off, whose FA strings are pure Persian ('\u0646\u0627\u0645 \u0645\u0624\u062b\u0631 ECH: {name} (\u062f\u0633\u062a\u06cc)', '\u062e\u0648\u062f\u06a9\u0627\u0631 \u0627\u0632 SNI \u0647\u0631 \u0646\u0648\u062f (\u0645\u062b\u0644\u0627\u064b {name})').",
    "location": "src/ui/panel/settings.js:192 (renderer), settings.js:363-371 (updateEchPreview)",
    "problem": "In an RTL document a dir=ltr paragraph starts typing at the left edge and resolves bidi from the LTR base: the FA sentence '\u0646\u0627\u0645 \u0645\u0624\u062b\u0631 ECH: example.com (\u062f\u0633\u062a\u06cc)' displays with the trailing '(\u062f\u0633\u062a\u06cc)' stranded at the line start/left, the colon mis-placed, and the hostname pulling the visual order apart. Every other dynamic hint in the panel uses dir=\"auto\" (home.js:99 country.hint does exactly this) \u2014 th\u2026",
    "recommendation": "Change dir=\"ltr\" to dir=\"auto\" on the data-ech-preview element, matching country.hint. Optionally wrap the interpolated hostname in <bdi> so a manual override value like a hostname can never flip the paragraph.",
    "severity": "high",
    "source": "src/ui/panel/settings.js:192",
    "title": "ECH live-preview paragraph hardcoded dir=\"ltr\" renders Persian hints backwards"
   },
   {
    "effort": "S",
    "evidence": "...'<span class=\"lbl\">'+esc(t('home.status.today'))+'</span><span class=\"mono\">'+esc(String(u.requestsToday!=null?u.requestsToday:'\u2014'))+'</span><span class=\"mono\">'+esc(String(u.requestsTotal!=null?u.requestsTotal:'\u2014'))+'</span>' \u2014 a stat-grid row of 2 labels and 3 value cells. Both dictionaries define home.status.total = '\u0645\u062c\u0645\u0648\u0639 \u062f\u0631\u062e\u0648\u0627\u0633\u062a\u200c\u0647\u0627', but no t('home.status.total') call exists in any panel f\u2026",
    "location": "src/ui/panel/home.js:120; dict.js key 'home.status.total' (dict.js:3 EN, dict.js:20 FA)",
    "problem": "The FA panel shows '\u062f\u0631\u062e\u0648\u0627\u0633\u062a\u200c\u0647\u0627\u06cc \u0627\u0645\u0631\u0648\u0632  \u06f1\u06f2\u06f3  \u06f4\u06f5\u06f6\u06f7\u06f8' where the second number has no label \u2014 an admin cannot know it is the all-time total without reading the EN source. In RTL layout the orphan value lands where a reader's eye reaches it as if it belonged to the today label. This is also a dead i18n key, which means the translation was written and then the rendering was never wired.",
    "recommendation": "Add the missing '<span class=\"lbl\">'+esc(t('home.status.total'))+'</span>' cell before the requestsTotal value, or drop the key from both dictionaries. The former is obviously correct.",
    "severity": "high",
    "source": "src/ui/panel/home.js:120",
    "title": "Worker-status card renders the requests-total value with NO label; its FA string exists but is orphaned"
   },
   {
    "effort": "S",
    "evidence": "String-search of all panel JS for 'checker.', 'ports.', 'proxyip.', 'warp.settings', 'routing.title', 'nav.checker' finds only dict.js itself. settings.js:564 still references the checker idea via ta.id.indexOf('checker-targets')===0 but no checker UI is ever rendered. The SECTIONS array (settings.js:30-125) has no 'ports'/'proxyip' keys, so t('tabs.settings.'+s.key) (home.js:79) can never reach t\u2026",
    "location": "src/ui/panel/dict.js:3 and :20 (keys checker.* x19, ports.* x20, proxyip.* x7, warp.settings, warp.view.*, routing.title, nav.checker, tabs.settings.ports, tabs.settings.proxyip)",
    "problem": "Roughly 40 keys per language are unreachable \u2014 dead weight in the single largest asset in the bundle (dict.js is 65KB and dominates the panel JS), and a standing trap: anyone editing the FA checker strings is polishing text no user will ever see. The stale ports.*/*proxyip* blocks also duplicate the live egress.*/*addresses.* terminology, so a future editor can patch the wrong copy (e.g. '\u0622\u062f\u0631\u0633\u200c\u0647\u0627\u06cc\u2026",
    "recommendation": "Delete the checker.*, ports.*, proxyip.*, warp.settings, warp.view.*, routing.title and nav.checker blocks from BOTH locales (keep tabs.settings.* for the 10 live SECTIONS keys). Then add a drift test that asserts every DICT key appears somewhere in the bundle, mirroring the existing test/settings/fields.spec.ts discipline.",
    "severity": "medium",
    "source": "src/ui/panel/dict.js",
    "title": "~88 dead dictionary keys per language, including an entire untranslated-but-written checker.* module"
   },
   {
    "effort": "S",
    "evidence": "shell.html:19 <a class=\"visually-hidden skip-link\" href=\"#main\">Skip to content</a>; :53 <div class=\"seg\" role=\"group\" aria-label=\"Language\" id=\"langseg\"></div> \u2014 buildShell() (home.js:20-58) only rebuilds langseg's innerHTML, never its aria-label; :67 <div class=\"subtabs\" role=\"tablist\" aria-label=\"Settings sections\" id=\"subtabs\"></div> \u2014 never re-set; :83 <noscript><p style=\"padding:24px;text-al\u2026",
    "location": "src/ui/panel/shell.html:19,53,67,83",
    "problem": "An FA screen-reader user hears 'Skip to content', 'Language', 'Settings sections' in English, and a JS-disabled FA visitor sees an English-only page. Compare: nav aria-label IS translated at runtime (home.js:56 setAttribute('aria-label',t('nav.views'))) and the theme toggle IS (dict.js refreshThemeIcon) \u2014 the coverage exists, these four were simply missed. Note the panel defaults to dir=rtl lang=f\u2026",
    "recommendation": "Give the skip-link a data-l key and translate it in buildShell (e.g. reuse a new 'a11y.skip' key); set the langseg aria-label to t('general.language.label') (already exists: '\u0632\u0628\u0627\u0646 \u067e\u0646\u0644'); add a 'a11y.sections' key for the subtabs tablist; translate or drop the noscript paragraph via inline FA text since it only shows without JS.",
    "severity": "medium",
    "source": "src/ui/panel/shell.html",
    "title": "Four hardcoded English strings survive the FA panel: skip link, Language group label, Settings tablist label, noscript"
   },
   {
    "effort": "M",
    "evidence": "FA copy deliberately uses Extended Arabic-Indic digits: 'security.rule':'\u062d\u062f\u0627\u0642\u0644 \u06f8 \u0646\u0648\u06cc\u0633\u0647 \u0648\u0627\u0631\u062f \u06a9\u0646\u06cc\u062f.', 'err.port':'\u067e\u0648\u0631\u062a \u0628\u0627\u06cc\u062f \u0628\u06cc\u0646 \u06f1 \u062a\u0627 \u06f6\u06f5\u06f5\u06f3\u06f5 \u0628\u0627\u0634\u062f', 'home.kill.desc_paused':'\u06a9\u0644\u0627\u06cc\u0646\u062a\u200c\u0647\u0627 \u067e\u0627\u0633\u062e \u06f5\u06f0\u06f3 \u0645\u06cc\u200c\u06af\u06cc\u0631\u0646\u062f', 'checker.tooMany':'\u062d\u062f\u0627\u06a9\u062b\u0631 \u06f1\u06f0\u06f0 \u0645\u0642\u0635\u062f.'. But rendered dynamic numbers are Latin: pr.latencyMs+'ms' in pool rows, String(u.requestsToday) in stat-grid, t('home.chips.protocols',{n:protoCount}) interpolates\u2026",
    "location": "dict.js FA block (e.g. '\u06f8 \u0646\u0648\u06cc\u0633\u0647', '\u06f5\u06f0\u06f3', '\u06f1\u06f0\u06f0 \u0645\u0642\u0635\u062f') vs src/ui/panel/home.js:120,167; warp.js:67; home.js:94 chips interpolation",
    "problem": "Persian technical UIs commonly accept Latin digits in mono/telemetry contexts, and the .stat-chip class even forces font-variant-numeric:tabular-nums + font-mono (app.css:78) where Latin is the pragmatic choice. But the current half-and-half means the same concept (a count) appears as \u06f1\u06f2 in a toast and 12 in a chip on the same view, which reads as inconsistency rather than intent. Prose hints usin\u2026",
    "recommendation": "Adopt and document a rule: prose/hints/errors use Persian digits (already true), all mono/telemetry values stay Latin (already true) \u2014 then normalize the stragglers: interpolate Persian digits into {n} chips and toasts via a small faNum() helper applied inside t() when LANG==='fa', or alternately convert the handful of Arabic-Indic digits in FA prose to Latin. Either way, make it one policy, not t\u2026",
    "severity": "low",
    "source": "src/ui/panel/dict.js",
    "title": "Digit policy split-brain: FA prose uses Persian digits, every JS-rendered number is Latin"
   },
   {
    "effort": "M",
    "evidence": "const at=S.poolAt?new Date(S.poolAt).toLocaleTimeString():''; then head='...'+esc(t('egress.pool.updated'))+' '+esc(at) \u2014 no locale argument, so the format comes from the browser OS. Elsewhere dates are raw ISO substrings: (a.created_at||'').slice(0,10) rendered as 'YYYY-MM-DD \u00b7 preset' in account cards.",
    "location": "src/ui/panel/warp.js:28 (renderPool), warp.js:59 (acct-date ISO slice), shell.html #mu-expiry and users.js bulk expiry (datetime-local inputs)",
    "problem": "An English-locale browser running the FA panel shows '4:05:30 PM' glued onto '\u0628\u0647\u200c\u0631\u0648\u0632\u0631\u0633\u0627\u0646\u06cc' \u2014 mixed-direction and mixed-script in one line. Raw ISO dates are directionally stable (first-strong LTR), which is fine, but they bypass the panel's own language setting entirely; and datetime-local pickers will show Gregorian with browser-locale strings. There is no Jalali option \u2014 acceptable scope-wise, b\u2026",
    "recommendation": "Replace with Intl.DateTimeFormat(LANG==='fa'?'fa-IR':'en-US',{timeStyle:'short'}) and reuse it for the pool timestamp; keep ISO for compact card dates but consider toLocaleDateString('fa-IR') behind the same helper. Document that Gregorian-only is deliberate (Jalali is a product decision, not an i18n bug).",
    "severity": "low",
    "source": "src/ui/panel/warp.js",
    "title": "Dates/times formatted with browser locale, not the panel language; ISO fragments left raw in RTL text"
   },
   {
    "effort": "M",
    "evidence": "--font-sans:ui-sans-serif,system-ui,-apple-system,\"Segoe UI\",Roboto,\"Vazirmatn\",\"Vazir\",\"IRANSansX\",\"Noto Sans Arabic\",\"Noto Sans\",Tahoma,sans-serif \u2014 Persian fonts are 6th\u20138th in the stack, and none is ever loaded via @font-face anywhere in the UI sources. body line-height:1.5 (app.css:23); dense sizes --fs-xs:11px, --fs-sm:12px are used for all field__hint text, which carries most of the FA expl\u2026",
    "location": "src/ui/panel/app.css:1 (--font-sans), app.css:23 (body line-height:1.5)",
    "problem": "On a machine without Vazirmatn/Vazir installed (the majority), FA text falls to Noto Sans Arabic \u2014 Arabic ye/kaf glyph shapes (\u064a/\u0643) instead of Persian (\u06cc/\u06a9) \u2014 or to Tahoma, the 2000s-era Windows fallback. line-height:1.5 at 11\u201312px is the bare minimum for Persian: dots of \u06cc/\u0686/\u067e and descenders of \u0698 nearly collide in multi-line hints inside .field__hint. The typographic ceiling of the whole FA panel\u2026",
    "recommendation": "Keep the zero-runtime-dependency policy by self-hosting Vazirmatn woff2 as build-time assets (esbuild already inlines .html; add a base64 @font-face for a single weight + bold, ~90KB) or document the CDN-free fallback as deliberate. If not bundling, raise --fs-xs/sm line-height for [lang=fa] (e.g. html[dir=rtl] body{line-height:1.65} and .field__hint{line-height:1.6}).",
    "severity": "low",
    "source": "src/ui/panel/app.css",
    "title": "No Persian webfont: FA typography depends on locally-installed Vazirmatn/Vazir, falls back to Tahoma-class rendering"
   },
   {
    "effort": "S",
    "evidence": ".warp-acc summary::before{content:'\u25b8';transition:rotate .15s var(--ease);...} .warp-acc[open]>summary::before{rotate:90deg} \u2014 no [dir=rtl] variant, unlike .back-btn which gets [dir=rtl] .back-btn svg{transform:scaleX(-1)} (app.css:263). Used for the WARP accounts <details> on home.",
    "location": "src/ui/panel/app.css:257 (.warp-acc summary::before content:'\u25b8')",
    "problem": "In an RTL disclosure, the closed affordance conventionally points left (toward where content will appear from); '\u25b8' pointing right reads as 'expand toward nothing'. The 90\u00b0 open rotation still works, so this is a polish miss in an otherwise meticulous mirror pass \u2014 but it is exactly the kind of detail that separates a shipped RTL skin from an RTL-native design.",
    "recommendation": "Add [dir=rtl] .warp-acc summary::before{content:'\u25c2'} (or transform:scaleX(-1) on the ::before) and keep the open rotation; verify the rotate direction still visually reads as 'now open' when mirrored.",
    "severity": "low",
    "source": "src/ui/panel/app.css",
    "title": "WARP accordion disclosure marker '\u25b8' is not mirrored in RTL"
   },
   {
    "effort": "S",
    "evidence": "'protocols.ech.label':'ECH (\u06a9\u0644\u0627\u06cc\u0646\u062a\u200c\u0647\u0644\u0648\u06cc \u0631\u0645\u0632\u0646\u06af\u0627\u0631\u06cc\u200c\u0634\u062f\u0647)' \u2014 literal 'Encrypted ClientHello'; 'protocols.ssDirect.label':'\u0634\u062f\u0648\u0633\u0627\u06a9\u0633 \u0645\u0633\u062a\u0642\u06cc\u0645 (\u0628\u062f\u0648\u0646 \u067e\u0644\u0627\u06af\u06cc\u0646)' while 'protocols.ss.title' stays 'Shadowsocks'; 'advanced.doh.short':'\u0645\u0628\u062f\u0644 DoH \u067e\u0634\u062a \u0646\u0642\u0637\u0647\u0654 \u062e\u0635\u0648\u0635\u06cc \u0632\u06cc\u0631' vs 'sources.tools.doh':'\u062d\u0644 DoH' and 'sources.tools.hint':'\u0622\u06cc\u200c\u067e\u06cc\u200c\u0647\u0627\u06cc \u0631\u0644\u0647 \u0631\u0627 \u0627\u0632 \u0627\u0633\u062a\u062e\u0631 DNS \u062f\u0627\u062e\u0644\u06cc \u067e\u06cc\u062f\u0627 \u0645\u06cc\u200c\u06a9\u0646\u062f' \u2014 three different renderings of resolver/fetc\u2026",
    "location": "dict.js FA block; keys protocols.ech.label, protocols.ssDirect.label, advanced.doh.short vs sources.tools.doh, ports.cleanAddrs.label vs addresses.card.label",
    "problem": "The FA translation is 90% genuinely idiomatic (\u06af\u0630\u0631\u0648\u0627\u0698\u0647, \u0628\u0631\u0648\u0646\u200c\u0628\u0631\u06cc/\u062f\u0631\u0648\u0646\u200c\u0631\u06cc\u0632\u06cc, \u06a9\u0645\u06cc\u0646\u0647/\u0628\u06cc\u0634\u06cc\u0646\u0647, \u0646\u0648\u06cc\u0633\u0647, the correctly flipped '\u062a\u0646\u0638\u06cc\u0645\u0627\u062a \u2190 \u067e\u0631\u0648\u062a\u06a9\u0644\u200c\u0647\u0627' arrow, the RLM-prefixed chain.uri.hint '\u200fsocks5://...' \u2014 this is the best FA I have audited in this product category), but the remaining 10% shows one translator's hand changing mid-project: one protocol spelled two ways, 'resolver' translated two ways, endpo\u2026",
    "recommendation": "Sweep: keep 'Shadowsocks' Latin everywhere (fix ssDirect to 'Shadowsocks \u0645\u0633\u062a\u0642\u06cc\u0645\u2026' or 'SS \u0645\u0633\u062a\u0642\u06cc\u0645'); use one resolver term ('\u062d\u0644\u200c\u06a9\u0646\u0646\u062f\u0647\u0654 DoH' or keep 'DoH resolver'); pick \u0622\u062f\u0631\u0633 OR \u0622\u06cc\u200c\u067e\u06cc for endpoints and stick to it across addresses/egress/checker-live-keys; shorten ECH label to 'ECH (\u0631\u0645\u0632\u0646\u06af\u0627\u0631\u06cc ClientHello)'; revert '\u0627\u0633\u067e\u0627\u06cc\u062f\u0631 \u0627\u06cc\u06a9\u0633' to 'SpiderX (spx)'. Since dict.js is embedded HTML, none of this risks w\u2026",
    "severity": "low",
    "source": "src/ui/panel/dict.js",
    "title": "FA microcopy drift: transliteration inconsistency and duplicated terminology across live vs legacy keys"
   },
   {
    "effort": "S",
    "evidence": ".tbl th{...text-transform:uppercase;letter-spacing:.08em;...} applied to headers like users.col.name='\u0646\u0627\u0645' and users.col.token='\u0646\u0634\u0627\u0646\u06cc \u0627\u0634\u062a\u0631\u0627\u06a9'. Mobile: .tbl td::before{content:attr(data-l) \": \";...} where data-l is set from esc(t('users.col.name')) etc. (users.js userRowHtml). .view-title{letter-spacing:-.02em} with FA titles like '\u06a9\u0627\u0631\u0628\u0631\u0627\u0646'.",
    "location": "src/ui/panel/app.css:223 (.tbl th text-transform:uppercase;letter-spacing:.08em), app.css:303 (mobile td::before attr(data-l)), app.css:62 (.view-title letter-spacing:-.02em)",
    "problem": "text-transform and letter-spacing are no-ops/harmless within a connected Persian word (browsers do not break joining), but .08em tracking between FA words slightly loosens header rhythm and the uppercase declaration is dead code for FA \u2014 a sign the table component was designed Latin-first and FA was tolerated rather than type-designed. The mobile attr(data-l) label mechanism is actually a good RTL\u2026",
    "recommendation": "Scope header tracking to LTR only ([dir=ltr] .tbl th{letter-spacing:.08em}) and drop the uppercase for RTL or leave as harmless; no change needed for td::before. Low priority; bundle with the typography pass.",
    "severity": "low",
    "source": "src/ui/panel/app.css",
    "title": "Mobile card-list labels and table headers inherit Latin-typography styling that dead-codes in FA"
   },
   {
    "effort": "S",
    "evidence": "Positive verifications: [dir=rtl] .select chevron moved left with padding-inline swap (app.css:113); switch thumbs translated -20px in RTL with explicit [dir=ltr] counterpart (app.css:141-142, addr-switch 185-186); toast animations slide from correct side (app.css:282 tInR, login.html:89 tinR); scroll masks flipped (app.css:196); skip-link and toasts use inset-inline-start/end (app.css:25, 280); c\u2026",
    "location": "src/ui/panel/app.css:113,141-142,185-186,196,263,282; src/ui/panel/shell.html:2; src/ui/panel/head.js:1; src/ui/panel/dict.js:37",
    "problem": "Nothing in the mirror layer needs emergency work; the residual issues are the six discrete findings above. The only structural gap is that dir handling is implemented per-node rather than through a component audit, which is why exactly one dynamic paragraph (ECH preview) and one glyph (\u25b8) escaped.",
    "recommendation": "No change. Use this as the baseline when regressing: any new dynamic string container must default to dir=\"auto\" unless it is a URL/credential/code field, which must be dir=\"ltr\" + unicode-bidi:isolate like .copy-field code.",
    "severity": "low",
    "source": "src/ui/panel/app.css",
    "title": "RTL mirroring coverage is genuinely excellent \u2014 recording what is right so it is not regressed"
   }
  ],
  "removals": [
   "dict.js (both locales): the entire checker.* key family (checker.title, checker.desc, checker.targets.label, checker.targets.hint, checker.run, checker.stop, checker.running, checker.results, checker.empty, checker.unsupported_port, checker.col.target, checker.col.latency, checker.col.rate, checker.col.status, checker.status.ok, checker.status.partial, checker.status.failed, checker.summary, checker.tooMany) \u2014 no checker UI exists in any panel source; also remove the vestigial ta.id.indexOf('checker-targets') branch in settings.js:564.",
   "dict.js (both locales): the legacy ports.* block (ports.domains.*, ports.hostnameOverride.*, ports.matrix.*, ports.policy.*, ports.cleanAddrs.*, ports.remoteSubs.*, ports.cdn.*), the legacy proxyip.* block (proxyip.mode.*, proxyip.list.*, proxyip.nat64.*), tabs.settings.ports, tabs.settings.proxyip, nav.checker, warp.settings, warp.presets.inuse, warp.view.accounts, warp.view.settings, routing.title, home.status.total (or wire it per finding 2 \u2014 do not keep both).",
   "shell.html: the hardcoded 'Skip to content' text and noscript 'JavaScript is required.' \u2014 replace with translated equivalents (findings 4) rather than keeping English fallbacks in a panel whose document default is lang=fa dir=rtl."
  ],
  "summary": "This is the strongest bilingual/RTL implementation in its product category \u2014 the 523-key FA dictionary is 100% complete with zero placeholder mismatches, mirroring is done with logical properties plus explicit [dir=rtl] overrides for selects, switches, masks, toasts and the back icon, and every URL/token/secret field is deliberately forced LTR \u2014 so the foundations shame BPB/cmliu outright. The defects are exactly what a per-node (rather than systemic) RTL approach leaves behind: one dynamic hint hardcoded dir=\"ltr\" renders Persian backwards, the requests-total stat renders with no label at all, ~88 dead keys per language (an entire unwired checker.* feature, stale ports.*/proxyip.* blocks) bloat a 65KB dict, and four hardcoded English strings survive the FA panel. Typography is the soft underbelly: no Persian webfont is ever loaded, so Vazirmatn in the font stack is aspirational, 11\u201312px hint text at line-height 1.5 is cramped for Persian, and digit/date formatting policy is split-brain (Persian digits in prose, Latin everywhere JS renders, toLocaleTimeString obeying the browser instead of the panel)."
 },
 {
  "consolidations": [
   "The 17-format row renderer is written twice (renderWarpDetail loop at warp.js:89 and warpSubsHtml loop at warp.js:92) \u2014 merge into one helper (format list + grouping + placement) so a new server format (registry.ts) cannot silently miss the UI; pair with a drift test like test/settings/fields.spec.ts does for settings fields.",
   "Amnezia parameter arrays are duplicated (AMZ at warp.js:66 vs AMZK at warp.js:81) with only the I1 id prefix differing \u2014 extract one module-level constant.",
   "Modal error handling is three-quarters built (wi-error on import, wp-error on preset) but missing on generate (m-warp-generate) and on the Amnezia save paths \u2014 adopt one err.fields-to-inline-element pattern in lib.js api() handling and reuse it.",
   "The users table failure pattern (users.js:31-34 usersLoadFailed + Retry button) should be generalized and applied to the WARP section load failure and other data-driven sections instead of each view hand-rolling (or skipping) error states.",
   "The WARP empty state (warp.js:61) and the Home empty subscriptions card (home.js:101-103) both point away from WARP \u2014 a single 'first WARP account' CTA component should be shared so both surfaces advertise the same path."
  ],
  "dimension": "WARP UX (panel/warp.js + settings warp subtab + src/warp formats registry)",
  "findings": [
   {
    "effort": "S",
    "evidence": "shell.html:43-44 defines the complete nav: `<a class=\"tab\" ... id=\"tab-home\" href=\"#/home\">` and `<a class=\"tab\" ... id=\"tab-settings\" href=\"#/settings/general\">`. settings.js:125 puts WARP last of 10 sections: `{key:'warp',cards:[{warpAccounts:true,fields:[]},{warpPresets:true,fields:[]},{warpAmnezia:true,fields:[]}]}`. dict.js:3 still ships the orphaned key `'nav.warp':'WARP'` (0 references in a\u2026",
    "location": "src/ui/panel/shell.html:43-44; src/ui/panel/settings.js:125; src/ui/panel/home.js:98-103; src/ui/panel/dict.js:3",
    "problem": "WARP is a consumer surface (it serves /sub/wg/* configs and has its own detail routes and 18KB of view code) yet it is filed under configuration, behind Sources and Advanced. A fresh admin has zero WARP affordance anywhere: not in the topbar, not on Home, and the Home empty-state CTA points at Protocols. The dead `nav.warp` key proves a top-level tab was designed and dropped; the current IA is a d\u2026",
    "recommendation": "Restore the top-level WARP tab (the `nav.warp` i18n key already exists in both languages) \u2014 or, minimum viable fix, mirror the Users pattern: a Home card showing account chips/status with a Generate CTA even when accounts.length===0. If the tab is not adopted, delete `nav.warp` and stop pretending.",
    "severity": "high",
    "source": "src/ui/panel/shell.html:43",
    "title": "WARP has no top-level presence: buried as the 10th (last) Settings subtab, invisible on Home until an account already exists"
   },
   {
    "effort": "S",
    "evidence": "First use: login submit \u2192 land on #/home (parseRoute default) \u2192 click Settings tab \u2192 click WARP subtab (10th of 10, settings.js:30-125) \u2192 click `warp-generate-open` \u2192 click `wg-go` in m-warp-generate \u2192 lands on `#/settings/warp/<id>` (actions.js:461 `location.hash='#/settings/warp/'+d.account.id`) \u2192 scroll past token panel + account card + custom-endpoints card \u2192 click copy on one of 17 rows. Retu\u2026",
    "location": "src/ui/panel/warp.js:74-89; src/ui/panel/actions.js:459-464; src/ui/panel/home.js:76",
    "problem": "The click budget itself is not catastrophic; the problem is that all 5 clicks are navigational guesswork (nothing on Home hints WARP exists), and even after landing on the account detail, the 17 URLs \u2014 the whole point of the feature \u2014 are the 4th card down, behind the raw subscription token (warp.js:74) which is the least-needed, most-sensitive item on the page.",
    "recommendation": "After fixing reach (see previous finding), reorder the detail page: Subscription URLs first, account settings second, token collapsed behind a reveal/summary. Keep the auto-navigate after generate (actions.js:461) \u2014 that behavior is correct.",
    "severity": "high",
    "source": "src/ui/panel/warp.js:74",
    "title": "Click cost confirmed: 5 interactions from login to a usable WARP config on first use; the deliverable is then the 4th card on the detail page"
   },
   {
    "effort": "S",
    "evidence": "warp.js:6 `function loadWarpIfNeeded(){return S.warp?Promise.resolve(S.warp):loadWarp().catch(()=>null)}`; warp.js:5 loadWarp's catch only toasts then rethrows; showSection (home.js:76) always calls `renderWarpDetail(warpId)` or `renderWarpSection()`, and warp.js:57 bails with `const W=S.warp;if(!W)return;` AFTER the three card shells are injected (warp.js:56). Result on API failure: 'Accounts' / \u2026",
    "location": "src/ui/panel/warp.js:5-6, 56-57; src/ui/panel/home.js:76; src/ui/panel/users.js:31-34",
    "problem": "A transient 401/500 on any of the three WARP API calls leaves the section permanently blank until the admin navigates away and back; there is no retry affordance, no skeleton, no inline error. This is the worst failure surface in the panel \u2014 even the lowly proxy-pool card shows `egress.pool.failed` text.",
    "recommendation": "Render an error card with the users-style Retry button when loadWarpIfNeeded resolves null; while loading, show the common.loading placeholder instead of empty card shells.",
    "severity": "high",
    "source": "src/ui/panel/warp.js:57",
    "title": "Failed WARP load renders three silent blank cards with no error text and no retry"
   },
   {
    "effort": "M",
    "evidence": "warp.js:2 is one 17-tuple array (`[['wireguard-conf','WireGuard .conf (ZIP)','grad-1'],...,'egern','Egern','grad-6']]`) looped identically at warp.js:89 (detail: `WARP_F.forEach(... '<span class=\"fmt-icon '+grad+'\"><svg><use href=\"#i-download\"/></svg> ... copyFieldHtml(warpSubUrl(a.token,f) ...)`) and warp.js:92 (home, inside `<details class=\"warp-acc\">`). Every row uses the same `#i-download` ico\u2026",
    "location": "src/ui/panel/warp.js:2, 89, 92; test/warp/formats.spec.ts",
    "problem": "A user with Hiddify must eyeball 17 rows to guess that 'sing-box' is theirs, distinguish 'sing-box legacy' (why legacy? for which versions?), and figure out that Amnezia variants only matter if they use AmneziaWG. Content types (ZIP vs URI vs YAML vs JSON) are invisible. The two .conf rows are downloadable ZIPs while every other row is a fetchable URL, yet all are presented identically with copy+Q\u2026",
    "recommendation": "Group into client families (WireGuard native (ZIP), Throne, V2RayN/Xray, sing-box (\u00d74), Clash/mihomo, other apps: Surge/Surfboard/Loon/Egern), add one-line 'for app X' hints per group, a content-type tag per row, and an 'Amnezia variants' toggle that hides the 7 Amnezia rows by default. Extract the format list into one shared renderer with a drift check against a formats list served by the API.",
    "severity": "high",
    "source": "src/ui/panel/warp.js:2",
    "title": "The 17 output formats are an ungrouped, undifferentiated dump \u2014 rendered twice with a bare download icon on every row"
   },
   {
    "effort": "S",
    "evidence": "dict.js defines `'warp.presets.inuse':'In use'` but grep finds 0 `t('warp.presets.inuse')` references \u2014 the badge was designed and never wired. Preset rows (warp.js:63-68) render name, endpoint preview, `warp.presets.count`, edit and delete \u2014 nothing about usage. The dependency only surfaces at failure time: handlers/api/warp.ts:297 `if (inUse) throw new ValidationError({ preset: \"preset is in use\u2026",
    "location": "src/ui/panel/dict.js (warp.presets.inuse key, en+fa); src/ui/panel/warp.js:63-68; src/handlers/api/warp.ts:294-303",
    "problem": "The account\u2192preset relationship is invisible in the presets list, so admins can't see blast radius before editing/deleting. The error text violates the panel's own bilingual copy standard (server messages are the known English-only gap), and a translated string was written for exactly this case and then left unused.",
    "recommendation": "Compute in-use counts from S.warp.accounts and render the existing `warp.presets.inuse` chip (with a count) on each preset row; disable delete with a tooltip when count>0. Localize the error as a fallback.",
    "severity": "medium",
    "source": "src/handlers/api/warp.ts:297",
    "title": "Preset 'In use' state was designed, then never wired: deleting an in-use preset fails only at click time with a raw English server toast"
   },
   {
    "effort": "S",
    "evidence": "actions.js:117-120: `'warp-preset-del'(el){ (async()=>{ try{await api('api/warp/presets/'+el.dataset.id,{method:'DELETE',mutate:true}); toast(t('warp.toast.presetDeleted'),'ok'); ...` \u2014 no confirmDialog call, while account deletion (actions.js:91-95) and token regen (actions.js:86-90) both wrap in `confirmDialog(...)`. Server-side in-use protection exists (warp.ts:297), but a spare preset vanishes\u2026",
    "location": "src/ui/panel/actions.js:117-120; cf. actions.js:86-95",
    "problem": "Inconsistent destructive-action policy on the same screen; presets are cheap to recreate but the interaction model trains the admin that one row's x button can destroy data instantly.",
    "recommendation": "Wrap in confirmDialog like every other destructive action, or drop the inline delete into the edit modal.",
    "severity": "medium",
    "source": "src/ui/panel/actions.js:117",
    "title": "Preset deletion has no confirmation dialog, unlike every other destructive WARP action"
   },
   {
    "effort": "S",
    "evidence": "warp.js:75 builds the select from presets only: `'<option value=\"'+esc(p.id)+'\"'+(a.endpoint_list.type==='preset'&&a.endpoint_list.preset_id===p.id?' selected':'')+'>'` \u2014 for `type==='custom'` no option is selected, so browsers display the first preset as chosen while the 'Custom endpoints' card directly below shows the real state. actions.js:396 wires an instant PUT on any change: `if(e.target.id\u2026",
    "location": "src/ui/panel/warp.js:75; src/ui/panel/actions.js:396; src/handlers/api/warp.ts (resolveEndpointList/buildAccount)",
    "problem": "The control lies about current state and any accidental interaction (a scroll-wheel over a focused select, a stray click) silently destroys the custom endpoint list that the admin typed in. This is the only silent-data-loss path in the WARP UI.",
    "recommendation": "When endpoint_list.type==='custom', inject a selected `<option value=\"\">Custom (n endpoints)</option>` placeholder and require an explicit confirm (the existing confirmDialog) before switching preset\u2192 and vice versa; keep the instant-save only for preset\u2192preset changes.",
    "severity": "high",
    "source": "src/ui/panel/warp.js:75",
    "title": "Endpoint preset select lies about state on custom-endpoint accounts and can silently destroy custom endpoints"
   },
   {
    "effort": "M",
    "evidence": "grep for license|plus|account type across src/ returns nothing in the WARP stack (only unrelated users/telegram quota code). src/warp/api.ts registerWarpDevice POSTs `{key, install_id:'', fcm_token:'', tos, model:'PC', type:'Windows', locale:'en_US'}` and extractConfig reads only id/token/config \u2014 no license binding (the client API supports attaching a license key to a device registration to get W\u2026",
    "location": "src/warp/api.ts (postReg/registerWarpDevice); src/warp/store.ts; src/ui/panel/warp.js",
    "problem": "Admins migrating from BPB-style panels expect to bind a WARP+ license or at least see that accounts are free-tier; here the limitation is invisible. There is also no way to see how many devices exist against any practical ceiling, and the UI copy (`warp.gen.desc':'Creates a real Cloudflare WARP device via the official API. Name is optional.'`) never mentions rate limits, though 429 handling exists\u2026",
    "recommendation": "Decide scope: (a) minimum \u2014 document free-tier-only in warp.gen.desc and show a small device-count chip; (b) parity \u2014 add optional license-key binding on the account (POST registration then PATCH license) with a field in the generate/import modals. Note the panel's honest `warp.chips.direct` chip is good copy and should be kept regardless.",
    "severity": "medium",
    "source": "src/warp/api.ts:110",
    "title": "No WARP+/license handling of any kind \u2014 accounts are anonymous free devices with no tier, quota, or device info surfaced"
   },
   {
    "effort": "S",
    "evidence": "The generate modal in shell.html (m-warp-generate) contains only `wg-title`, `wg-desc`, `wg-name-label`, and action buttons \u2014 no `.field__error` element \u2014 so actions.js:460-464 falls back to `catch(err){toastErr(err)}`. Compare m-warp-import (`wi-error`, handled inline at actions.js:468-470 `if(err&&err.fields&&err.fields.config){$('wi-error').textContent=...}`) and m-warp-preset (`wp-error`). The\u2026",
    "location": "src/ui/panel/shell.html (m-warp-generate modal); src/ui/panel/actions.js:459-464, 466-471; src/ui/panel/lib.js (api)",
    "problem": "Generate is the one action that talks to an external API and the one most likely to fail (rate limit, CF API outage), yet it has the weakest error surface of the three WARP modals, in English only, with no recovery affordance.",
    "recommendation": "Add a wg-error field to the generate modal and mirror the import modal's inline handling; for RateLimitedError, render the retry delay (the data is already parsed).",
    "severity": "medium",
    "source": "src/ui/panel/actions.js:460",
    "title": "Error surfaces are inconsistent: the generate modal (the only WARP action that talks to Cloudflare and can be rate-limited) has no inline error field"
   },
   {
    "effort": "M",
    "evidence": "The labels are `[['Jc','Jc (0-128)'],['Jmin','Jmin (0-1280)'],...,['H4','H4']]` (warp.js:66) and repeated at warp.js:81 (AMZK) for the detail page \u2014 two hand-copied arrays that must stay in sync. `warp.amnezia.desc` ('Applied to Amnezia subscriptions unless an account overrides them. Zeros mean the parameter is omitted.') is good, but the per-account card only distinguishes `warp.detail.amnezia_on\u2026",
    "location": "src/ui/panel/warp.js:59-61, 66, 81-89; src/ui/panel/actions.js:126-131",
    "problem": "An admin who doesn't already know AmneziaWG semantics cannot safely edit these fields; the UI offers neither education nor safety (free-text inputs, toast-only errors, duplicated constants). Most users will never touch this card, yet it occupies permanent third position on the section \u2014 pure configuration noise sitting above/beside the consumer surface.",
    "recommendation": "Extract one AMZ constant, add per-field help triggers (the helpTrigger pattern already exists in settings.js), show effective values on the detail card (global vs override diff), validate ranges client-side, and consider collapsing the global card behind a disclosure.",
    "severity": "medium",
    "source": "src/ui/panel/warp.js:66",
    "title": "Amnezia UI is a 12-field wall of cryptic protocol parameters with duplicated constants and toast-only errors"
   },
   {
    "effort": "S",
    "evidence": "warp.js:92: `'<details class=\"warp-acc\"><summary><span class=\"acct-name\">'+esc(a.name)+'</span> \u00b7 '+WARP_F.length+'</summary>` \u2014 renders literally as 'Account \u00b7 17' with no word. Each expanded details renders 17 fmt-rows \u00d7 (URL code + copy btn + QR btn) \u2014 ~100+ DOM nodes per account appended into the Home subscriptions card, and the `warpSubsHtml` block has no Manage link (the Users Home card has \u2026",
    "location": "src/ui/panel/warp.js:90-93; src/ui/panel/home.js:93-98",
    "problem": "'\u00b7 17' is meaningless at a glance; users can't tell it means formats. The users-side Home card got a Manage affordance; the WARP side got a bare count and no path to the settings section, reinforcing the reach problem.",
    "recommendation": "Use the existing count pattern `t('home.chips.formats',{n:WARP_F.length})` or a dedicated key ('17 formats \u2014 Manage'), and add a small Manage/`#/settings/warp` link in the block header.",
    "severity": "low",
    "source": "src/ui/panel/warp.js:92",
    "title": "Home WARP accordion summary shows an unlabeled '\u00b7 17' and heavy per-account DOM"
   },
   {
    "effort": "S",
    "evidence": "Verified zero `t()` references for: `'nav.warp'`, `'nav.checker'`, `'warp.settings'`, `'warp.view.accounts'`, `'warp.view.settings'`, `'warp.presets.inuse'`, `'tabs.settings.ports'`, `'tabs.settings.proxyip'`, plus the whole `checker.*` (~15 keys), `proxyip.*` (~8), and `ports.*` namespaces \u2014 including `checker.title':'Proxy IP checker'` describing a view that does not exist in SECTIONS at all. ch\u2026",
    "location": "src/ui/panel/dict.js:3,20; src/ui/panel/chrome.js:79",
    "problem": "A removed WARP-top-level view and a removed IP Checker view left ~60 dead strings in a 65KB dictionary (which is bundled verbatim into dist and shipped to clients), and the settings.search shortcut is a no-op \u2014 evidence the IA was restructured without cleanup, exactly the drift this audit is for.",
    "recommendation": "Prune the dead namespaces (or wire `nav.warp` if the top-level tab is adopted) and delete or implement the `#settings-search` Ctrl+K path; add a drift test that fails on dict keys with no t() reference.",
    "severity": "low",
    "source": "src/ui/panel/dict.js:3",
    "title": "Dead i18n and nav archaeology: ~6 WARP/nav keys plus entire checker/proxyip/ports namespaces have zero references, and Ctrl+K targets a selector that does not exist"
   }
  ],
  "removals": [
   "dict.js dead keys with zero t() references: nav.warp (unless the top-level tab is adopted), nav.checker, warp.settings, warp.view.accounts, warp.view.settings, warp.presets.inuse (unless wired to the preset row), tabs.settings.ports, tabs.settings.proxyip, and the entire checker.*, proxyip.*, and ports.* namespaces (~60 strings shipped in both languages).",
   "chrome.js:79 `#settings-search` lookup \u2014 the input does not exist anywhere in shell.html; either implement the search field or reduce the Ctrl+K handler to the home fallback.",
   "The duplicated AMZK array in renderWarpDetail (warp.js:81) once AMZ is centralized \u2014 keep one source."
  ],
  "summary": "WARP is the panel's most self-contained feature (18KB view code, 17 server formats, its own /sub/wg namespace and detail routes) and it is treated as a configuration afterthought: it is the 10th and last Settings subtab, Home never advertises it until an account already exists, and dead i18n keys (`nav.warp`, `warp.view.*`) show a top-level design existed and was demoted. The 17 formats are a flat, undifferentiated dump rendered in two places with an identical download icon, no client-family grouping, no per-format guidance, and no drift protection between the hand-copied UI list and the server registry; meanwhile the endpoint-preset select actively lies about state on custom-endpoint accounts and can silently destroy typed-in endpoints with one stray click. Error surfaces are the weakest in the panel (failed loads render blank cards with no retry, the generate modal has no inline error, 429s vanish as toasts), and the answer to license/plus handling is that it simply does not exist anywhere in the code."
 },
 {
  "consolidations": [
   "Three label tables for the same formats should merge into one exported map (subscribe.ts:15 FORMAT_LABELS + status.ts:45 buildSubUrls + warp.js:1 WARP_F) \u2014 do this as part of the Subscriptions hub so the hub, info page and WARP list can all consume it.",
   "The three empty-state styles (bare hint row users.js:29, empty-card home.js:100, empty-card warp.js:61) should become one shared empty-state builder with icon/title/message/CTA \u2014 users.js is the one violating the house pattern the other two already established.",
   "Dead i18n keys 'nav.warp'/'nav.checker' (dict.js:3) and the dead '#settings-search' shortcut (chrome.js:79) should be consolidated into the hub/navigation decision: either implement the top-level tabs and command palette they imply, or delete the keys and the shortcut branch.",
   "Cache/update-interval truth should be consolidated: one helper that derives Profile-Update-Interval/Cache-Control from subUpdateIntervalHours (headers.ts + throttleHeaders) so the setting, the header, and the hub's 'updates' hint can never disagree again."
  ],
  "dimension": "Subscription delivery audit (Q Proxy admin panel UI)",
  "findings": [
   {
    "effort": "L",
    "evidence": "shell.html:43-44 `'<a class=\"tab\" role=\"tab\" id=\"tab-home\" href=\"#/home\" data-view=\"home\"></a><a class=\"tab\" role=\"tab\" id=\"tab-settings\" href=\"#/settings/general\" data-view=\"settings\"></a>'` \u2014 only Home and Settings exist. The subscription inventory is smeared across: (a) Home 'Subscriptions' card rows rendered from bootstrap (home.js:96-97 `'urls.forEach((entry,i)=>{sh+='<div class=\"row\">\u2026'+copy\u2026",
    "location": "src/ui/panel/shell.html:43-44; src/ui/panel/home.js:93-100; src/ui/panel/actions.js:546; src/ui/panel/warp.js:73-79; src/ui/panel/users.js:8-13",
    "problem": "The core deliverable of the product is a URL, yet an admin must remember four unrelated surfaces to reconstruct it. New-admin onboarding ends at wizard step 2 with one base64 URL; WARP subs and user subs live in two different settings subtabs; the browser-facing info page (?view=html, subscribe.ts:93-97) is exposed only as a cryptic home row labeled 'Panel info' (status.ts:54). Compare BPB Panel /\u2026",
    "recommendation": "Add a top-level '#/subs' hub (third tab). Contents: (1) main subscription, one row per format (base64/clash/singbox/surge/loon/quantumult) with copy/QR/download/open-in-client deep links; (2) per-user section reusing usersSubUrl with a per-user format dropdown or per-format rows; (3) WARP accounts with their 17 formats (reuse warpSubsHtml); (4) 'Panel info' page link with one-line explanation; (5)\u2026",
    "severity": "high",
    "source": "repo audit",
    "title": "No Subscriptions hub: the core product is scattered across four surfaces, none complete"
   },
   {
    "effort": "M",
    "evidence": "shell.html:43-44 renders exactly two tabs (see finding 1). WARP is the last entry of the settings subtab list: settings.js:117 `\"{key:'warp',cards:[{warpAccounts:true,fields:[]},{warpPresets:true,fields:[]},{warpAmnezia:true,fields:[]}]}` \u2014 reachable only as the 10th subtab under Settings. The dictionary still carries an orphaned top-nav key proving a dedicated tab was planned and dropped: dict.js\u2026",
    "location": "src/ui/panel/shell.html:43-44; src/ui/panel/settings.js:117; src/ui/panel/dict.js:3; src/ui/panel/home.js:98",
    "problem": "CONFIRMED. WARP is a first-class feature (17 output formats, its own route family /sub/wg/<token>/<format>, 18KB of view code) yet sits two levels deep behind generic settings chrome. Discovery depends on reading wizard copy that disappears forever after first run.",
    "recommendation": "Promote WARP to a third top-level tab (the dead nav.warp key is already translated EN+FA) or at minimum give Home a persistent WARP card with a 'Generate account' CTA even when accounts are empty, mirroring the empty-card pattern already used in the WARP section itself (warp.js:61).",
    "severity": "high",
    "source": "repo audit",
    "title": "Owner pain (1) confirmed: WARP is buried as subtab #10; the nav even ships a dead 'WARP' tab key"
   },
   {
    "effort": "S",
    "evidence": "users.js:29 `'tb.innerHTML=S.users.length?S.users.map(userRowHtml).join(''):\\'<tr><td colspan=\"6\"><span class=\"field__hint\">\\'+esc(t(\\'users.empty\\'))+\\'</span></td></tr>\\';'` \u2014 the entire zero-state is the string 'No users yet' (dict.js `'users.empty':'No users yet'`) in a table cell. There is no button, no explanation of what users are for, no link to the Add User modal. The codebase already has\u2026",
    "location": "src/ui/panel/users.js:29; src/ui/panel/home.js:113-117",
    "problem": "CONFIRMED. An admin who has never added a user sees a dead table and a card telling them there are no users. The feature's value (scoped links, protocol filters, quotas, expiry \u2014 dict users.desc) is never explained at the moment of emptiness, so the feature reads as noise.",
    "recommendation": "Reuse the empty-card pattern in the users table: icon + one-line value prop + primary 'Add User' button (data-action=\"users-add\" already exists) + secondary explainer. Mirror it on the Home users card when count is 0. Effort S.",
    "severity": "medium",
    "source": "repo audit",
    "title": "Owner pain (3) confirmed: Users empty state is a bare hint row with no CTA"
   },
   {
    "effort": "S",
    "evidence": "The server fully supports per-user format targeting: routes.ts:74-80 accepts `'if ((rest.length === 3 || rest.length === 4) && rest[1] === \"u\" && /^[0-9a-f-]{36}$/i.test(rest[2]!)) return { kind: \"user-sub\" };'` (4th segment = format), users-sub.ts:52-53 `'const pathTarget = segs.length === 5 ? segs[4] : undefined; const format = pickSubFormat(req, pathTarget);'` and negotiate.ts:14-16 honors `'if\u2026",
    "location": "src/core/routes.ts:74-80; src/handlers/users-sub.ts:52-53; src/subscription/negotiate.ts:14-16; src/ui/panel/users.js:2-13",
    "problem": "Half the delivery pipeline is invisible. Admins sharing per-user links cannot guarantee the right format for a named client app, and per-user per-format URLs are the main reason the user system exists. WARP gets 17 explicit format rows (warp.js:1, WARP_F) while users \u2014 the higher-stakes sharing surface \u2014 get one.",
    "recommendation": "In the users table (or a per-user detail drawer on the hub), render the format variants `<userSubUrl>/<format>` for all six SUB_FORMATS behind a small expander, mirroring warpSubsHtml. Pure client-side string concat; zero backend work needed.",
    "severity": "medium",
    "source": "repo audit",
    "title": "Per-user per-format subscription URLs exist server-side but the UI never exposes them"
   },
   {
    "effort": "S",
    "evidence": "The admin configures it under Advanced: settings.js:111 `'FL('subUpdateIntervalHours','num','advanced.subInterval.label',null,{min:1,max:168})'` ('Sub update interval (hours)'). The subscription header generator receives it in its meta contract \u2014 headers.ts:7 `'updateIntervalHours: number;'` \u2014 and then never reads it: headers.ts:49-54 hardcodes `'\"Cache-Control\": `public, max-age=${SUB_THROTTLE_SE\u2026",
    "location": "src/subscription/headers.ts:41,49-54; src/subscription/headers.ts:7; src/nodes/emitters/surge-conf.ts:96-97; test/subscription/headers.spec.ts:63-66; src/ui/panel/settings.js:111",
    "problem": "A settings field that provably does nothing for 5 of 6 formats (only Surge consumes it) erodes trust and produces real confusion when a client keeps polling every 60 minutes regardless of the configured 12h interval. The golden test will 'break on purpose' if fixed \u2014 per repo rules that requires an architecture note.",
    "recommendation": "Either derive Profile-Update-Interval (hours) from subUpdateIntervalHours (noting the 60s edge-cache TTL is a separate concern from client polling) or relabel the setting to state it applies to Surge managed-config only. Golden test update required.",
    "severity": "medium",
    "source": "repo audit",
    "title": "'Sub update interval (hours)' setting is silently ignored by subscription headers \u2014 Profile-Update-Interval is hardcoded to 60 minutes"
   },
   {
    "effort": "M",
    "evidence": "Merging happens in exactly one branch: render.ts:56-59 `'if (input.format === \"base64\") { \u2026 const [ownLines, remoteLines] = await Promise.all([\u2026, fetchRemoteSubLines(input.settings.remoteSubUrls, \u2026)])'` \u2014 clash/singbox/surge/loon/quantumult never see remote lines. Per-user links are force-drained: users-sub.ts:90 `'const subSettings = { ...s, remoteSubUrls: [] as string[] };'`. The field itself is\u2026",
    "location": "src/subscription/render.ts:53-60; src/handlers/users-sub.ts:90; src/subscription/merge.ts:64-121; src/ui/panel/settings.js:76-78",
    "problem": "An admin who pastes a provider URL and then opens the Clash link sees zero merged nodes and gets no signal why. This is the classic silent-feature trap; rival panels surface merge results or at least node counts per source.",
    "recommendation": "Minimum: extend the field hint to 'Base64/admin subscriptions only \u2014 per-user links and format-specific outputs exclude these.' Better: a status row in Sources/Addresses showing per-URL last-fetch result (ok/skipped/line count) reusing the address-probe pattern (actions.js 'addr-probe' exists as precedent).",
    "severity": "medium",
    "source": "repo audit",
    "title": "Remote-sub merge is base64-only and admin-only, with zero feedback \u2014 silent failure by design"
   },
   {
    "effort": "S",
    "evidence": "The server silently rewrites the payload per User-Agent: ua.ts:10-33 classifyUA maps ~25 client tokens to formats and returns 'browser' for Mozilla/Chrome/Safari/Firefox, which triggers the HTML info page (negotiate.ts:11, subscribe.ts:93-97). The panel's only URL-parameter hint anywhere is the country filter: home.js:99 `'if(urls.length)sh+='<p class=\"field__hint\" dir=\"auto\">'+esc(t('country.hint\u2026",
    "location": "src/core/ua.ts:10-33; src/subscription/negotiate.ts:12-24; src/ui/panel/home.js:93-99; src/handlers/subscribe.ts:71-97; src/handlers/api/status.ts:54",
    "problem": "The format matrix is the product's most clever mechanism and it is undocumented in the admin UI. Admins cannot answer 'why did my client get YAML when I pasted the base64 link?' without reading source. The Panel info row wastes its explanatory potential.",
    "recommendation": "One collapsible 'How links negotiate' hint on the subs card: UA auto-detection, ?target= override, ?mode=fragment, ?country=XX, and 'open the Panel info URL in a browser to see all variants'. Rename the row label to 'Info page (open in browser)'. Effort S, i18n keys needed.",
    "severity": "low",
    "source": "repo audit",
    "title": "UA negotiation and URL parameters are completely unexplained in the UI; the info page surfaces as a cryptic 'Panel info' row"
   },
   {
    "effort": "S",
    "evidence": "qr.js:17-18 `'while(ver<=25&&dataCw(ver)*8-4-(ver<10?8:16)<bytes.length*8)ver++; if(ver>25)return null;'` \u2014 byte mode, single EC table, versions 1\u201325 (~1 KB payload ceiling). Failure path is a dead end: actions.js openQr \u2192 `if(!QR.render($('qr-canvas'),url)){toast(t('toast.tooLong'),'err');return}` ('URL too long for a QR code.') with no alternative offered (no SVG export, no shortened link). Ever\u2026",
    "location": "src/ui/panel/qr.js:17-18; src/ui/panel/actions.js:26-34; src/ui/panel/actions.js:openQr",
    "problem": "The hard ceiling is fine for typical sub URLs (<200 chars) but silently caps exotic ones (long workers.dev hostnames + query params), and the single filename is actively wrong metadata for the user-sub use case where QRs are shared onward.",
    "recommendation": "Pass a context string into download-qr (data-filename on the modal or derive from the QR'd value), and on version-25 overflow offer 'copy instead' in the toast. Effort S.",
    "severity": "low",
    "source": "repo audit",
    "title": "QR encoder is a competent hand-rolled implementation but caps at version 25 and names every download q-proxy-subscription.png"
   },
   {
    "effort": "S",
    "evidence": "The same six formats carry three independent label sets: status.ts:46-53 buildSubUrls \u2192 'Base64/Mixed','Clash / mihomo','sing-box','Surge','Loon','Quantumult X'; subscribe.ts:15-22 FORMAT_LABELS \u2192 'Base64 / v2rayNG','Clash / mihomo','sing-box','Surge','Loon','Quantumult X'; warp.js:1 WARP_F \u2192 17 hardcoded tuples ('WireGuard .conf (ZIP)', 'Throne wg://', \u2026) \u2014 none localized, none shared. On Home th\u2026",
    "location": "src/handlers/api/status.ts:45-55; src/handlers/subscribe.ts:15-22; src/ui/panel/warp.js:1; src/ui/panel/home.js:90",
    "problem": "An admin sees 'Base64/Mixed' on Home and 'Base64 / v2rayNG' on the public info page for the identical URL. Drift here is exactly what the fields.spec.ts drift-test philosophy exists to prevent, but only settings fields got that treatment \u2014 labels did not.",
    "recommendation": "Single-source format labels in one exported map (server or i18n dict) consumed by buildSubUrls, infoPageHtml and the emitters' UI lists; include 'Panel info' in the chip count or move it out of the list into a footer link.",
    "severity": "low",
    "source": "repo audit",
    "title": "Format labels are triplicated with inconsistent naming, and the Home 'formats' chip disagrees with the visible list"
   },
   {
    "effort": "M",
    "evidence": "The only consumption affordances anywhere are copy and QR: home.js:56 copyFieldHtml `'<code id=\"'+idAttr+'\" dir=\"ltr\">'+esc(value)+'</code><button \u2026data-action=\"copy\"\u2026><button \u2026data-action=\"qr\"\u2026>'`. The server already does the heavy lifting for downloads \u2014 subscribe.ts emits 'Content-Disposition: attachment; filename*=UTF-8\u2026' per format (subscribe.ts headers assembly; render.ts SUB_CONTENT_TYPES) \u2026",
    "location": "src/ui/panel/home.js:56; src/ui/panel/dict.js:confirm.securepath_body; src/handlers/subscribe.ts (Content-Disposition headers); src/subscription/render.ts:SUB_CONTENT_TYPES",
    "problem": "Every rival panel (BPB's one-click import buttons, edgetunnel's per-client links) reduces time-to-connected below Q Proxy's copy\u2192switch-app\u2192paste loop. The server's attachment headers are wasted because the panel never exposes them.",
    "recommendation": "On the hub rows add a download button (plain GET link) and, where trivially safe, client deep-links behind a details expander. After a securePath change, show the new sub URLs inline in the confirmation dialog. Effort M.",
    "severity": "low",
    "source": "repo audit",
    "title": "No one-click client import / download actions \u2014 the panel stops at clipboard, rivals ship deep links"
   },
   {
    "effort": "S",
    "evidence": "Every sub response carries a 60-second throttle: headers.ts:41 `'export const SUB_THROTTLE_SECONDS = 60;'`, headers.ts:51 `'\"Cache-Control\": `public, max-age=${SUB_THROTTLE_SECONDS}, s-maxage=${SUB_THROTTLE_SECONDS}`'`, and the edge cache key is versioned by settings etag so edits do invalidate: subscribe.ts:27-31 versionedEdgeCacheKey `keyUrl.searchParams.set(\"_v\", settingsCacheStamp(s))`. The ho\u2026",
    "location": "src/subscription/headers.ts:41-54; src/handlers/subscribe.ts:27-31,97; src/ui/panel/dict.js:home.subs.quota",
    "problem": "The classic post-edit complaint 'I changed settings but my client still shows old nodes' has no in-panel answer. One hint line prevents a whole support category.",
    "recommendation": "One hint under the subs card: 'Client updates: profile header advertises ~60 min; edge cache serves up to 60s-stale after changes (versioned cache key busts it on settings save).' Effort S.",
    "severity": "low",
    "source": "repo audit",
    "title": "Cache/versioning behavior is implemented well but entirely invisible to the admin"
   },
   {
    "effort": "S",
    "evidence": "chrome.js:79 `'if(mod&&e.key.toLowerCase()==='k'){e.preventDefault();const s=document.querySelector('#settings-search');if(s)s.focus();else location.hash='#/home';return}'` \u2014 the selector appears nowhere in shell.html or any generated markup (grep across src/ui/panel: single hit). The advertised shortcut sheet even promises it: chrome.js:79 rows list `['Ctrl / \u2318 + K','shortcuts.search']` in render\u2026",
    "location": "src/ui/panel/chrome.js:79; src/ui/panel/shell.html (no #settings-search element)",
    "problem": "A advertised keyboard shortcut is a silent no-op that dumps you on Home. It is also the fossil of an intended global search that a Subscriptions hub (or the settings sprawl: 10 subtabs, 55 functions in settings.js) genuinely needs.",
    "recommendation": "Either implement the command palette (search sections, sub URLs, users, WARP accounts) or remove the shortcut row. If the hub lands, this becomes its jump-to-url box. Effort S (removal) / M (palette).",
    "severity": "low",
    "source": "repo audit",
    "title": "Ctrl/Cmd+K targets a search box that does not exist \u2014 silently reroutes to Home"
   },
   {
    "effort": "S",
    "evidence": "The header contract declares the knob \u2014 headers.ts:7 `'updateIntervalHours: number;'` \u2014 and both call sites dutifully pass it (subscribe.ts meta object, users-sub.ts `'updateIntervalHours: s.subUpdateIntervalHours'`), but subscriptionHeaders (headers.ts:57+) reads only meta.expireAt and meta.webPageUrl; the golden test explicitly asserts the override never applies: test/subscription/headers.spec.t\u2026",
    "location": "src/subscription/headers.ts:7,57-77; src/handlers/subscribe.ts; src/handlers/users-sub.ts; test/subscription/headers.spec.ts:63-66",
    "problem": "Dead contract parameter + a test that enshrines the dead behavior = future contributors will keep assuming it works. Distinct from finding 5 in that this is the code-hygiene evidence for it.",
    "recommendation": "When fixing finding 5, either consume meta.updateIntervalHours in throttleHeaders or delete it from SubscriptionMeta and both call sites; update the golden test in the same commit per the wire-format-change rule (ask first, changelog note).",
    "severity": "low",
    "source": "repo audit",
    "title": "SubscriptionMeta.updateIntervalHours is a dead parameter threaded through the whole stack"
   },
   {
    "effort": "S",
    "evidence": "Both views end at 'Manage': home.js:113-117 `'\u2026<a class=\"btn btn--ghost btn--sm\" href=\"#/settings/users\">'+esc(t('home.users.manage'))+'</a>\u2026'` and home.js:169-176 renderHomeUsers renders `'\u2026<span class=\"lbl\">'+esc(t('users.title'))+'</span><span class=\"mono\">'+all.length+'</span>\u2026'` only. The WARP equivalent on Home is strictly better \u2014 actual per-account subscription rows in a collapsible (warp.\u2026",
    "location": "src/ui/panel/home.js:113-117; src/ui/panel/home.js:169-176; src/ui/panel/warp.js:92",
    "problem": "Home is supposed to be the operator's answer to 'who is consuming what', but for users it is a link factory. Combined with the empty-state gap (finding 3), the users surface reads as unfinished relative to WARP.",
    "recommendation": "Either mirror warpSubsHtml (per-user link rows in a collapsible, with per-format expander) or fold users into the hub page and shrink the Home card to status chips. Effort S once finding 1's hub exists.",
    "severity": "low",
    "source": "repo audit",
    "title": "Home users card is a stats stub \u2014 WARP gets real per-account link rows, users get a counter"
   }
  ],
  "removals": [
   "Nothing qualifies for outright deletion except dead weight: the orphaned 'nav.checker' i18n key (dict.js:3) and the '#settings-search' branch in chrome.js:79 if the command palette is not built \u2014 both reference UI that does not exist.",
   "The 'Panel info' copy-row on Home (status.ts:54 rendered via home.js:96-97) should be removed from the link list and re-expressed as a labeled footer link once the hub exists \u2014 it is not a subscription URL and its presence is why the formats chip needs a special-case filter (home.js:90)."
  ],
  "summary": "The sub URL is the product, yet delivery is fragmented across a Home card, a one-time wizard step, a WARP detail card, and a users-table token column \u2014 there is no single place that answers \"which URL, for which app, for whom\". The backend is strong (6 admin formats, UA sniffing, per-user per-format path routing, versioned edge cache) but the UI surfaces roughly half of it, with the rest hidden behind silent server behavior (base64-only remote merge, hardcoded 60-minute update header, per-user format routes). All three owner pain points are substantially confirmed: WARP is subtab #10 with a dead \"WARP\" nav key proving a dropped top-level tab, main subs are copyable but unexplained and unranked, and the Users empty state is a bare \"No users yet\" table row with no CTA. A dedicated Subscriptions hub holding per-format links, per-user per-format links, merge status, and cache/update-interval truth would fix the root cause of all three at once."
 },
 {
  "consolidations": [
   "proxyip.* (9 keys, dead) are string-identical to the live egress.* keys \u2014 e.g. dict.js:3 'proxyip.list.label':'Proxy IP list' == 'egress.list.label':'Proxy IP list', 'proxyip.nat64.help' == 'egress.nat64.help'. The Egress rename duplicated copy instead of moving it; deleting the dead set also removes the duplication.",
   "'addresses.remoteSubs.label/hint' (live, dict.js:3) duplicates the dead 'ports.remoteSubs.label/hint' verbatim \u2014 one copy survives only as drift bait.",
   "One feature is named three ways: switch label 'general.killSwitch.label' = 'Kill switch', confirm title 'confirm.killswitch_title' = 'Pause all traffic?', toast 'toast.killOn' = 'Traffic paused'. Pick 'pause' everywhere (the kill switch only pauses, and the confirm already says so) and keep 'Kill switch' at most as a secondary tag.",
   "'users.status.disabled' chip duplicates the 'users.col.enabled' toggle column state in the same row (users.js:14) \u2014 one representation of enabled/disabled is enough.",
   "home.users.desc (dict.js:3) is a compressed duplicate of users.desc \u2014 keep one source and reuse it in both places, or differentiate them deliberately (home card = count-focused, settings tab = feature-focused).",
   "login.html:127 maintains its own DICT that re-defines common.theme_light/theme_dark/theme_toggle/common.reveal/common.close already present in panel dict.js:3 \u2014 two dictionaries must be edited in lockstep for identical strings."
  ],
  "dimension": "Copy & Microcopy \u2014 Q Proxy admin panel (dict.js EN/FA sample, hardcoded strings, destructive-op confirmations, dead-key sweep)",
  "findings": [
   {
    "effort": "S",
    "evidence": "grep across all view sources (home.js, settings.js, users.js, warp.js, actions.js, chrome.js, lib.js, shell.html, login.html) finds zero references to: nav.warp, nav.checker, all 20 checker.* keys, all 23 ports.* keys, all 9 proxyip.* keys, tabs.settings.ports, tabs.settings.proxyip, warp.settings, warp.view.accounts, warp.view.settings, toast.saveFailed, toast.langChanged, err.url, err.port, err.\u2026",
    "location": "src/ui/panel/dict.js:3 (EN) and :20 (FA)",
    "problem": "64 dead keys is not noise, it is fossil record: an entire removed 'Proxy IP Checker' feature (checker.title 'Proxy IP checker', checker.summary '{ok} healthy of {total.}'), the removed Ports/CDN-fronting settings group (ports.cdn.title 'CDN fronting', ports.matrix.legend_tls 'TLS ports'), and a pre-Egress rename layer (proxyip.*) all remain fully translated in EN and FA. Any translator or agent ed\u2026",
    "recommendation": "Delete all 64 keys from both the EN and FA blocks. Delete the dead `case 'ports'` in bindHtml (settings.js:222-226) and the checker-targets branch (settings.js:564) that keep common.yes/common.no alive. Add a drift test that fails when a dict key is unreferenced after a build (mirror of test/settings/fields.spec.ts).",
    "severity": "high",
    "source": "src/ui/panel/dict.js",
    "title": "64 dead dict keys (8.5% of the dictionary): two removed features and one rename still fully translated in EN+FA"
   },
   {
    "effort": "S",
    "evidence": "`const WARP_F=[['wireguard-conf','WireGuard .conf (ZIP)','grad-1'],\u2026['throne','Throne wg://',\u2026],['v2rayn','V2RayN (base64)',\u2026],['singbox-legacy','sing-box legacy',\u2026],['clash','Clash / mihomo',\u2026],['surge','Surge',\u2026],['egern','Egern','grad-6']]` \u2014 none of these 17 strings appear in dict.js. They are rendered verbatim via `'<span class=\"fmt-label\">'+esc(fmt[1])+'</span>'` in renderWarpDetail (warp.js\u2026",
    "location": "src/ui/panel/warp.js:2",
    "problem": "This is the single largest dict bypass in the panel: the WARP subscription section \u2014 one of the product's headline features \u2014 renders 17 untranslated English labels to Persian-locale users. Format names like 'sing-box' are legitimately untranslatable, but '(ZIP)', 'legacy', and '(base64)' qualifiers are prose and belong in the dict (cf. the 'warp.presets.count' '{n} endpoints' pattern that does ex\u2026",
    "recommendation": "Move the 17 labels to dict keys (warp.fmt.wireguard_conf etc.) with FA translations for the prose qualifiers, keeping protocol names as shared tokens. A small drift test (key count == WARP_F length) prevents regression.",
    "severity": "high",
    "source": "src/ui/panel/warp.js",
    "title": "17 WARP subscription format labels hardcoded in WARP_F, bypassing the dict entirely \u2014 English-only in the FA UI"
   },
   {
    "effort": "S",
    "evidence": "`'settings-import'(){ \u2026 const parsed=JSON.parse(await file.text()); if(parsed&&parsed.kind==='q-proxy-settings'&&parsed.settings){ await api('api/settings/import',{method:'POST',body:{settings:parsed.settings}}); toast(t('general.backup.imported'),'ok'); location.reload()} else toast(t('general.backup.badfile'),'err')}` \u2014 no confirmDialog anywhere on the path. Contrast 'reset-defaults' (actions.js\u2026",
    "location": "src/ui/panel/actions.js:46-62",
    "problem": "Import overwrites every setting in one shot \u2014 the same blast radius as reset-to-defaults, which the panel itself treats as confirm-worthy ('confirm.reset_title':'Reset to defaults?'). The current copy implies import is additive ('general.backup.import_hint' says what it contains) but never warns that current settings will be replaced. One mis-click on a stale export file silently reverts live conf\u2026",
    "recommendation": "Gate the import behind confirmDialog with a new key (title 'Import settings?', body 'Current settings are replaced immediately. This cannot be undone.') and show the file name in the dialog. Upgrade badfile copy to 'That file is not a Q Proxy settings export \u2014 export a fresh copy first.'",
    "severity": "high",
    "source": "src/ui/panel/actions.js",
    "title": "Settings import overwrites the entire config with NO confirmation dialog"
   },
   {
    "effort": "S",
    "evidence": "`'warp-preset-del'(el){ (async()=>{ try{ await api('api/warp/presets/'+el.dataset.id,{method:'DELETE',mutate:true}); toast(t('warp.toast.presetDeleted'),'ok'); \u2026` \u2014 no confirmDialog call. Every comparable destructive action in the same file wraps itself: 'warp-delete' uses warp.confirm.delete_title, 'users-del' uses users.confirm_delete_title, even logout confirms.",
    "location": "src/ui/panel/actions.js:117-124",
    "problem": "A single click on a 16px X icon (warp.js:68, `btn--icon btn--ghost-danger`) permanently deletes an endpoint preset, with accounts that referenced it left showing a raw preset_id (warp.js:62 fallback `{name:a.endpoint_list&&a.endpoint_list.preset_id||''}`). The app's own confirmation discipline \u2014 the best part of its copy \u2014 is silently skipped here. The toast 'Preset deleted' arrives with no undo a\u2026",
    "recommendation": "Add warp.confirm.presetdelete_title/body keys ('Delete this preset? Accounts using it fall back to a raw endpoint value.'). Same one-liner pattern as warp.confirm.delete_title.",
    "severity": "high",
    "source": "src/ui/panel/actions.js",
    "title": "WARP preset deletion is a one-click irreversible destructive action with no confirmation"
   },
   {
    "effort": "S",
    "evidence": "`if(!(await confirmDialog('users.bulk.selected','users.bulk.confirm',true,{n:ids.length})))return;` where dict.js:3 has 'users.bulk.selected':'{n} selected' (a status chip label) and 'users.bulk.confirm':'Apply to {n} user(s)?' \u2014 the dialog title reads e.g. \"3 selected\".",
    "location": "src/ui/panel/users.js:49",
    "problem": "The dialog never says WHAT will be applied. 'Enable', 'Disable', 'Set expiry' and 'Delete' (users-bulk-del) all funnel into this one generic dialog, so the admin confirms 'Apply to 3 user(s)?' without knowing whether they are enabling or deleting. For a destructive bulk delete, the danger styling of the OK button is the only signal. This is the only confirm dialog in the panel whose body omits the\u2026",
    "recommendation": "Add a per-action body key, e.g. users.bulk.confirm_delete ('Delete {n} user(s)? Their subscription links stop working immediately.') mirroring the excellent users.confirm_delete_body, and pass the action verb into the dialog title.",
    "severity": "medium",
    "source": "src/ui/panel/users.js",
    "title": "Bulk-user confirmation reuses the status-chip string as the dialog title and never names the action"
   },
   {
    "effort": "S",
    "evidence": "`'<td data-l=\"'+esc(t('users.col.token'))+'\" dir=\"ltr\">'+(url?'<code \u2026>'+esc(url)+'</code>':'<span class=\"field__hint\">'+esc(t('users.toast.regen'))+'</span>')+'</td>'` \u2014 users.toast.regen (dict.js:3) is the success toast '\u062a\u0648\u06a9\u0646 \u062c\u062f\u06cc\u062f \u0622\u0645\u0627\u062f\u0647 \u0634\u062f' / 'New token ready'.",
    "location": "src/ui/panel/users.js:15",
    "problem": "A completion toast ('New token ready') is shown as a permanent hint in the subscription-URL column of any user whose token is missing \u2014 asserting something just happened when nothing happened. This is exactly the string-reuse drift the toast/label key separation exists to prevent, and it produces a nonsensical table row ('user: alice \u2014 New token ready').",
    "recommendation": "Add a dedicated users.token_missing key ('No link yet \u2014 regenerate the token') for the empty cell, and keep users.toast.regen strictly for the post-regeneration toast + rotation modal.",
    "severity": "medium",
    "source": "src/ui/panel/users.js",
    "title": "Toast string 'New token ready' reused as a table-cell placeholder for a MISSING token"
   },
   {
    "effort": "M",
    "evidence": "settings.js:532 `const err=fw.querySelector('.field__error'); if(err)err.textContent=/\\s/.test(msg)||!DICT[LANG][msg]&&!DICT.en[msg]?msg:t(msg);` \u2014 any server message containing a space is rendered verbatim. validate.ts `fail(fields, key, \"must be a boolean\")`, `\"must be a string\"`, `\"must be a Cloudflare-proxied TLS port\"`, `\"must not target a local or private address\"` (83 fail() calls, none key\u2026",
    "location": "src/settings/validate.ts:37 + src/ui/panel/settings.js:532",
    "problem": "The FA locale shows JSON-validator English ('must be a boolean') at the exact moment of failure \u2014 the highest-attention copy moment in the panel. The four client-side err.* keys that exist for this purpose (err.port, err.url, err.ip_or_host, err.ipv6_prefix) are dead because the server never emits them, so the i18n plumbing for validation errors exists but is connected to nothing. Documented as a \u2026",
    "recommendation": "Minimum: have validate.ts emit stable codes ('err.port', 'err.url'\u2026 \u2014 the keys already exist in the dict) and let showFieldError translate them, falling back to the English sentence for unknown codes. Full fix is bilingual server messages, which can wait.",
    "severity": "medium",
    "source": "src/settings/validate.ts",
    "title": "Server validation messages render as raw English sentences in the FA UI ('must be a boolean')"
   },
   {
    "effort": "S",
    "evidence": "shell.html:19 `<a class=\"visually-hidden skip-link\" href=\"#main\">Skip to content</a>`; shell.html:67 `<div class=\"subtabs\" role=\"tablist\" aria-label=\"Settings sections\" id=\"subtabs\">`; shell.html:83 `<noscript><p style=\"padding:24px;text-align:center\">JavaScript is required.</p></noscript>`; shell.html:67's label is never overwritten \u2014 buildShell only sets tab text, h1s, logout, QR, modal labels; \u2026",
    "location": "src/ui/panel/shell.html:19, :67, :83",
    "problem": "buildShell retro-localizes most chrome (nav aria-label \u2190 t('nav.views'), swatches \u2190 t('accent.label'), theme toggle \u2190 theme keys), but four strings are never touched, so a FA screen-reader user hears 'Settings sections' and 'Language' in English on every page, and the noscript fallback is English-only. Inconsistent application of the app's own runtime-localization pass rather than missing capabili\u2026",
    "recommendation": "Bind the four stragglers in buildShell: subtabs aria-label (new 'tabs.settings.aria' key), skip-link text, noscript text (or drop the noscript copy to a language-neutral icon), and the seg group aria-label.",
    "severity": "medium",
    "source": "src/ui/panel/shell.html",
    "title": "Hardcoded English chrome in shell.html that the FA locale never sees translated: skip link, subtabs aria-label, noscript, 'Language' group"
   },
   {
    "effort": "S",
    "evidence": "`case 'ports':{ const fam=f.family==='tls'?TLS_PORTS:PLAIN_PORTS; \u2026 title=\"'+esc(t('common.yes'))+'/'+esc(t('common.no'))+'\" \u2026 aria-label=\"'+esc(t(f.label))+' all\"` \u2014 but no FL() entry anywhere in SECTIONS (settings.js:26-120) declares type:'ports', so this branch is unreachable. common.yes/common.no ('Yes'/'No') are referenced nowhere else.",
    "location": "src/ui/panel/settings.js:222-226",
    "problem": "Dead code keeping two pointless dict keys alive, and even when it was alive the copy was wrong: a tooltip reading 'Yes/No' explains nothing about a select-all checkbox, and 'TLS ports all' / '\u067e\u0648\u0631\u062a\u200c\u0647\u0627\u06cc TLS all' is a mixed-language aria-label. The replacement pattern already exists in the codebase (users-select-all uses users.col.select 'Select').",
    "recommendation": "Delete case 'ports' from bindHtml, delete common.yes/common.no from both languages. If a port master toggle returns, give it a real aria key ('Select all {label}').",
    "severity": "medium",
    "source": "src/ui/panel/settings.js",
    "title": "Dead `case 'ports'` renderer keeps common.yes/common.no alive and concatenates English 'all' onto translated aria-labels"
   },
   {
    "effort": "S",
    "evidence": "users.js:29 `tb.innerHTML=S.users.length?\u2026:'<tr><td colspan=\"6\"><span class=\"field__hint\">'+esc(t('users.empty'))+'</span></td></tr>'` where dict.js:3 'users.empty':'No users yet'. Contrast home.js renderHome: `'<div class=\"empty-card\"><div class=\"empty-icon\">\u2026</div><div class=\"empty-title\">\u2026</div><p class=\"empty-msg\">'+esc(t('home.subs.empty_msg'))+'</p><div class=\"empty-actions\"><a class=\"btn bt\u2026",
    "location": "src/ui/panel/users.js:29 + dict.js:3 ('users.empty':'No users yet')",
    "problem": "The Users empty state breaks the panel's own established empty-state grammar (icon + title + one-sentence value pitch + primary CTA). 'No users yet' tells the admin nothing about why they'd add one \u2014 no mention of scoped links, per-user protocol filters, quota or expiry, which is precisely the feature's value proposition. This compounds the owner-verified pain that the Users page feels meaningless\u2026",
    "recommendation": "Give users.empty the full pattern: title 'No users yet', msg 'Share scoped subscription links with per-user protocol filters, limits and expiry.', CTA button reusing users.add ('Add User'). The keys for msg/CTA can alias users.desc and users.add.",
    "severity": "medium",
    "source": "src/ui/panel/users.js",
    "title": "Users empty state is a bare one-liner while Subscriptions and WARP get icon + title + message + CTA"
   },
   {
    "effort": "M",
    "evidence": "dict.js:3 'egress.nat64.help':'One IPv6 prefix per line (e.g. 2a02:898:146:64::).' \u2014 format only, no purpose; 'protocols.sniCase.label':'Randomize SNI case' + hint 'Randomizes letter case of the SNI hostname.' \u2014 defines SNI case-randomization in terms of SNI. Counter-example done right, same file: 'protocols.ech.label':'ECH (Encrypted ClientHello)', 'protocols.ech.short':'Encrypts the TLS ClientHe\u2026",
    "location": "src/ui/panel/dict.js:3 ('egress.nat64.help', 'protocols.sniCase.hint') vs ('protocols.ech.label')",
    "problem": "The panel's own ECH copy proves the glossary pattern works (label expands the acronym once, .short gives the one-line effect, .help gives behavior). NAT64 never says what it does or when an admin would choose it over a Proxy IP list (it is the egress fallback for v6-only reachability); SNI case randomization never says it evades DPI matching. The short/help suffix system exists \u2014 these fields just\u2026",
    "recommendation": "Add .short/.help pairs for nat64Prefixes ('Lets nodes exit via an IPv6-only relay when no proxy IP works'), sniCase, and a Home tooltip for 'CF colo' mirroring the FA wording. Reuse the ECH structure verbatim.",
    "severity": "medium",
    "source": "src/ui/panel/dict.js",
    "title": "Jargon policy is inconsistent: ECH is glossed beautifully, NAT64 and SNI are not glossed at all"
   },
   {
    "effort": "S",
    "evidence": "dict.js:3 'sources.tools.hint':'Resolves relay IPs from the built-in DNS pool (edgetunnel-style).' \u2014 an aside naming a third-party project (cmliu/edgetunnel) inside the operational UI.",
    "location": "src/ui/panel/dict.js:3 ('sources.tools.hint')",
    "problem": "Every other hint explains behavior in terms of what the setting does; this one explains it by analogy to a different product's implementation. For a single-admin tool the reference is harmless, but it is the only copy in the panel that assumes the reader knows rival projects, it will confuse anyone else, and it ages badly if that project renames or dies.",
    "recommendation": "Rewrite behavior-first: 'Resolves relay IPs from the built-in DNS pool \u2014 no extra setup needed.' Keep the edgetunnel attribution in docs/research notes, not UI copy.",
    "severity": "low",
    "source": "src/ui/panel/dict.js",
    "title": "Helper copy name-drops a rival project: '(edgetunnel-style)'"
   },
   {
    "effort": "S",
    "evidence": "dict.js:3 'addresses.empty_hint':'No addresses yet - nodes use your worker hostname on the default port.' and 'remote.nodes.help':'\u2026holds no private keys - only public credentials are stored\u2026' \u2014 both use a bare hyphen. Nearly every other multi-clause string uses an em dash: 'common.generated':'Generated \u2014 Apply to save.', 'toast.networkError':'Network error \u2014 check your connection.', 'home.kill.de\u2026",
    "location": "src/ui/panel/dict.js:3 ('addresses.empty_hint', 'remote.nodes.help')",
    "problem": "Two strings break the panel's typographic convention in the same way, which reads as copy written outside the final editorial pass. The FA block consistently uses the em dash elsewhere, so the inconsistency also propagates into the FA strings for these two keys.",
    "recommendation": "Replace ' - ' with ' \u2014 ' in both keys (EN and FA blocks).",
    "severity": "low",
    "source": "src/ui/panel/dict.js",
    "title": "Dash typography drift: ASCII hyphen where the rest of the copy uses an em dash"
   },
   {
    "effort": "S",
    "evidence": "dict.js:3 Title Case: 'warp.generate':'Generate Account', 'warp.import':'Import Config', 'warp.presets.add':'Add Preset', 'users.add':'Add User'. Sentence case, same file: 'addresses.add':'Add address', 'remote.nodes.add':'Add node', 'checker.run':'Run test' (dead), 'egress.pool.fetch':'Fetch pool'.",
    "location": "src/ui/panel/dict.js:3",
    "problem": "Same-class elements (action buttons) follow two different casing rules within one screen family \u2014 Settings \u2192 Addresses renders 'Add address' next to nothing else, while Settings \u2192 WARP renders 'Generate Account' and 'Add Preset'. Minor individually, but visible on every empty state where both patterns can co-occur (warp empty state shows Title Case CTAs; addresses empty state shows sentence-case o\u2026",
    "recommendation": "Standardize on sentence case for all button labels (GitHub/Cloudflare-dash convention): 'Generate account', 'Import config', 'Add preset', 'Add user'. Mechanical dict edit, no code changes.",
    "severity": "low",
    "source": "src/ui/panel/dict.js",
    "title": "Button-label capitalization is inconsistent: Title Case vs sentence case across equivalent CTAs"
   },
   {
    "effort": "S",
    "evidence": "dict.js:20 'fragment.split':'\u0628\u06cc\u0634\u06cc\u0646\u0647\u0654 \u0634\u06a9\u0633\u062a \u06a9\u0645\u06cc\u0646\u0647\u2013\u0628\u06cc\u0634\u0646\u0647' (literally 'maximum of breaking minimum\u2013maximum' \u2014 double superlative, hard to parse). dict.js:20 'chain.uri.hint':'\u200fsocks5://user:pass@host:port \u06cc\u0627 http://host:port' \u2014 the string begins with U+200F RLM to keep the URI LTR inside RTL text.",
    "location": "src/ui/panel/dict.js:20 ('fragment.split', 'chain.uri.hint')",
    "problem": "The FA block is otherwise the strongest part of the i18n (idiomatic phrasing, Persian digits in err.port '\u06f1 \u062a\u0627 \u06f6\u06f5\u06f5\u06f3\u06f5', guillemets \u00ab\u0627\u0639\u0645\u0627\u0644 \u062a\u063a\u06cc\u06cc\u0631\u0627\u062a\u00bb, correct ZWNJ usage) \u2014 these two are its weak spots. The invisible RLM character works but is a copy-paste and diff hazard: anyone re-translating the key can silently drop it and flip the URI's rendering direction.",
    "recommendation": "Rewrite fragment.split as '\u0634\u06a9\u0633\u062a: \u06a9\u0645\u06cc\u0646\u0647\u2013\u0628\u06cc\u0634\u06cc\u0646\u0647' (mirroring fragment.length '\u0637\u0648\u0644 \u06a9\u0645\u06cc\u0646\u0647\u2013\u0628\u06cc\u0634\u06cc\u0646\u0647'). For chain.uri.hint, wrap the URI in an explicit LRM span via markup or a directional-isolate character (U+2066/U+2069) with a comment in the dict source noting it must not be stripped.",
    "severity": "low",
    "source": "src/ui/panel/dict.js",
    "title": "FA-specific copy defects: garbled fragment.split translation and an invisible RTL-mark character inside chain.uri.hint"
   },
   {
    "effort": "S",
    "evidence": "warp.js:92 `'<summary><span class=\"acct-name\">'+esc(a.name)+'</span> \u00b7 '+WARP_F.length+'</summary>'` \u2014 renders e.g. \"Phone \u00b7 17\" with no unit or label; the 17 is the number of download formats hidden inside the collapsed details.",
    "location": "src/ui/panel/warp.js:92",
    "problem": "The collapsed WARP account row on Home shows a naked integer after the separator. Nothing tells the reader it counts formats (and '17' changes if WARP_F grows), so it reads as noise or a version number. Every other count in the panel is labeled via a {n} dict key ('{n} endpoints', '{n} formats', '{n} accounts').",
    "recommendation": "Reuse home.chips.formats ('{n} formats') inside the summary, or drop the number entirely \u2014 the chevron already communicates that content is hidden.",
    "severity": "low",
    "source": "src/ui/panel/warp.js",
    "title": "Home WARP summary renders a bare meaningless count: 'AccountName \u00b7 17'"
   },
   {
    "effort": "S",
    "evidence": "settings.js:98 `field('address',t('addresses.field.address'),'1.2.3.4 / asdasd.workers.dev')` \u2014 'asdasd' is keyboard-mash filler, while the same file uses realistic placeholders elsewhere ('203.0.113.10', 'www.microsoft.com', '162.159.192.1:2408' in warp.js:87).",
    "location": "src/ui/panel/settings.js:98 (addrCardHtml address placeholder)",
    "problem": "Placeholder text is user-facing copy and the address field is the panel's most important input (it feeds every generated node). 'asdasd.workers.dev' looks like a typo left in testing and sets a sloppy tone in an otherwise meticulous UI; it also fails to model the real input shape (a bare workers.dev subdomain, which is what most users will type).",
    "recommendation": "Change to 'myworker.workers.dev' (or 'myworker.example.workers.dev') to match the doc tone used everywhere else.",
    "severity": "low",
    "source": "src/ui/panel/settings.js",
    "title": "Placeholder text 'asdasd.workers.dev' ships in the primary Addresses field"
   },
   {
    "effort": "S",
    "evidence": "users.js:28 load failure renders `t('users.load_failed')` PLUS `<button \u2026 data-action=\"users-reload\">'+esc(t('common.retry'))`. home.js:158 pool failure renders only `t('egress.pool.failed')` ('Could not load the pool.') with no recovery control. settings.js:225 aside, toastErr (home.js:188) falls back to `e.message||t('common.error')` where common.error is 'Something went wrong'.",
    "location": "src/ui/panel/home.js:158 vs users.js:28",
    "problem": "Two data-loading failures on the same view get different recovery affordances: the users card offers Retry (correct), the relay-pool card dead-ends with a sentence. 'Something went wrong' as the universal fallback is the least actionable string in the dict and pairs with dead toast.saveFailed ('Save failed') to suggest error copy was redesigned partway and never finished.",
    "recommendation": "Add the common.retry button to the pool failure path (the action handler 'pool-fetch' already exists), and delete toast.saveFailed with the dead-key sweep. For toastErr, prefer the specific api error code message over the generic fallback when present.",
    "severity": "low",
    "source": "src/ui/panel/home.js",
    "title": "Error-recovery affordances are inconsistent: users table gets a Retry button, relay pool errors dead-end; toast.saveFailed is dead while toastErr falls back to 'Something went wrong'"
   },
   {
    "effort": "S",
    "evidence": "Sampled keys judged: hints are consistently sentence-case and consequence-oriented \u2014 'security.allowlist.hint':'One IP or CIDR per line; empty allows all. A wrong entry locks the panel \u2014 recover by deleting the qproxy:settings KV key.' (recovery path included!), 'confirm.killswitch_body':'All clients lose connectivity immediately. The panel stays reachable so you can undo this.' (reversibility sta\u2026",
    "location": "src/ui/panel/dict.js:3 and :20 \u2014 sampled: common.apply/generated/fixErrors, security.allowlist.hint, users.limit_none/expiry_none, users.override.hint, country.hint, warp.detail.subs_desc, warp.amnezia.desc, fragment.preset.help, advanced.tg.hint, advanced.speedtest.help, egress.list/nat64 help, addresses.card.hint/help, remote.nodes.help, home.subs.quota, err.required/port/uuid/domain/duplicate/invalid_line, toast.networkError/settingsSaved/killOn/killOff/tooLong, users.toast.*, warp.toast.*, confirm.killswitch/reset/securepath/logout bodies, confirm.yes, users.bulk.done, home.subs.empty_*, warp.empty_*, users.empty, addresses.empty_hint, remote.nodes.empty, graph.empty, egress.pool.idle/empty, rotation.hint, backup.remind, totp.* flow, wizard.*",
    "problem": "The overall copy quality is well above the BPB/edgetunnel baseline \u2014 this reads like a product, not a script dump. The residual issues are distributional: error strings are the one category where 'what to DO next' is regularly missing (badfile, updateCheckFailed, egress.pool.failed), while hints and confirmations consistently nail it. The '(s)' construction and 'Yes, continue' vs bare 'Yes' show t\u2026",
    "recommendation": "Audit just the error-class keys with one rule: every error names the fix or offers the button that performs it. Replace 'Fix {count} error(s)' with '{count} fields need attention' (mirrors the FA which already avoids the hack).",
    "severity": "low",
    "source": "src/ui/panel/dict.js",
    "title": "Sampled-key verdict (\u224860 keys EN+FA): tone, case and consequence-discipline are genuinely good \u2014 the strengths worth protecting"
   }
  ],
  "removals": [
   "All 64 verified-dead dict keys, EN+FA: nav.warp, nav.checker, checker.* (20 keys), ports.* (23 keys), proxyip.* (9 keys), tabs.settings.ports, tabs.settings.proxyip, warp.settings, warp.view.accounts, warp.view.settings, toast.saveFailed, toast.langChanged, err.url, err.port, err.ip_or_host, err.ipv6_prefix",
   "Dead `case 'ports'` renderer in settings.js:222-226 (no field declares type 'ports') and the common.yes / common.no dict keys it alone references",
   "Stale checker-targets branch at settings.js:564 (`ta.id.indexOf('checker-targets')===0`) left over from the removed IP Checker view",
   "Duplicate dead twins of live copy: ports.cleanAddrs.short/help and ports.remoteSubs.label/hint (identical text to the live addresses.card.* / addresses.remoteSubs.* keys) \u2014 covered by the 64-key sweep but worth calling out as the rename leftover"
  ],
  "summary": "The dictionary is the strongest asset in this panel \u2014 layered short/help copy, confirmations that state consequences and reversibility honestly (\"Copy it now \u2014 it is shown only once\"), and genuinely idiomatic FA \u2014 but it is rotting at the edges: 64 of 756 keys (8.5%) are dead, including two entire removed features (IP Checker, CDN fronting) still translated in both languages. Four destructive operations skip the confirmation pattern the app itself established (settings import, WARP preset delete, webhook remove, bulk ops reuse a status chip as a dialog title), and a meaningful minority of visible copy bypasses the dict entirely \u2014 17 WARP format labels, shell chrome, Amnezia parameter labels \u2014 so FA users see untranslated English in exactly the flows they use most. Error copy is the weakest category: server messages surface as raw JSON-validator English (\"must be a boolean\"), the four client err.* keys intended for this are dead, and state-rule errors (\"Invalid UUID\") say what is wrong but never what to do."
 },
 {
  "consolidations": [
   "login.html renderSetup (login.html:172) and the panel force-change card (settings.js securityCardHtml:273) are two hand-rolled password-change forms with different rules, different meters, different strings \u2014 extract one shared component (fields + meter + rule copy) used by both, and one shared rule definition mirrored by the server.",
   "Empty-state pattern: .empty-card (app.css:224) already exists and WARP uses it well (warp.js:61); apply it to Users (settings table + home card) and future empty sections instead of bare 'field__hint' text rows.",
   "Sub-URL copy rows already share copyFieldHtml (home.js:58) across home, wizard, and WARP detail \u2014 extend it to also carry the format's client-app hint, so 'which URL for which app' is answered in one place.",
   "First-run messaging lives in three disconnected places (login boot-hint, force-change explainer, wizard) \u2014 consolidate into one onboarding narrative so the admin hears one story: deploy \u2192 claim/rotate passphrase \u2192 protocols on \u2192 import URL \u2192 optional WARP/Users."
  ],
  "dimension": "First-run & onboarding audit (login/setup card, forced-change screen, wizard, home empty states)",
  "findings": [
   {
    "effort": "S",
    "evidence": "The setup card (renderSetup) is rendered ONLY from the login error path: `showError('e-pw',err.code==='SETUP_REQUIRED'?'':t('login.error.wrong'));pw.focus();pw.select();if(err.code==='SETUP_REQUIRED'){renderSetup();return}` \u2014 triggered by the server's `return jsonError(409, \"SETUP_REQUIRED\", \"admin password is not configured yet\")` (auth.ts:201). Meanwhile the login card's only first-run hint read\u2026",
    "location": "src/ui/login.html:167 (renderLogin) + login.html:128 (dict) + src/handlers/api/auth.ts:201",
    "problem": "A fresh manual deployer sees a 'Sign in' form whose hint is false for their path, must guess that submitting a throwaway passphrase is the unlock gesture, and never learns the 24h setup window (SETUP_WINDOW_MS) they are racing against. This is the single worst first-run moment and it is pure UI: the server flow is fine.",
    "recommendation": "Render the setup card directly when no password exists: expose hasPassword on an unauthenticated probe (e.g. a tiny GET on the setup route or health payload) and branch in the login boot IIFE; rewrite the hint to cover both paths ('deploy script output' vs 'new panel: set your first passphrase now').",
    "severity": "high",
    "source": "src/ui/login.html",
    "title": "Fresh manual deploys: setup card is only reachable by failing a login, and the login hint is wrong for that path"
   },
   {
    "effort": "S",
    "evidence": "handleSetup sets `passwordIsBootstrap: true` (auth.ts:282) \u2014 same flag the deploy script's injected `qproxy-XXXXXXXX` password gets. login.html then calls `goPanel()`; panel boot hits `if(forceFlagged())showForceChange(); if(S.forceChange)return;` (actions.js:531-532) where `forceFlagged(){if(S.set&&S.set.passwordIsBootstrap===true)return true;...}`. The overlay's copy: `'onboarding.forcechange.ex\u2026",
    "location": "src/handlers/api/auth.ts:282 + src/ui/panel/actions.js:293-295,531 + src/ui/panel/settings.js:273 (securityCardHtml reused as the forced overlay)",
    "problem": "Two consecutive password screens with different rules and contradictory copy, for a password the human personally chose. The forced-change is justified only for script-injected passwords that were printed to a terminal once; for self-claimed setup it is wasted friction and makes the explainer a lie.",
    "recommendation": "Have the login-page setup card claim the panel with a marker (e.g. body flag or a dedicated claim endpoint variant) that leaves passwordIsBootstrap false; reserve the forced-change for the deploy-script path where the password was machine-generated.",
    "severity": "high",
    "source": "src/handlers/api/auth.ts",
    "title": "Self-service setup card marks the human-chosen passphrase as bootstrap, forcing an immediate SECOND password change"
   },
   {
    "effort": "M",
    "evidence": "shell.html nav is exactly two tabs: `<a class=\"tab\" id=\"tab-home\" href=\"#/home\">` and `<a class=\"tab\" id=\"tab-settings\" href=\"#/settings/general\">`. dict.js still carries `'nav.warp':'WARP','nav.checker':'IP Checker'` (dict.js:3) \u2014 rendered nowhere; leftover from a flatter IA. WARP sits last in the 10-item SECTIONS list, and on Home it appears only when accounts already exist: `if(S.warp&&S.warp.a\u2026",
    "location": "src/ui/panel/shell.html:41-44 + src/ui/panel/settings.js:30 (SECTIONS order, warp last) + src/ui/panel/dict.js:3,20 + src/ui/panel/home.js:73",
    "problem": "Pain point 1 confirmed. First-run admins cannot discover WARP exists; the only signpost is one wizard sentence they may have already skipped. Meanwhile dead nav keys prove WARP was once top-level and got demoted.",
    "recommendation": "Promote WARP to a third top-level tab (parseRoute already special-cases #/settings/warp/:id, so the route model exists), or at minimum add a Home card with a 'Generate your first WARP account' CTA mirroring the subscriptions empty-card.",
    "severity": "high",
    "source": "src/ui/panel/shell.html",
    "title": "Owner pain 1 confirmed: WARP is buried at the end of Settings and has zero first-run presence outside a wizard sentence"
   },
   {
    "effort": "M",
    "evidence": "The empty state is gated on `if(!urls.length&&!(S.warp&&S.warp.accounts.length))` (home.js:100) with CTA 'Enable a protocol'. But urls come from `S.subs=(d.subUrls&&d.subUrls.urls)||[]` and buildSubUrls unconditionally returns seven entries (`return [{format:'base64',label:'Base64/Mixed',url:base}, ... 'Panel info']`) regardless of vlessEnabled/vmessEnabled/trojanEnabled/ssEnabled. So the empty-ca\u2026",
    "location": "src/ui/panel/home.js:100 vs src/handlers/api/status.ts buildSubUrls",
    "problem": "The panel's one purpose-built first-run warning is unreachable, and the state it was built for (nothing to serve) is presented as a fully working subscription list. A fresh admin who disables all protocols gets no error anywhere \u2014 the client just imports an empty profile.",
    "recommendation": "Gate the card on computed node capability (protoCount===0, same computation the chip already does) and render a warning variant of the subs card (URLs shown but marked 'returns no nodes') instead of the dead !urls.length branch.",
    "severity": "medium",
    "source": "src/handlers/api/status.ts",
    "title": "The home 'No subscriptions yet' empty state is dead code \u2014 URLs always render, even when they will download zero nodes"
   },
   {
    "effort": "S",
    "evidence": "Settings \u2192 Users empty state is one dim cell: `tb.innerHTML=S.users.length?...:'<tr><td colspan=\"6\"><span class=\"field__hint\">'+esc(t('users.empty'))+'</span></td></tr>'` with `users.empty:'No users yet'` \u2014 no CTA in the row, no explanation of what a user link is. Home's users card is worse for day zero: `renderHomeUsers` renders a stat grid `'<span class=\"lbl\">Users</span><span class=\"mono\">0</sp\u2026",
    "location": "src/ui/panel/users.js:29 + src/ui/panel/home.js:121,171-175",
    "problem": "Pain point 3 confirmed in both locations. Zero-user state communicates nothing about the feature's value (per-user scoped links with filters/limits/expiry) and has no conversion path, while the same codebase proves it knows how to build a proper empty card for WARP.",
    "recommendation": "Reuse the WARP empty-card pattern for the users table ('No users yet \u2014 create a scoped link for a friend or device' + Add User CTA), and hide the home zeros grid in favor of a one-line invite with a Manage/Add link until users exist.",
    "severity": "medium",
    "source": "src/ui/panel/users.js",
    "title": "Owner pain 3 confirmed: Users first-run is a bare 'No users yet' table cell plus a meaningless 0/0 stat grid on Home"
   },
   {
    "effort": "S",
    "evidence": "Step 0's CTA is `<a class=\"btn btn--primary btn--sm\" href=\"#/settings/protocols\" onclick=\"document.getElementById('wiz-skip').click()\">Open Protocols</a>` and wiz-skip does `localStorage.setItem('qp_wizard_done','1')` (actions.js:545,549). So the one action the wizard encourages permanently deletes steps 2-3 (show the base64 URL, 'You are set'). The Escape handler (actions.js:443-451) covers m-con\u2026",
    "location": "src/ui/panel/actions.js:538-551 + actions.js:443-451 + src/ui/panel/dict.js:3 (wizard.s1_title dead)",
    "problem": "The onboarding funnel's primary button destroys the rest of the funnel, the modal ignores both Escape convention and the codebase's own modal-close pattern, and nothing can ever bring the tour back.",
    "recommendation": "CTA should navigate WITHOUT setting qp_wizard_done (resume at step 2 next boot), add m-wizard to the Escape chain, and add a 'Replay setup tour' item in the shortcuts/help modal.",
    "severity": "medium",
    "source": "src/ui/panel/actions.js",
    "title": "The wizard's own 'Open Protocols' CTA dismisses the wizard forever \u2014 the funnel dies at exactly the step it advertises"
   },
   {
    "effort": "M",
    "evidence": "Setup card: `if(np.value.length<8){...} if(!(/[A-Z]/.test(np.value)&&/\\d/.test(np.value))){...}` with `setup.rule:'At least 8 characters, one uppercase letter and one digit.'`. Forced-change card: `if(nw.value.length<8){setFw('fw-sec-new',t('security.rule'));...}` with `security.rule:'Use at least 8 characters.'` and no strength meter. Server: both handleSetup (auth.ts:277-279) and handlePasswordC\u2026",
    "location": "src/ui/login.html:172 (renderSetup rule) vs src/ui/panel/actions.js:207-231 (change-password) vs src/handlers/api/auth.ts:286,312",
    "problem": "The same admin, minutes apart, is told two different password policies; whichever screen they learned from, the other contradicts it, and the strictest advertised rule is not actually enforced where it matters.",
    "recommendation": "Pick one policy, state it identically in both UIs, and enforce it once server-side (or honestly downgrade the setup-card copy to length-only if uppercase/digit stays client-only).",
    "severity": "medium",
    "source": "src/handlers/api/auth.ts",
    "title": "Two first-run password screens present two different password policies, and neither is fully enforced server-side"
   },
   {
    "effort": "S",
    "evidence": "Catch-all in renderSetup: `showError('e-np',err.status===0?t('toast.networkError'):t('setup.rule'))` \u2014 a 500 or KV failure displays 'At least 8 characters, one uppercase letter and one digit.' to someone whose 16-char passphrase was just rejected for infrastructure reasons. The expired branch replaces the whole card with only text: `t('onboarding.setup.expired')` = 'This setup window has expired \u2014\u2026",
    "location": "src/ui/login.html:172 (renderSetup catch + success toast) + src/handlers/api/auth.ts:274-275",
    "problem": "Error states blame the user's password for server problems, the dead-end offers one word of recovery ('redeploy') with no pointer, and the success toast reads like a bug.",
    "recommendation": "Map errors by code (dedicated strings for expiry/network/validation), link docs/DEPLOYMENT on expiry, and add a proper setup.success string for the toast.",
    "severity": "low",
    "source": "src/ui/login.html",
    "title": "Setup-card error handling: any server failure shows the password rule, expiry is a dead end with no docs pointer, and the success toast reuses the form title"
   },
   {
    "effort": "S",
    "evidence": "maybeBackupBanner: `let last=0... if(last&&now-last<30*864e5){box.hidden=true;return}` \u2014 on first ever login last=0 so the banner renders with `backup.remind:'No settings backup in over 30 days. Export a copy so you can recover.'` on a panel whose settings were seeded seconds ago.",
    "location": "src/ui/panel/chrome.js:27-40",
    "problem": "The very first banner a new admin sees states a falsehood about their panel's age, teaching them the panel's warnings can be ignored.",
    "recommendation": "Suppress until the panel is older than the threshold (compare against a stored first-run timestamp or seed date from status), or reword to 'You have no settings backup yet.'",
    "severity": "low",
    "source": "src/ui/panel/chrome.js",
    "title": "Day-zero backup banner says the panel hasn't been backed up 'in over 30 days' 30 seconds after creation"
   },
   {
    "effort": "S",
    "evidence": "IP card ships idle: `'home.stats.idle':'Hit Refresh to fetch IP info.'` and loadMyIp is wired only to the refresh button (`'refresh-ip'(){loadMyIp()},` actions.js:74) \u2014 the most useful trust-building data (egress IP, colo, ASN) requires a click nobody knows to make. The status card renders `'Requests today'` followed by two bare `<span class=\"mono\">` cells; `home.status.total:'Requests total'` exi\u2026",
    "location": "src/ui/panel/home.js:109,174-175 + src/handlers/api/status.ts buildSubUrls + src/ui/panel/dict.js (home.status.total, home.subs.mode.fragment)",
    "problem": "Two of five right-column cards are manual-prompt placeholders on first load, one stat is unlabeled, and the one genuinely powerful URL (fragment mode) is unexplained \u2014 the home page asks the admin to click things without telling them why.",
    "recommendation": "Auto-load my-ip once on first home render (it is one authenticated fetch); add the missing total label; put a one-line hint under the mode seg; visually separate the Panel info row.",
    "severity": "low",
    "source": "src/ui/panel/home.js",
    "title": "First-run Home renders two dead 'press the button' cards and an unlabeled requests-total number"
   },
   {
    "effort": "S",
    "evidence": "`const getLang=()=>{const m=document.cookie.match(/(?:^|;\\s*)qp_lang=(en|fa)/);return m&&m[1]||'fa'}` in login.html; head.js uses the identical fallback (`const l=m&&m[1]||'fa'`); DEFAULT_SETTINGS.language='fa' (settings.ts:168). No navigator.language sniff exists anywhere in the boot path. An English deployer's first view is a Persian RTL login card until they find the seg below the card. (Caveat\u2026",
    "location": "src/ui/login.html:127,169 (getLang fallback 'fa', html lang=fa) + src/ui/panel/head.js:1 + src/types/settings.ts:168",
    "problem": "First impression defaults to the minority language for non-Persian admins; language selection is reactive, not detected.",
    "recommendation": "When no qp_lang cookie exists, initialize from navigator.language (fa for fa-*), then let settings.language seed the cookie as it already does.",
    "severity": "low",
    "source": "src/ui/login.html",
    "title": "Every fresh visitor's first paint is Persian \u2014 default language is fa with no browser-locale detection"
   },
   {
    "effort": "S",
    "evidence": "The literal path is: deploy scripts print panel/login/sub URLs and the generated password (deploy-direct.mjs:567-581) \u2192 login page (good a11y: role=alert errors, focus management, reveal buttons, TOTP with recovery codes and a pre-auth cookie in auth.ts) \u2192 forced change with a clear explainer ('still using the temporary password from deployment') \u2192 boot() re-runs \u2192 wizard opens (skippable, 2 steps\u2026",
    "location": "src/ui/panel/actions.js:515-537 (boot\u2192forceChange\u2192wizard chain) + src/ui/panel/home.js:58-100 + src/settings/seed.ts (fillIdentity) + scripts/deploy-direct.mjs:567-581",
    "problem": "None material on the scripted path beyond the items above; this is the baseline the forked paths (manual deploy, lost output) fail to match.",
    "recommendation": "Preserve this chain while fixing findings 1, 2 and 6; do not regress the wizard or the forced rotation.",
    "severity": "low",
    "source": "scripts/deploy-direct.mjs",
    "title": "The happy path itself is solid: scripted deploy \u2192 login \u2192 forced rotation \u2192 wizard \u2192 working client config in under a minute"
   }
  ],
  "removals": [
   "Dead i18n keys with no consumer in any panel JS (verified by grep): 'nav.warp', 'nav.checker' (dict.js:3,20), 'tabs.settings.ports', 'tabs.settings.proxyip', the whole checker.* block (checker.title/desc/targets/run/stop/running/results/empty/unsupported_port/col.*/status.*/summary/tooMany), 'wizard.s1_title', 'home.status.total'. Delete or wire them up (checker looks like a planned top-level IP Checker view that was cut).",
   "Dead empty-state branch: home.js:100 `if(!urls.length&&...)` can never fire because buildSubUrls always returns 7 entries \u2014 replace with the capability-based warning (finding 4) rather than leaving unreachable code that suggests the state is handled."
  ],
  "summary": "The scripted happy path (deploy \u2192 printed password \u2192 forced rotation \u2192 wizard \u2192 copy Base64 URL) is genuinely better than BPB/edgetunnel's non-existent onboarding, but every fork off it punishes the admin: manual dashboard deploys meet a \"Sign in\" form whose only hint is false for that path, a setup card reachable only by failing a login, and then a forced SECOND passphrase change for the password they chose seconds ago. WARP \u2014 the product's richest surface \u2014 has zero first-run presence beyond one wizard sentence that itself admits it lives in Settings, and Users gets a bare \"No users yet\" table cell plus a 0/0 stat grid where WARP gets a proper empty card. The home subscription card largely refutes pain point 2 (links are prominent, copy+QR per format), but its \"No subscriptions yet\" empty state is dead code and nothing warns that a zero-protocol panel still serves confident-looking URLs that download nothing."
 },
 {
  "consolidations": [
   "Findings 1 and 6 are the same root cause \u2014 custom radiogroup-like button clusters with no APG keyboard behavior: one shared 'segmented/radio cluster keyboard controller' (arrow keys, roving tabindex, RTL flip) wired to chips (settings.js:218), fragment presets (settings.js:237), home sub-mode (home.js:93), and the language segment (home.js:54) would close both.",
   "Findings 3 and 4 are the same generator bug class: field-rendering helpers emitting sibling <label> without for/id \u2014 fix addrCardHtml (settings.js:141), remoteNodeCardHtml (settings.js:164), and the three override labels in shell.html:77 in one pass.",
   "Findings 2 and 12 are both 'async state changes are silent to AT': one helper that writes an error/state message into an element AND toggles a polite/assertive live region (or reuses toast) applied to modal errors, inline field errors, the apply bar, and the subtab dirty asterisk closes both."
  ],
  "dimension": "Accessibility (WCAG 2.2 AA) \u2014 Q Proxy admin panel UI",
  "findings": [
   {
    "effort": "M",
    "evidence": "actions.js:468 `catch(err){if(err&&err.fields&&err.fields.config){$('wi-error').textContent=err.fields.config;$('wi-error').style.display='block'}else toastErr(err)}` \u2014 same pattern at actions.js:479 (wp-error) and actions.js:495 (mu-error). The error paragraphs in shell.html:75\u201377 (`<p class=\"field__error\" id=\"wi-error\">`, `id=\"wp-error\"`, `id=\"mu-error\" style=\"display:none\"`) carry no role, no a\u2026",
    "location": "src/ui/panel/actions.js:468,479,495; src/ui/panel/shell.html:75-77",
    "problem": "WCAG 3.3.1 (Error Identification) and 4.1.3 (Status Messages) fail. Validation failure in the WARP import/preset and user-creation modals is communicated exclusively through a visually-displayed paragraph. There is no aria-live region, no focus move to the invalid field, and the toast fallback is suppressed precisely when a field error exists.",
    "recommendation": "Give the three modal error <p>s `role=\"alert\"` (or aria-live=\"assertive\"), set `aria-invalid=\"true\"` + `aria-describedby` on the offending input, and move focus to the first invalid field after a failed submit. Cheapest correct fix: also call toast(t('common.fixErrors'),'err') in the fields-error branch so the existing alert path fires.",
    "severity": "critical",
    "source": "src/ui/panel/actions.js",
    "title": "Modal form validation errors are silent to screen readers (no live region, no focus move, toast suppressed)"
   },
   {
    "effort": "M",
    "evidence": "settings.js:218-220 `case 'chips':{ ... '<div role=\"radiogroup\" class=\"chip-row\" ... >'+f.opts.map(([v,k])=>'<button type=\"button\" role=\"radio\" class=\"chip\" data-chip=... aria-checked=...` \u2014 these are real <button>s, so all are in the Tab order and Enter/Space work, but no keydown handler exists for them. handleChip (actions.js:355-363) is click-only. Same pattern for fragment presets (settings.js\u2026",
    "location": "src/ui/panel/settings.js:218-221,237-247; src/ui/panel/actions.js:355-363; src/ui/panel/home.js:93-95",
    "problem": "WCAG 2.1.1 (Keyboard) \u2014 the ARIA radiogroup pattern requires arrow-key movement between radios with roving tabindex (APG: 'Radio Group Example'). Keyboard-only users can still reach each chip via Tab (buttons), so it is not a total block, but the announced semantics (radio) promise arrow behavior that does not exist, and Tab-order-per-chip is exactly what the pattern forbids.",
    "recommendation": "Extract a generic keydown handler (copy wireTabKeys) for `[role=radiogroup]` containers: Arrow Left/Right (flipped for RTL) moves focus+selection, Home/End jump, non-selected chips get tabindex=-1. One helper covers chips, fpreset, and the home mode segment.",
    "severity": "high",
    "source": "src/ui/panel/settings.js",
    "title": "ARIA radiogroups (egress mode, fragment presets, home sub-mode, language segment) implement no radio keyboard pattern"
   },
   {
    "effort": "S",
    "evidence": "settings.js:141 `return '<div class=\"addr-card__field\"><label>'+esc(label)+'</label><input class=\"input input--mono\" type=\"text\" data-addr-field=\"'+k+'\" ...>'` \u2014 label and input are siblings with no `for` and no `id`. Duplicated at settings.js:164 for remote nodes (7\u201311 fields per card: address, port, sni, uuid, pbk, sid, password...). shell.html:77: `<label id=\"mu-ov-address-label\"></label><input\u2026",
    "location": "src/ui/panel/settings.js:141,164; src/ui/panel/shell.html:77",
    "problem": "WCAG 1.3.1 / 4.1.2 \u2014 the inputs' accessible names are empty; AT users hear 'edit text, blank' seven times per address card and must guess which is SNI vs port vs label. This is the panel's most data-dense form (Addresses, remote nodes, user override) and every field in it is unnamed.",
    "recommendation": "Generate `id=\"af-'+i+'-'+k+'\"` and `for=\"...\"` inside the two `field()` helpers (one-line change each), and add `for=\"mu-ov-address\"` / `for=\"mu-ov-port\"` / `for=\"mu-ov-label2\"` in shell.html.",
    "severity": "high",
    "source": "src/ui/panel/settings.js",
    "title": "Every address-card and remote-node input renders an unassociated <label> (accessible name missing)"
   },
   {
    "effort": "S",
    "evidence": "app.css:5-6 remaps --bg, --surface*, --border*, --text*, --accent-ink, --cyan* for `html[data-theme=\"light\"]` but --success:#34d399, --warning:#fbbf24, --danger:#f87171, --danger-strong:#ef4444 are only defined in :root (app.css:1) and never remapped. Computed: .field__error text #f87171 on #f8fafc \u2248 2.6:1 (app.css:91); .chip-status.ok text #34d399 on --success-bg \u2248 1.9:1 (app.css:210); --text-gho\u2026",
    "location": "src/ui/panel/app.css:1,5-6,91,173,203,210",
    "problem": "WCAG 1.4.3 fails for every error message, success chip (kill-switch status, user status), and micro-label in light theme; 1.4.11 fails for the help icon. Dark theme passes; light theme systematically fails \u2014 and light is a first-class option (theme toggle in topbar, shell.html:54).",
    "recommendation": "Inside the `html[data-theme=\"light\"]` block add: `--danger:#b91c1c;--danger-strong:#dc2626;--success:#047857;--warning:#92400e;--text-ghost:#64748b;`. Zero markup changes.",
    "severity": "high",
    "source": "src/ui/panel/app.css",
    "title": "Light theme fails contrast for errors, success/warning text, and table/field micro-labels"
   },
   {
    "effort": "S",
    "evidence": "shell.html:19 `<a class=\"visually-hidden skip-link\" href=\"#main\">Skip to content</a>`, shell.html:82 `<div class=\"toasts\" id=\"toasts\" role=\"status\" aria-live=\"polite\">` (lib.js:6 adds `role=\"alert\"` to error toasts), app.css:25 global `:focus-visible{outline:2px solid rgba(var(--accent-rgb),.7)}` with per-component switch/port-cell focus outlines (app.css:141,194), Escape closes every modal and cl\u2026",
    "location": "src/ui/panel/shell.html:19,82; src/ui/panel/lib.js:6,44; src/ui/panel/head.js:1; src/ui/panel/app.css:25,301",
    "problem": "Baseline is strong; the gaps below are specific and fixable. Listed so the auditor-of-record does not re-litigate the foundation.",
    "recommendation": "None \u2014 preserve these behaviors in refactors; they are the parts of the panel that already meet AA.",
    "severity": "low",
    "source": "src/ui/panel/shell.html",
    "title": "Positive baseline worth preserving: skip link, toast live regions, :focus-visible ring, Escape+focus-return, correct lang/dir pre-paint, reduced-motion"
   },
   {
    "effort": "M",
    "evidence": "settings.js:510-519 sets `el.setAttribute('aria-invalid','true'); const errId='err-'+(++UID); err.id=errId; el.setAttribute('aria-describedby',errId)` \u2014 a good association, but the error <p> (app.css:91 `.field__error{display:none}` \u2192 shown via `.field--error`) is an ordinary paragraph: no role=alert, no aria-live. applySection's catch (settings.js:660-667) loops `showFieldError(path,...)` then `t\u2026",
    "location": "src/ui/panel/settings.js:510-519,660-667,589-599",
    "problem": "WCAG 3.3.1/4.1.3 partial failure: the user is told 'N errors exist' but not what or where, and must Tab-hunt. blur-validation errors are entirely silent to AT (aria-describedby only helps once the user re-focuses the field).",
    "recommendation": "After the showFieldError loop, `document.querySelector('#settings-panels .field--error [data-bind], #settings-panels [aria-invalid=\"true\"]')` \u2192 focus(). Add `aria-live=\"polite\"` to a shared wrapper or set role=alert on the .field__error <p>s generated in fieldWrap (settings.js:129-137).",
    "severity": "high",
    "source": "src/ui/panel/settings.js",
    "title": "Inline settings-panel errors associate but never announce: no live region and no focus move on failed Apply"
   },
   {
    "effort": "M",
    "evidence": "buildShell (home.js:77-84) creates 10 subtabs and navigate()/showSection set aria-selected/aria-controls correctly (home.js:69,74; settings.js:307 `p.role='tabpanel';p.setAttribute('aria-labelledby','st-'+s.key)`), but no code ever sets `tabindex=-1` on unselected tabs \u2014 every tab is a native button/link and remains in the tab sequence. shell.html:43-44 topbar tabs likewise. Additionally wireTabKe\u2026",
    "location": "src/ui/panel/actions.js:456-457,497-505; src/ui/panel/home.js:74; src/ui/panel/shell.html:43-44",
    "problem": "ARIA APG tabs pattern violation (roving tabindex): keyboard users must Tab through up to 12 tab stops before reaching panel content, and the skip link only bypasses the topbar. RTL arrow semantics contradict the APG ('arrow keys follow visual order'). 2.4.3 Focus Order is arguable; this is primarily a conformance-to-pattern and ergonomics failure.",
    "recommendation": "In navigate()/showSection, set `tabindex=\"0\"` on the selected tab and `tabindex=\"-1\"` on the rest; in wireTabKeys, invert Left/Right when `document.documentElement.dir==='rtl'`.",
    "severity": "medium",
    "source": "src/ui/panel/actions.js",
    "title": "Tablists lack roving tabindex: 12 tab stops (2 top tabs + 10 subtabs) pollute the tab order; arrows ignore RTL"
   },
   {
    "effort": "S",
    "evidence": "shell.html:53 `<div class=\"seg\" role=\"group\" aria-label=\"Language\" id=\"langseg\">` \u2014 children are created with `b.setAttribute('role','radio')` at home.js:54. ARIA requires role=radio to be owned by role=radiogroup. The hardcoded aria-labels are also never re-set by buildShell (home.js:33-78 updates nav, swatches, logout, shortcuts \u2014 not #langseg or the subtabs group), so in FA the group is announc\u2026",
    "location": "src/ui/panel/shell.html:53,67; src/ui/panel/home.js:54",
    "problem": "4.1.2 Name/Role/Value: broken container/child relationship for role=radio; English names inside a Persian RTL document (3.1.2 Language of Parts adjacent). shell.html:67 `<div class=\"subtabs\" role=\"tablist\" aria-label=\"Settings sections\">` is likewise never localized.",
    "recommendation": "Change shell.html:53 to `role=\"radiogroup\"`; in buildShell set `$('langseg').setAttribute('aria-label',t('lang.label'))` and localize the subtabs aria-label (add dict keys).",
    "severity": "medium",
    "source": "src/ui/panel/shell.html",
    "title": "role=group container holds role=radio children (language segment) and keeps hardcoded English aria-labels in FA"
   },
   {
    "effort": "M",
    "evidence": "settings.js:127-128: `'<span class=\"help-wrap\"><button type=\"button\" class=\"help-trigger\" aria-label=\"'+esc(t('common.help'))+'\"><svg aria-hidden=\"true\">...</button><span class=\"help-pop\" role=\"tooltip\">'+esc(t(key))+'</span></span>'` \u2014 the pop has no id and the button no aria-describedby, so the tooltip content is visually revealed (app.css:101 `.help-wrap:focus-within .help-pop`) but never expos\u2026",
    "location": "src/ui/panel/settings.js:127-128; src/ui/panel/app.css:99-101",
    "problem": "4.1.2 (description not programmatically tied to the control) and a degraded 1.4.13 (dismissable) story. The help content exists precisely for fields users get wrong (ECH, egress lists, fragment) and is inaccessible to the population most likely to need it.",
    "recommendation": "Give the pop `id=\"hp-'+(++UID)+'\"` and the button `aria-describedby=\"hp-...\" aria-label=\"'+esc(t(label))+' \u2014 '+t('common.help')+'\"`; content is then announced on focus without extra JS.",
    "severity": "medium",
    "source": "src/ui/panel/settings.js",
    "title": "Field help tooltips are unreachable for screen readers and unlabeled per-field"
   },
   {
    "effort": "S",
    "evidence": "settings.js:155 `'<span class=\"addr-dot\" data-addr-dot=\"...\" hidden></span>'` with no aria-label; warp.js:46 sets only `dot.className='addr-dot '+(r.status==='ok'?'dot--ok':'dot--bad');dot.title=...` \u2014 state is color (app.css:179-180 .addr-dot.dot--ok green / .dot--bad red) plus a title tooltip that keyboard and touch users never see. The probe button ('addresses.test', settings.js:260) triggers t\u2026",
    "location": "src/ui/panel/settings.js:155; src/ui/panel/warp.js:35-47,33; src/ui/panel/app.css:179-180",
    "problem": "WCAG 1.4.1 (Use of Color): ok/bad is conveyed only by hue for the address health dots; the title attribute is not a text alternative for AT (inconsistently announced) nor for keyboard/touch. Probe results are invisible to a color-blind or screen-reader admin.",
    "recommendation": "Set `dot.setAttribute('role','img'); dot.setAttribute('aria-label', ok?t('addresses.probe_ok',{ms}):t('addresses.probe_bad'))` in renderAddrDots, and pair each dot with a visually-hidden \u2713/\u2717 (or visible glyph like the pool rows).",
    "severity": "medium",
    "source": "src/ui/panel/warp.js",
    "title": "Address probe status is color-only (green/red dot with title-only tooltip)"
   },
   {
    "effort": "S",
    "evidence": "app.css:53 `.swatch{width:18px;height:18px;...}` \u2014 the four accent buttons are 18\u00d718 (shell.html:48-51); app.css:99 `.help-trigger{...width:14px;height:14px...}` on desktop (only raised to 44px under `@media(pointer:coarse)`, app.css:302 \u2014 2.5.8 applies to mouse pointers too). The port 'select all' master checkbox is `style=\"position:absolute;opacity:0;width:1px;height:1px\"` with a title-only labe\u2026",
    "location": "src/ui/panel/app.css:53,99,302; src/ui/panel/settings.js:225; src/ui/panel/shell.html:48-51",
    "problem": "WCAG 2.5.8 (Target Size Minimum, 24px) fails for swatches and the desktop help trigger; the invisible 1px master checkbox is a precision target with a hover-only name (its aria-label saves 4.1.2 but not usability).",
    "recommendation": "Bump swatches to 24px (or add a transparent hit-area via padding + background-clip) and the help-trigger to 24px on desktop; give the master checkbox a visible 28px label button like the other icon buttons.",
    "severity": "medium",
    "source": "src/ui/panel/app.css",
    "title": "Sub-24px targets: accent swatches (18px), help triggers (14px desktop), invisible port-master checkbox"
   },
   {
    "effort": "S",
    "evidence": "shell.html:71 `<div class=\"applybar\" id=\"applybar\" hidden>...<span id=\"dirty-label\"></span>` \u2014 shown/hidden by updateApplyBar (settings.js:631) with no role or aria-live, so the appearance is never announced. settings.js:634 appends a literal `'*'` to subtab text (`a.textContent=base+(S.dirty.has(a.dataset.sec)?' *':'')`) and toggles `.section-actions__hint` (settings.js:632-633) \u2014 all visual-only\u2026",
    "location": "src/ui/panel/settings.js:631-635; src/ui/panel/shell.html:71",
    "problem": "WCAG 4.1.3: unsaved-changes state \u2014 the single most consequential state in a settings-heavy panel \u2014 is conveyed visually only. SR users get no cue that Apply is pending (they do get the beforeunload guard, but not in-page feedback).",
    "recommendation": "Add `role=\"status\"` to #applybar (its text is set when shown, so insertion announces), and replace the appended `'*'` with a visually-hidden `<span aria-hidden=\"true\">*</span><span class=\"visually-hidden\">(unsaved)</span>` or an `aria-label` update on the subtab.",
    "severity": "medium",
    "source": "src/ui/panel/settings.js",
    "title": "Dirty/unsaved state (apply bar, subtab asterisk, per-section hint) has no accessible announcement"
   },
   {
    "effort": "S",
    "evidence": "app.css:1 dark tokens: `--border:rgba(255,255,255,.06);--border-strong:rgba(255,255,255,.10)` against --bg:#05080f. Composite of .input borders \u2248 #232933 vs #05080f \u2248 1.3:1. Applied to .input/.select/.textarea (app.css:106), .btn--ghost (app.css:122), .chip (app.css:186), .switch__track (app.css:136). The focus ring (accent @ .7) is fine; the resting affordance is not.",
    "location": "src/ui/panel/app.css:1,106,122,136,186",
    "problem": "WCAG 1.4.11 (Non-text Contrast, 3:1) fails for the primary affordance boundary of every text field, select, ghost button, chip, and off-switch in dark theme \u2014 including users who cannot perceive the faint surface tint.",
    "recommendation": "Raise --border-strong to `rgba(255,255,255,.22)` (\u22483:1 composite on #05080f) or give inputs a 1px inner border + surface-2 fill with \u22653:1 against bg. Light theme borders (.07 black on white \u2248 2.6:1) need the same nudge to .11\u2192.16.",
    "severity": "medium",
    "source": "src/ui/panel/app.css",
    "title": "Input/button borders in dark theme are ~1.3:1 against the background (non-text contrast)"
   },
   {
    "effort": "S",
    "evidence": "lib.js:45 `modal.querySelectorAll('button,input,[href],[tabindex]:not([tabindex=\"-1\"])')` \u2014 `textarea` and `select` are missing. m-warp-import (wi-config textarea, shell.html:75) and m-warp-preset (wp-endpoints textarea, shell.html:76) only work because their first focusable happens to be the name input; any future modal whose first focusable is a textarea would be skipped by openModal (lib.js:43 \u2026",
    "location": "src/ui/panel/lib.js:43,45; src/ui/panel/actions.js:439-441",
    "problem": "Latent 2.4.3/2.1.2 hazard rather than a live failure: today's eight modals dodge it by DOM luck. confirmDialog also lacks aria-describedby on the dialog (shell.html:73 has only aria-labelledby), so the body text is not announced with the dialog name.",
    "recommendation": "Change both selectors to `'button,input,select,textarea,[href],[tabindex]:not([tabindex=\"-1\"])'`; add `aria-describedby=\"cf-body\"` to the m-confirm dialog panel.",
    "severity": "low",
    "source": "src/ui/panel/lib.js",
    "title": "Modal focus trap selector omits textarea and select elements (works today by DOM-order luck)"
   },
   {
    "effort": "S",
    "evidence": "chrome.js:13-23: `'<svg viewBox=\"0 0 '+w+' '+ht+'\" role=\"img\" aria-label=\"'+esc(t('graph.title'))+'\">'` \u2014 30 days of request data reduced to a static title; no table/text alternative. shell.html:72: `<canvas id=\"qr-canvas\" width=\"560\" height=\"560\" ...>` with no role/aria-label (the URL is duplicated as text in #qr-caption, which mitigates). shell.html:80 wizard body renders `onclick=\"document.getE\u2026",
    "location": "src/ui/panel/chrome.js:13-23; src/ui/panel/shell.html:72",
    "problem": "1.1.1 partial: chart consumers get only the title '30-day requests' (graph.title) with no data; the canvas is an unnamed image object. Severity bounded because the subscription URL itself is available as text right below the canvas.",
    "recommendation": "Canvas: `role=\"img\" aria-label=t('qr.alt')`. Chart: keep role=img with a meaningful label ('requests today, last 30 days, peak N') or add a visually-hidden 30-row summary/last-7-days text.",
    "severity": "low",
    "source": "src/ui/panel/chrome.js",
    "title": "QR canvas and traffic chart lack accessible names for their content"
   },
   {
    "effort": "S",
    "evidence": "home.js:78 `document.title='Q Proxy'` \u2014 navigate() (home.js:63-71) switches views/subsections without touching the title. shell.html:83 `<noscript><p style=\"padding:24px;text-align:center\">JavaScript is required.</p></noscript>` \u2014 not routed through dict.js (the rest of the panel is fully bilingual). chrome.js:79 `document.querySelector('#settings-search')` targets an element that exists nowhere i\u2026",
    "location": "src/ui/panel/home.js:78; src/ui/panel/shell.html:83; src/ui/panel/chrome.js:79",
    "problem": "2.4.2 (Page Titled) weakened for SPA views \u2014 SR users' rotor/history lists show identical entries for Home and ten Settings sections. The noscript and the phantom search shortcut are polish gaps, not conformance failures.",
    "recommendation": "In navigate(): `document.title=t(r.view==='settings'?'tabs.settings.'+r.sec:'nav.home')+' \u00b7 Q Proxy'`. Add a dict key for the noscript. Either implement #settings-search or delete the Ctrl+K branch and its shortcuts row (chrome.js:40 lists 'Ctrl/\u2318+K').",
    "severity": "low",
    "source": "src/ui/panel/home.js",
    "title": "Static document title across all views/routes; English-only <noscript>"
   }
  ],
  "removals": [
   "chrome.js:79 \u2014 the Ctrl+K handler targets '#settings-search', an element that does not exist anywhere in the built UI; remove the dead branch (or the entire shortcut + its row in renderShortcuts, chrome.js:40) until a search actually ships.",
   "settings.js:225 \u2014 the `title=\"yes/no\"` attribute on the port-master <label> conveys nothing to AT or keyboard users; delete it and rely on the existing aria-label once the control gets a real visible target (see target-size finding).",
   "shell.html:55 \u2014 hardcoded `aria-label=\"?\"` on the shortcuts button is dead weight: buildShell (home.js:37) immediately overwrites it with t('shortcuts.title'); remove the initial attribute to avoid an English flash in FA."
  ],
  "summary": "A deceptively strong baseline \u2014 skip link, toast live regions, global :focus-visible ring, Escape-to-close with focus return, and correct lang/dir pre-paint put this far above BPB/edgetunnel \u2014 but it is not WCAG 2.2 AA. The two worst offenders are validation flows that are literally silent to screen readers (modal field errors render with no live region, no focus move, and toast suppressed exactly when an inline error exists) and every ARIA radiogroup (egress mode, fragment presets, language segment, home sub-mode) shipping role=\"radio\" buttons with zero arrow-key support. Light theme systematically fails 1.4.3/1.4.11 (danger/success/warning tokens never remapped, --text-ghost micro-labels at ~2.4:1), address-card inputs render unassociated <label> siblings with no accessible names, and the tablists lack roving tabindex so keyboard users wade through 12 tab stops. The a11y work is clearly intentional but stops at the demo-polish layer: patterns are half-implemented and the hard 20% \u2014 announcements, keyboard completeness, contrast in light mode \u2014 was never finished."
 },
 {
  "consolidations": [
   "Version/update/status-card rework merges findings 3 (dead-end update toast), 4 (unlabeled Requests total + orphan key), and 5 (no About/docs/version surface) into one pass over home.js:120 + actions.js:60-70 + api/version.ts",
   "Users surface rework merges findings 2 (bare empty state) and 8 (URL overflow, missing usage column, full re-render) into one users.js pass mirroring the existing .empty-card pattern from warp.js:63",
   "IA pass merges findings 1 (top-level WARP/Users tabs) and 7 (subscription deep links) since both change the Home/nav contract; both must update test/workers/router.spec.ts expectations and the dict",
   "Drift sweep merges finding 9 (orphaned dict keys) with the icon cleanup (i-play/i-stop) and should be paired with a new orphan-usage assertion in test/settings/fields.spec.ts so it cannot regress"
  ],
  "dimension": "UI polish & product feel (admin panel vs Cloudflare dash / Tailscale / Uptime Kuma / BPB / cmliu-edgetunnel)",
  "findings": [
   {
    "effort": "M",
    "evidence": "shell.html:42-49 renders exactly two tabs: '<a class=\"tab\" role=\"tab\" id=\"tab-home\" href=\"#/home\" data-view=\"home\"></a><a class=\"tab\" role=\"tab\" id=\"tab-settings\" href=\"#/settings/general\" data-view=\"settings\"></a>'. settings.js:30 SECTIONS ends '...{key:'users'...},{key:'warp'...}' \u2014 WARP is subtab #10 reachable only at #/settings/warp (home.js:74-80 parseRoute). dict.js:3 still carries a dead to\u2026",
    "location": "src/ui/panel/shell.html:42-49; src/ui/panel/settings.js:30; src/ui/panel/dict.js:3,20",
    "problem": "The owner's #1 pain point is real: WARP account management, presets and Amnezia require Settings\u2192WARP\u2192(detail). The presence of the orphaned nav.warp/nav.checker keys shows the flat IA was an afterthought of removing tabs without redesigning navigation. Rival panels (BPB, 3x-ui) give WARP/users their own top-level surface.",
    "recommendation": "Promote WARP and Users to top-level tabs in shell.html nav (keep #/settings/* deep links working via parseRoute). Use the existing dead nav.warp key. Consider a Home quick-actions row as the lighter alternative.",
    "severity": "high",
    "source": "src/ui/panel/shell.html",
    "title": "Information architecture buries WARP and Users as subtabs #9/#10 of Settings"
   },
   {
    "effort": "S",
    "evidence": "users.js:29: \"tb.innerHTML=S.users.length?S.users.map(userRowHtml).join(''):'<tr><td colspan=\"6\"><span class=\"field__hint\">'+esc(t('users.empty'))+'</span></td></tr>'\" \u2014 bare 'No users yet' text under six table headers, no icon, no CTA. Contrast warp.js:63 which renders a full .empty-card with icon and two CTAs (Generate/Import) for the same zero-state condition.",
    "location": "src/ui/panel/users.js:29 (renderUserRows); src/ui/panel/home.js:118-120 (renderHomeUsers)",
    "problem": "An admin with no users sees an empty 6-column table skeleton with no guidance; the Home Users card compounds it with a meaningless 'Users 0 / Active 0' stat block. The empty-card component already exists in the codebase (used by home subscriptions and WARP) \u2014 it is just not applied here, so empties are inconsistent across the product.",
    "recommendation": "Reuse .empty-card for the users zero state with an 'Add User' primary CTA; hide or re-purpose the Home users card below 1 user (or turn it into a 'Create your first user' CTA).",
    "severity": "medium",
    "source": "src/ui/panel/users.js",
    "title": "Users zero-state is a bare text row while WARP/subscriptions get illustrated empty-card CTAs"
   },
   {
    "effort": "S",
    "evidence": "chrome.js:79: \"if(mod&&e.key.toLowerCase()==='k'){e.preventDefault();const s=document.querySelector('#settings-search');if(s)s.focus();else location.hash='#/home';return}\" \u2014 grep confirms no element with id 'settings-search' is ever created. chrome.js:40 ships a shortcut row: \"['Ctrl / \u2318 + K','shortcuts.search']\" and dict.js:14 defines \"'shortcuts.search':'Focus search, or go home'\".",
    "location": "src/ui/panel/chrome.js:79 (globalKeys); src/ui/panel/chrome.js:40 (renderShortcuts); src/ui/panel/dict.js:14",
    "problem": "The keyboard-shortcuts modal explicitly advertises a search feature that does not exist anywhere in the DOM. Shipping help text for a removed/never-built feature erodes trust and is exactly the kind of drift a paid product never shows.",
    "recommendation": "Either ship a settings search (66 fields is searchable territory and the dict keys suggest it was planned) or remove the Ctrl+K row and rename the key; the silent fallback to Home should not be user-visible behavior.",
    "severity": "medium",
    "source": "src/ui/panel/chrome.js",
    "title": "Shortcuts modal advertises a Ctrl+K settings search that does not exist"
   },
   {
    "effort": "S",
    "evidence": "actions.js:60-70 'check-update': \"else if(d.updateAvailable)toast(t('home.status.updateAvailable',{v:d.latest.replace(/^v/,'')}),'ok');else toast(t('home.status.upToDate'),'ok')\" \u2014 toast-only, button re-enabled, no link rendered. Server knows the repo: src/handlers/api/version.ts:7 \"const UPSTREAM_REPO = \\\"QMahyar/Q-Proxy\\\"\"; failure copy dict.js:3 \"'home.status.updateCheckFailed':'Could not reach\u2026",
    "location": "src/ui/panel/actions.js:60-70; src/handlers/api/version.ts:7; src/ui/panel/dict.js:3",
    "problem": "An update notification that evaporates in 3.5 seconds with no link, changelog, or persistent badge is not an update UX. For an audience behind censored networks, 'Could not reach GitHub' is the expected steady state and deserves guidance (e.g. check from a different network / use the Telegram bot), not a dead-end error.",
    "recommendation": "Persist update state next to the version row (chip + link to https://github.com/QMahyar/Q-Proxy/releases), make the toast actionable, and soften the GitHub-unreachable copy with guidance. Optionally auto-check once per session.",
    "severity": "medium",
    "source": "src/ui/panel/actions.js",
    "title": "Update check UX is a dead-end toast with no release link, badge, or auto-check"
   },
   {
    "effort": "S",
    "evidence": "home.js:120 last stat row: \"'<span class=\"lbl\">'+esc(t('home.status.today'))+'</span><span class=\"mono\">'+today+'</span><span class=\"mono\">'+total+'</span>'\" \u2014 the third grid cell (requestsTotal) has no label; app.css collapses .stat-grid to 2 columns on mobile making it a floating number. dict.js:3/20 define \"'home.status.total':'Requests total'\" in both languages; grep across all panel JS shows \u2026",
    "location": "src/ui/panel/home.js:120 (renderHome status card); src/ui/panel/dict.js:3,20",
    "problem": "A metric renders unlabeled while its label string is written and translated but never wired \u2014 visible drift that ships in both languages. Also 'v'+esc(S.status.version||'') renders a lone 'v' if version is ever empty.",
    "recommendation": "Add the missing label span using t('home.status.total') (or drop the key), and guard the version fallback (e.g. '\u2014' when empty).",
    "severity": "medium",
    "source": "src/ui/panel/home.js",
    "title": "Requests-total metric renders unlabeled while its dict key home.status.total is orphaned"
   },
   {
    "effort": "M",
    "evidence": "Grep of src/ui/panel for external hrefs returns exactly one: home.js:109 \"<a href=\"+esc(BASE)+'my-ip'\" target=\"_blank\" rel=\"noopener\">\". Version appears only in the last right-column card of Home (home.js:120 \"<span class=\"mono\">v'+esc(S.status.version||'')+'</span>\"); home.js:52 sets \"document.title='Q Proxy'\" for all views; there is no About modal and no footer anywhere in shell.html.",
    "location": "src/ui/panel/home.js:109 (only external link); src/ui/panel/home.js:52,120; src/ui/panel/shell.html (no footer)",
    "problem": "A self-hosted panel aimed at non-expert admins has no path from the UI to its own documentation, repo, or release notes. Version is discoverable only by scrolling Home's right rail; the tab bar and page title never change per view. Uptime Kuma and rival proxy panels all surface version + repo/docs links in an About/footer surface.",
    "recommendation": "Add an About surface (topbar version chip opening a modal with version, colo, repo + docs links, update check) and per-view document.title. The docs/ tree in-repo should be linked (GitHub URLs) from the panel.",
    "severity": "medium",
    "source": "src/ui/panel/home.js",
    "title": "Zero documentation/help links, no About section, version buried in bottom-right status card"
   },
   {
    "effort": "M",
    "evidence": "home.js:84-92 renders all 7 rows identically: \"urls.forEach((entry,i)=>{sh+='<div class=\"row\"><span class=\"field__label\" ... >'+esc(entry.label||entry.format)+'</span>'+copyFieldHtml(...)}\" \u2014 including the '?view=html' 'Panel info' row from src/handlers/api/status.ts:54 \"({ format: \"base64\", label: \"Panel info\", url: `${base}?view=html` })\" which gets a QR button like any importable sub. Fragment \u2026",
    "location": "src/ui/panel/home.js:84-92; src/handlers/api/status.ts:45-55; src/ui/panel/actions.js (onDocClick data-mode)",
    "problem": "The links are on Home (refutes 'hidden'), but acquisition is copy-paste only: no clash://install-config?url=, sing-box://import-remote-profile?url=, hiddify://import/ deep links, no file download, no visual hierarchy for the main Base64 link, and an HTML viewer URL presented as an importable subscription with a QR code. The index-based fragment re-bind is fragile if list composition changes.",
    "recommendation": "Add per-format 'Open in client' deep links and a download button; restyle 'Panel info' as a secondary link row without QR; visually elevate the Base64 main URL; drive the fragment toggle from data attributes rather than index mapping.",
    "severity": "medium",
    "source": "src/ui/panel/home.js",
    "title": "Subscription acquisition is a flat 7-row list: no client deep links, no download, Panel-info HTML URL rendered like an importable sub with a QR button"
   },
   {
    "effort": "M",
    "evidence": "users.js:14 name cell: \"style=\"max-width:10rem;overflow:hidden;text-overflow:ellipsis;white-space:nowrap\"\" vs users.js:15 token cell: \"'<code dir=\"ltr\" class=\"mono\" style=\"font-size:var(--fs-sm);direction:ltr\">'+esc(url)+'</code>'\" \u2014 no max-width or word-break, so an unbreakable ~70-90 char URL stretches the row. todayHits/dailyReqLimit exist (users.js:3 userChip uses them) but are not displayed; \u2026",
    "location": "src/ui/panel/users.js:14-15 (userRowHtml); src/ui/panel/users.js:29 (renderUserRows full re-render)",
    "problem": "Long hostnames blow out the card width; the table re-renders wholesale on each enable/disable toggle (focus/scroll churn); the admin cannot see how close a user is to quota \u2014 the Telegram bot shows exact counts the web panel hides. At MAX_USERS=50 with no search/sort, finding a user means scanning.",
    "recommendation": "Give the URL cell the .copy-field treatment (ellipsis + word-break) already used elsewhere; add a Hits/Quota column fed by existing fields; add a name filter input; update the toggled row in place instead of full reload.",
    "severity": "medium",
    "source": "src/ui/panel/users.js",
    "title": "Users table: unclamped subscription-URL cell overflows, no per-user usage numbers, full re-render on toggle"
   },
   {
    "effort": "S",
    "evidence": "settings.js:269: \"return '<section class=\"card\"><div class=\"card__head\"><div class=\"card__title hint-danger\">'+esc(t(card.title))+'</div></div><button type=\"button\" class=\"btn btn--ghost-danger\" data-action=\"reset-defaults\">'...\" \u2014 no description, no card--danger class (defined in app.css but unused here), positioned as the 3rd of 6 General cards (settings.js:30-46) between Kill switch and Backup \u2026",
    "location": "src/ui/panel/settings.js:40 (SECTIONS), settings.js:269 (cardHtml danger branch)",
    "problem": "The only irreversible action in the panel (reset to factory values) is presented as a bare button with no explanation of scope, no visual quarantine, and no typed confirmation. Cloudflare-dash convention: danger zones are visually distinct, describe consequences, and sit last.",
    "recommendation": "Move the reset card to the end of General, add card--danger + a one-sentence consequence description (the confirm modal copy confirm.reset_body already has the right words), and consider verb-specific confirm buttons.",
    "severity": "medium",
    "source": "src/ui/panel/settings.js",
    "title": "Danger zone is a bare title+button card placed mid-section with no description"
   },
   {
    "effort": "S",
    "evidence": "actions.js:441: \"['m-warp-generate','m-warp-import','m-warp-preset','m-user','m-rot','m-keys'].forEach(id=>$(id).addEventListener('keydown',e=>{if(e.key==='Tab')trapFocus($(id),e)}))\" and the Escape chain at actions.js:445-452 handles m-confirm, m-qr and the same five warp/user modals \u2014 'm-wizard' appears in neither. Only m-confirm and m-qr have backdrop-click close handlers. lib.js closeModal set\u2026",
    "location": "src/ui/panel/actions.js:441-452 (wireEvents); src/ui/panel/lib.js:45 (closeModal)",
    "problem": "The first-run wizard \u2014 the first modal a new admin sees \u2014 can be tabbed out of into the page behind it and ignores Escape. Modal dismissal behavior differs per modal for no reason, and all modals close with an abrupt cut while toasts animate out.",
    "recommendation": "Include m-wizard in the trap/Escape/backdrop lists; unify dismissal (backdrop + Escape for all); add a short exit animation.",
    "severity": "medium",
    "source": "src/ui/panel/actions.js",
    "title": "Modal behavior inconsistent: onboarding wizard has no focus trap or Escape; backdrop click only closes confirm/QR; no exit animation"
   },
   {
    "effort": "M",
    "evidence": "Verified by t()-grep across all panel JS \u2014 these keys exist in dict.js:3 and dict.js:20 but are never referenced: nav.warp, nav.checker, tabs.settings.ports, tabs.settings.proxyip, proxyip.*, checker.* (~14 keys), ports.* legacy set (~12), warp.view.accounts, warp.view.settings, warp.settings, warp.presets.inuse, app.name, home.status.total. shell.html defines SVG symbols #i-play and #i-stop (0 re\u2026",
    "location": "src/ui/panel/dict.js:3,20; src/ui/panel/shell.html (SVG symbol defs); test/settings/fields.spec.ts:112-127",
    "problem": "Two full languages of dead strings (~40+ keys with long Persian text) plus dead SVG symbols ship inside the ~470KB dist bundle, and they are evidence of removed views (IP checker, ports/proxyip tabs) whose removal left residue everywhere. Nothing in CI prevents the next drift.",
    "recommendation": "Delete orphaned keys and symbols; add a CI assertion that every DICT key is referenced by at least one t() call in the panel sources (the same harness as fields.spec.ts can do this).",
    "severity": "low",
    "source": "src/ui/panel/dict.js",
    "title": "Shipped-then-removed drift: ~40 orphaned i18n keys in both languages plus orphaned SVG symbols ride along in the 64KB dict and bundle"
   },
   {
    "effort": "S",
    "evidence": "shell.html:9 / login.html:9: \"<link rel=\"icon\" href=\"data:image/svg+xml,...\">\" only \u2014 grep for theme-color, apple-touch, and manifest across src/ui returns nothing. shell.html:8 \"<title>Q Proxy</title>\" is static; login.html DICT tagline \"'app.tagline':'Cloudflare proxy panel'\". camo.html:7 has its own decoy favicon (\"<link rel=\"icon\" href=\"data:image/svg+xml,<svg viewBox='0 0 16 16'>...\" notebook\u2026",
    "location": "src/ui/panel/shell.html:8-9; src/ui/login.html:8-9; src/ui/camo.html:7",
    "problem": "Browsers without SVG-favicon support (older Safari, all iOS home-screen saves) show a blank/globe icon; there is no theme-color so mobile browser chrome never matches the dark/light theme; the title never reflects the current view. The login page also plainly self-identifies the site's purpose while camo.html poses as a blog \u2014 defensible since the real gate is the secret securePath, but it should \u2026",
    "recommendation": "Add a small PNG favicon fallback + apple-touch-icon + theme-color (both themes), and set per-view document.title ('Users \u00b7 Q Proxy'). Document the login-branding-vs-camo tradeoff in docs.",
    "severity": "low",
    "source": "src/ui/panel/shell.html",
    "title": "Favicon/branding gaps: SVG-only favicon with no raster fallback, no theme-color, no apple-touch icon, static document title"
   },
   {
    "effort": "M",
    "evidence": "app.css defines a staggered entrance only for home: \".home-grid>.card{animation:fadeUp .5s var(--ease) both}...nth-child(2){animation-delay:.06s}...\" while home.js showSection does \"SECTIONS.forEach(s=>{const p=$('sp-'+s.key);if(p)p.hidden=s.key!==sec})\" \u2014 instant swap, no transition. home.js:103-104: \"if(!S.warp)loadWarpIfNeeded().then(()=>{if(S.warp)renderHome()})\" \u2014 full renderHome() re-render \u2026",
    "location": "src/ui/panel/app.css (fadeUp rules); src/ui/panel/home.js (showSection, renderHome re-entry)",
    "problem": "The two views of the app animate differently: Home feels alive, Settings feels like a document swap. The async WARP arrival re-renders the entire home grid (killing scroll position and any in-progress interactions) rather than patching the one card.",
    "recommendation": "Add a lightweight enter animation to settings panels (reduced-motion safe), and patch only the WARP portion on async arrival instead of re-rendering renderHome().",
    "severity": "low",
    "source": "src/ui/panel/app.css",
    "title": "Motion polish uneven: settings subtab swaps are instant hidden-toggles while home animates; late WARP data re-renders the whole home grid"
   },
   {
    "effort": "S",
    "evidence": "chrome.js:27-38: \"let last=0...try{last=Number(localStorage.getItem(EXPORT_KEY)||'0')}...if(last&&now-last<30*864e5){box.hidden=true;return}\" \u2014 with last=0 the guard falls through and box.hidden=false; banner text dict.js:16 \"'backup.remind':'No settings backup in over 30 days...'\".",
    "location": "src/ui/panel/chrome.js:27-38 (maybeBackupBanner)",
    "problem": "A brand-new admin's first login surfaces the force-change overlay, then the welcome wizard, then immediately a stale-backup warning \u2014 an onboarding pile-up that reads as nagging. The banner logic itself (30d reminder, 7d dismiss, export tracking) is otherwise solid and per-browser.",
    "recommendation": "Suppress the banner until qp_wizard_done is set (or until first settings save), keeping the 30/7-day logic for established installs.",
    "severity": "low",
    "source": "src/ui/panel/chrome.js",
    "title": "Backup-reminder banner fires on brand-new installs alongside the welcome wizard"
   },
   {
    "effort": "S",
    "evidence": "Grep for '@media print' and 'window.print' across src/ui/panel: zero matches. Printable surfaces (users table, subscription URL list) would render topbar, dotgrid/noise textures, blob backgrounds, and the fixed applybar.",
    "location": "src/ui/panel/app.css (entire file, no @media print)",
    "problem": "Minor for this product, but the audit dimension explicitly covers print: an admin archiving the user list or subscription links to paper/PDF gets decorative noise. The responsive table already degrades to labeled blocks, which is 80% of a print stylesheet.",
    "recommendation": "Add a small @media print block hiding topbar, .texture-wrap, .applybar, .toasts and forcing light colors.",
    "severity": "low",
    "source": "src/ui/panel/app.css",
    "title": "No print styles anywhere in app.css"
   },
   {
    "effort": "S",
    "evidence": "shell.html:55: \"<button type=\"button\" class=\"btn btn--icon btn--ghost\" id=\"shortcuts-btn\" data-action=\"shortcuts\" aria-label=\"?\">?</button>\"; warp.js:34 pool-add renders \"aria-label=\"'+esc(t('common.add'))+'\"><svg aria-hidden=\"true\"><use href=\"#i-copy\"/></svg>\" (copy glyph for an add action); lib.js:47: \"ok.textContent=t('confirm.yes')\" \u2014 'Yes, continue' (dict confirm.yes) for delete user, reset d\u2026",
    "location": "src/ui/panel/shell.html:55; src/ui/panel/warp.js:34; src/ui/panel/lib.js:47; src/ui/panel/dict.js:3 (confirm.yes)",
    "problem": "Small consistency debts that read as unfinished: a text '?' button among SVG icons with a hardcoded aria-label=\"?\" until JS overrides it, a copy glyph meaning add, X meaning both close and delete, and generic confirm copy where every serious admin panel uses verb-specific destructive buttons.",
    "recommendation": "Give shortcuts-btn a keyboard SVG + proper aria-label; use a plus glyph for pool-add; thread a verb-specific label through confirmDialog (title text already exists); reserve X for close.",
    "severity": "low",
    "source": "src/ui/panel/shell.html",
    "title": "Icon/label consistency nits: text '?' button among SVG icons, copy glyph for an add action, X for both close and delete, generic 'Yes, continue' on destructive confirms"
   }
  ],
  "removals": [
   "Orphaned i18n keys in BOTH dict.js languages: nav.warp, nav.checker, tabs.settings.ports, tabs.settings.proxyip, the legacy proxyip.* set, the checker.* set (~14 keys), the legacy ports.* set (~12 keys), warp.view.accounts, warp.view.settings, warp.settings, warp.presets.inuse, app.name \u2014 verified unreferenced by any t() call",
   "Orphaned SVG symbols #i-play and #i-stop in shell.html (remnants of the removed IP-checker view; zero references)",
   "The '?view=html' 'Panel info' entry rendered as a copyable+QR subscription row in the Home list (src/handlers/api/status.ts:54) \u2014 restyle as a plain secondary link or remove from the importable list",
   "The misleading 'shortcuts.search' Ctrl+K row in the shortcuts modal (chrome.js:40) if a settings search is not shipped"
  ],
  "summary": "The panel's interaction engineering is genuinely above rival proxy panels (per-section undo/redo, debounced kill-switch, TOTP recovery flow, ETag caching, reduced-motion, RTL), but the product shell betrays it: two top-level tabs bury WARP and Users, the shipped shortcuts modal advertises a Ctrl+K search that does not exist, and there is no About, docs link, or update path beyond a 3.5-second toast. Empty states are inconsistent \u2014 WARP gets an illustrated CTA card while Users gets a bare 'No users yet' hint under six table headers. Visible shipped-then-removed drift (orphaned i18n keys in both languages, dead SVG symbols, an unlabeled metric with an unused label key) survives CI because the drift test enforces key parity but never key usage."
 },
 {
  "consolidations": [
   "actions.js:154 tg-setup, :60 check-update, change-password, totp-*, and modal saves (wg-go/wi-go/wp-go/mu-go) all hand-roll el.disabled=true/try/finally \u2014 extract one withBusy(el, fn) helper and apply it to the ~10 unprotected mutations (finding 4).",
   "Three loading idioms coexist (actions.js:514 skeleton, users.js:8 bare text, warp.js:16 text+spinner) \u2014 consolidate into one loading component with a skeleton variant used by home cards, users table, warp grids, and pool lists (finding 8).",
   "Kill-switch optimistic+rollback+abort (home.js:138-153) and user-toggle rollback (actions.js:385-389) are two hand-rolled variants \u2014 merge into one optimistic-toggle helper and reuse it for the WARP preset select (finding 12).",
   "Seven WARP handlers repeat invalidateWarp();loadWarpIfNeeded().then(render\u2026) \u2014 extract one warpMutate() success pipeline.",
   "markDirty() re-collects and stringifies all 10 sections on every keystroke (settings.js:452-460) and scheduleDirtyPush re-collects all dirty sections again 1s later (chrome.js:49-53) \u2014 one diff/snapshot pipeline should feed dirty flags, the apply bar, and the undo stack.",
   "pool-fetch / source-doh / home-pool-refresh are three actions wrapping one loader \u2014 collapse to one parametrized action."
  ],
  "dimension": "Interaction Patterns (actions.js + chrome.js focus: loading, optimistic updates, undo/redo, shortcuts, toasts/inline errors, dirty-state, staleness, clipboard, double-submit)",
  "findings": [
   {
    "effort": "S",
    "evidence": "chrome.js:79: `if(mod&&e.key.toLowerCase()==='k'){e.preventDefault();const s=document.querySelector('#settings-search');if(s)s.focus();else location.hash='#/home';return}`. Grep over src/ui/panel confirms `settings-search` appears ONLY in chrome.js:79 (and the built panel.html:1004) \u2014 no element with that id is ever rendered by shell.html or any JS. chrome.js:40 still advertises it: `['Ctrl / \\u23\u2026",
    "location": "src/ui/panel/chrome.js:79; src/ui/panel/chrome.js:40; src/ui/panel/shell.html (no #settings-search element anywhere)",
    "problem": "The flagship shortcut (GitHub/Slack muscle memory) is a broken promise: pressing Ctrl+K on Home navigates away from what you were doing under the pretense of focusing search. It is the worst kind of dead code \u2014 one that teaches the admin the shortcuts modal lies. Compare Cloudflare dash / Linear where Ctrl+K is a real command palette.",
    "recommendation": "Either implement the search input (it would genuinely help in a 10-subtab settings IA) or delete the Ctrl+K branch and its row in renderShortcuts(). Do not ship it as a fake 'go home' alias.",
    "severity": "high",
    "source": "src/ui/panel/chrome.js",
    "title": "Ctrl/Cmd+K advertises a search box that does not exist"
   },
   {
    "effort": "S",
    "evidence": "actions.js:44-45: `discard(){[...S.dirty].forEach(sec=>discardSection(sec))}` \u2014 discardSection (settings.js:499-509) overwrites every [data-bind] from the saved snapshot with no confirmDialog. Every other destructive action confirms (reset-defaults actions.js:70, warp-delete actions.js:91, users-del actions.js:137, logout actions.js:37). The undo stack cannot reliably rescue you: chrome.js:49-53 p\u2026",
    "location": "src/ui/panel/actions.js:44-45; src/ui/panel/chrome.js:49-53,63-72; src/ui/panel/settings.js:499-509",
    "problem": "One mis-click on a floating bar button nukes edits across up to 10 sections; recovery requires being in the right section, knowing an invisible shortcut, and winning a 1-second race. Professional panels confirm before discarding and keep the discarded state one gesture away.",
    "recommendation": "Wrap discard() in confirmDialog (danger variant), and after discard push the discarded state as an undo entry for every affected section so Ctrl+Z per section is a real recovery path.",
    "severity": "high",
    "source": "src/ui/panel/actions.js",
    "title": "Discard bar button silently destroys ALL unsaved sections with no confirmation"
   },
   {
    "effort": "M",
    "evidence": "chrome.js:75/80: `if(isEditable(e.target))return;` runs BEFORE `if(mod&&e.key.toLowerCase()==='z'){...undoSection();return}` \u2014 so in the one place admins edit (inputs/textareas) Ctrl+Z does native text undo, never section undo. chrome.js:64: `const sec=currentSection();` \u2014 undo only touches the route's section; User/WARP/delete actions are not in any undo stack. chrome.js:49-53 pushes the POST-edi\u2026",
    "location": "src/ui/panel/chrome.js:44-53,63-72,74-83; src/ui/panel/chrome.js:37-42",
    "problem": "A settings admin who fat-fingers a UUID or a fragment range cannot Cmd+Z while focused in the field; they must click away, press an invisible combo, on the right subtab, within the debounce timing. Rivals (BPB, Uptime Kuma) at least offer visible reset/revert per form. This is a feature that exists to be demoed, not used.",
    "recommendation": "Push the PRE-edit state on first change (seed from S.snap), coalesce by field/section rather than time, allow undo while focus is in a field (except true text-caret undo), add Undo/Redo buttons to the apply bar, and state the section scoping in the shortcut label.",
    "severity": "high",
    "source": "src/ui/panel/chrome.js",
    "title": "Undo/redo: invisible, section-scoped, debounced into a race, and disabled inside the inputs you edit"
   },
   {
    "effort": "M",
    "evidence": "settings.js:472-473: applySection always does `const btn=$('apply-btn');btn.disabled=true;` \u2014 but settings.js:318 renders `data-action=\"section-save\"` buttons that call applySection directly (actions.js:213) and are never disabled: double-click fires two concurrent PATCH /api/settings/save of the same diff. Same gap: users-regen (actions.js:141-144, no disabled \u2014 double-click rotates the token twi\u2026",
    "location": "src/ui/panel/settings.js:318,471-474; src/ui/panel/actions.js:46-63,86-94,100-109,141-144,200-203",
    "problem": "Inconsistent in-flight guarding means the highest-stakes actions (token rotation, account deletion, full settings import) are exactly the ones you can double-fire. The section-save case also reveals a latent bug: applySection hardcodes $('apply-btn') regardless of which button triggered it.",
    "recommendation": "Extract one withBusy(el, asyncFn) helper (disable + aria-busy + optional spinner) and use it in every action handler; in applySection, disable the actual triggering button, not a hardcoded id.",
    "severity": "high",
    "source": "src/ui/panel/actions.js; src/ui/panel/settings.js",
    "title": "Double-submit protection exists for half the mutations; section-save disables the wrong button"
   },
   {
    "effort": "S",
    "evidence": "users.js:29: `tb.innerHTML=S.users.length?S.users.map(userRowHtml).join(''):'<tr><td colspan=\"6\"><span class=\"field__hint\">'+esc(t('users.empty'))+'</span></td></tr>';` with dict 'users.empty':'No users yet'. The table header, select-all checkbox and bulk bar frame still render around this single dim sentence. The codebase already has the right pattern and uses it twice: home.js:100-101 (empty-car\u2026",
    "location": "src/ui/panel/users.js:29,8; contrast src/ui/panel/warp.js:69-71 and src/ui/panel/home.js:100-101",
    "problem": "Owner pain point (3) CONFIRMED. The users subtab on a fresh install shows a dead 6-column table skeleton and one gray sentence; the Add button is a small ghost-primary in the card head. Every other empty state in this app teaches the admin what to do next except the one about people.",
    "recommendation": "Reuse .empty-card: icon, 'No users yet', one line about per-user scoped links, and a primary 'Add first user' CTA that opens the m-user modal. Optionally hide the table chrome (thead/select-all) until count > 0.",
    "severity": "high",
    "source": "src/ui/panel/users.js",
    "title": "Users empty state is a dead-end table frame \u2014 worst empty state in an app that already has the right pattern"
   },
   {
    "effort": "L",
    "evidence": "lib.js:18-22: `const API_TTL=30000;` with `sessionStorage.getItem('qpc:'+p)` 30s cache and ETag revalidation; server-side 60s isolate cache + KV colo cache documented in AGENTS.md Known Gaps (stale-cache read-modify-write). settings.js:483: `await api('api/settings/save',{method:'PUT',body:patch});` \u2014 no If-Match, no updatedAt precondition. Negative grep: 'updatedAt', 'If-Match', any version/confl\u2026",
    "location": "src/ui/panel/lib.js:18-26; src/ui/panel/settings.js:471-487; (absence verified: no 'updatedAt'/'If-Match' anywhere in src/ui/panel)",
    "problem": "The diff-patch PUT (settings.js:473-477) does narrow the blast radius to touched leaves \u2014 good \u2014 but the UI gives the admin zero signal that the baseline under their form is rotting. Cloudflare dash revalidates on save and surfaces 'changed since you opened this page'; here the only hint is a 30s cache the admin cannot see.",
    "recommendation": "Return an updatedAt/revision in bootstrap and settings/save responses; render 'Settings updated <time>' in the apply bar; on save, if the server revision differs from the bootstrap revision, warn with a confirmDialog before overwriting, and purge qpc:api/bootstrap more aggressively (it is already done for general/addresses at settings.js:463-467 \u2014 do it for every section).",
    "severity": "high",
    "source": "src/ui/panel/lib.js",
    "title": "Concurrent-edit staleness (60s KV + 30s sessionStorage) ships with zero UI affordance"
   },
   {
    "effort": "M",
    "evidence": "actions.js:43: `(async()=>{for(const sec of[...S.dirty])await applySection(sec)})()`. Each applySection fires its own `toast(t('toast.settingsSaved'),'ok')` (settings.js:487) \u2014 apply-all with 5 dirty sections queues 5 success toasts into a container that hard-drops them: lib.js:11 `while(box.children.length>3)box.firstChild.remove();`. The apply button's disabled/applying state (settings.js:472-47\u2026",
    "location": "src/ui/panel/actions.js:43; src/ui/panel/settings.js:471-501; src/ui/panel/lib.js:11",
    "problem": "Ctrl+S on a heavily-edited session produces a 5-round-trip serial commit whose feedback is three dropped toasts. If section 3 fails validation, sections 1-2 are already saved, the loop continues to 4-5, and the only summary is 'Fix N error(s)' per failing section.",
    "recommendation": "Batch the apply-all loop: one summary toast with per-section outcome, a sequential progress label on the apply button ('Saving 2/5'), and an AbortController-style cancel. Or collapse to a single PATCH per request when sections are independent.",
    "severity": "medium",
    "source": "src/ui/panel/actions.js",
    "title": "Apply-all runs a serial N-section commit with one shared button state and toast spam the container caps at 3"
   },
   {
    "effort": "M",
    "evidence": "actions.js:514 renderBootSkeleton is used exactly once (shimmer .skeleton in app.css). Everything else is bare text: users.js:8 `'<tr><td colspan=\"6\"><span class=\"field__hint\">'+esc(t('common.loading'))+'</span></td></tr>'`; home.js:115 (home users card), home.js:157 (my-ip), home.js:177 (home pool). The ONLY spinner in the whole panel is warp.js:16: `'<span class=\"field__hint\">Loading\u2026</span><spa\u2026",
    "location": "src/ui/panel/actions.js:514; src/ui/panel/users.js:8; src/ui/panel/home.js:115,157,177; src/ui/panel/warp.js:16; src/ui/panel/actions.js:200-203",
    "problem": "The polish bar is set by the boot skeleton and then abandoned; three different loading idioms (skeleton / text / text+spinner) and one no-feedback case ship in one app. A text-only 'Loading\u2026' in a table reads as a broken page on slow colo links \u2014 which is precisely this product's audience.",
    "recommendation": "Pick one grammar: skeleton rows for card/table bodies, .spin inside buttons for actions, and a busy state for addr-probe/pool-test. Reuse login's setBusy helper in the panel.",
    "severity": "medium",
    "source": "src/ui/panel/home.js",
    "title": "Loading-state grammar is one skeleton, one spinner, and seven bare 'Loading\u2026' text nodes"
   },
   {
    "effort": "S",
    "evidence": "lib.js:38: `try{document.execCommand('copy')}catch(e){}ta.remove()` \u2014 failure is swallowed; lib.js:37 `return Promise.resolve(fallbackCopy(s))` resolves unconditionally. actions.js:4-9: `copyText(val||'').then(()=>{el.classList.add('copied');...toast(t('common.copied'),'ok')})` \u2014 no failure branch exists anywhere. On non-secure origins or when clipboard permission is denied, the admin sees 'Copied\u2026",
    "location": "src/ui/panel/lib.js:37-38; src/ui/panel/actions.js:4-9",
    "problem": "For a panel whose core job is handing out subscription URLs, a false 'Copied' is the most damaging kind of lie \u2014 the admin pastes something else and debugs their client for an hour. Q Proxy is frequently self-hosted on http://IP:8787 (per docs), where navigator.clipboard is unavailable and execCommand is the only path.",
    "recommendation": "Make fallbackCopy return boolean; on false show 'Copy failed \u2014 select and copy manually' (error kind) and auto-select the code element so Ctrl+C works.",
    "severity": "medium",
    "source": "src/ui/panel/lib.js",
    "title": "Copy-to-clipboard reports success unconditionally; the fallback swallows failure"
   },
   {
    "effort": "S",
    "evidence": "actions.js:493: `const tok=d&&d.user&&d.user.token;closeModal('m-user');toast(t('users.toast.created'),'ok');if(tok){const url=userSubUrl(tok);copyText(url);openQr(url)}` \u2014 copyText's result is ignored (no success/failure handling), while the regen path (actions.js:141-144) shows the dedicated showRotation(tok) modal with a copy button and hint. Create gets a QR of a URL that may not be on the cli\u2026",
    "location": "src/ui/panel/actions.js:493; contrast src/ui/panel/actions.js:141-144 and users.js:showRotation",
    "problem": "Token rotation got the careful handoff UI; token creation did not. If the clipboard write fails (finding 9), the only recovery is reading a URL off a QR canvas. Inconsistent ritual for the same secret.",
    "recommendation": "Route create through showRotation() like regen: one modal with the full URL, copy button (with real failure state), and QR. Drop the fire-and-forget copyText.",
    "severity": "medium",
    "source": "src/ui/panel/actions.js",
    "title": "User creation copies the token silently and skips the rotation modal that regeneration gets"
   },
   {
    "effort": "M",
    "evidence": "actions.js:453-456: `window.addEventListener('hashchange',navigate);window.addEventListener('beforeunload',e=>{if(S.dirty.size){e.preventDefault();e.returnValue=''}})` \u2014 navigate() (home.js:63-72) never consults S.dirty; only full unload is guarded (and the browser then shows its own untranslated dialog). Three confirm idioms coexist: styled confirmDialog (lib.js:40+), native confirm on 401 (lib.j\u2026",
    "location": "src/ui/panel/actions.js:453-456; src/ui/panel/home.js:54,63-72; src/ui/panel/lib.js:27",
    "problem": "Mitigations exist (apply bar + ' *' on dirty subtabs, settings.js updateApplyBar) but the safety net is three different dialog systems with inconsistent text and zero explanation of WHAT is unsaved. In the FA locale the beforeunload and 401 prompts appear in browser-locale English.",
    "recommendation": "One guard: intercept in-app navigation (hashchange) with confirmDialog listing dirty sections; replace the 401 native confirm with confirmDialog; leave beforeunload as the last resort for reload/close only.",
    "severity": "medium",
    "source": "src/ui/panel/actions.js",
    "title": "Dirty-state warnings: guarded on unload, silent on in-app navigation, three different confirm idioms"
   },
   {
    "effort": "M",
    "evidence": "actions.js:396: `if(e.target.id==='warp-preset'){...await api('api/warp/account/'+id,{method:'PUT',body:{endpoint_list:{type:'preset',preset_id:e.target.value}}});toast(t('common.saved'),'ok');...}catch(err){toastErr(err)}` \u2014 the select renders its new value instantly (native select), there is no pending class, no debounce (each change = one PUT), and on failure the stale preset is never written b\u2026",
    "location": "src/ui/panel/actions.js:396; contrast src/ui/panel/actions.js:385-389 and src/ui/panel/home.js:138-153",
    "problem": "Same card, three interaction models (name/dns explicit Apply buttons, preset instant-save select, amnezia bulk save) and only the settings-side toggles got the optimistic treatment. A failed preset PUT leaves the UI asserting a state the server rejected until the next full reload.",
    "recommendation": "Apply the user-toggle pattern here: pending class on the select row, snapshot prior value, restore on error. Debounce rapid changes.",
    "severity": "medium",
    "source": "src/ui/panel/actions.js",
    "title": "WARP preset <select> auto-PUTs on every change with no pending state, no optimistic rollback"
   },
   {
    "effort": "S",
    "evidence": "actions.js:46-63: `fileInput.onchange=async()=>{...JSON.parse(await file.text())...await api('api/settings/import',...);toast(...);location.reload()}` \u2014 no el.disabled around the await (double-select double-imports), no confirmDialog (unlike reset-defaults actions.js:70 which confirms the equally destructive reset), and location.reload() while S.dirty.size>0 surfaces the generic beforeunload bar (\u2026",
    "location": "src/ui/panel/actions.js:46-63",
    "problem": "Restoring a backup is the most destructive operation in the panel and it has the weakest guard rail: no confirm, no busy state, no explicit dirty-state warning. One accidental double-click of the file dialog confirm overwrites everything twice.",
    "recommendation": "confirmDialog('settings will be replaced') before the PUT; withBusy on the import button; after success, purge S.dirty and navigate instead of relying on the browser dialog.",
    "severity": "medium",
    "source": "src/ui/panel/actions.js",
    "title": "Settings import: no busy state, double-import possible, discards unsaved edits via the generic browser dialog"
   },
   {
    "effort": "M",
    "evidence": "home.js:97-99: `urls.forEach((entry,i)=>{sh+='<div class=\"row\">...'+copyFieldHtml(subUrlWithMode(entry.url),'sub-u'+i)+'</div>'});...if(urls.length)sh+='<p class=\"field__hint\" dir=\"auto\">'+esc(t('country.hint'))+'</p>';` \u2014 the country feature (?country=XX) is documented in a hint sentence rather than as a control. The only download affordance is inside the QR modal (actions.js:18-25 downloads a PN\u2026",
    "location": "src/ui/panel/home.js:97-99; src/ui/panel/actions.js:18-25; src/ui/panel/warp.js:87 (WARP detail rows)",
    "problem": "Owner pain point (2) PARTIALLY CONFIRMED: the links are present and copy/QR work well (copy-field pattern with real clipboard fallback is good), but getting a working client requires copy \u2192 open client \u2192 paste, per format, every time. The 40%-max-width labels and 8+ rows also make the primary base64 URL visually equal to the panel-info URL.",
    "recommendation": "Add per-format download buttons (the server already emits these formats) and one-tap deep links for the big clients; promote the primary subscription above the format list; make country filter a small select that rewrites URLs, same mechanism as the fragment-mode seg.",
    "severity": "medium",
    "source": "src/ui/panel/home.js",
    "title": "Main subscription links: copy/QR only \u2014 no copy-all, no per-format download, country filter documented as a hint instead of a control"
   },
   {
    "effort": "M",
    "evidence": "shell.html:30-33: the nav is exactly two tabs (`#tab-home`, `#tab-settings`); SECTIONS (settings.js) lists 10 subtabs with 'warp' last, rendered into an overflow-x bar with a fade mask (app.css .subtabs). home.js:98: `if(S.warp&&S.warp.accounts.length)sh+=warpSubsHtml();` \u2014 home surfaces WARP output only after the admin has already been to Settings\u2192WARP at least once; home.js:103 lazy-loads it on \u2026",
    "location": "src/ui/panel/shell.html:30-33; src/ui/panel/home.js:98,103; src/ui/panel/dict.js:3; src/ui/panel/settings.js (SECTIONS order, warp 10th)",
    "problem": "Owner pain point (1) CONFIRMED. WARP is a first-class product surface (17 output formats, accounts, presets) buried behind two clicks and nine sibling tabs, with horizontal-scroll discoverability. The leftover nav.* keys prove the top bar used to promise more and was quietly narrowed. This is an IA decision, not a code defect \u2014 flagging for the owner.",
    "recommendation": "Owner decision required: promote WARP to a top-level tab (dict key already exists) or, minimally, add a persistent 'WARP' shortcut card on Home regardless of account existence.",
    "severity": "medium",
    "source": "src/ui/panel/shell.html",
    "title": "WARP reachability: subtab 10 of 10, home only surfaces WARP after it exists, and dead nav.* dict keys betray the old nav"
   },
   {
    "effort": "S",
    "evidence": "shell.html:55: `<button type=\"button\" class=\"btn btn--icon btn--ghost\" id=\"shortcuts-btn\" data-action=\"shortcuts\" aria-label=\"?\">?</button>` (buildShell later overwrites the label, but the static HTML advertises nothing). The modal (chrome.js:37-42) lists 5 shortcuts, one dead (Ctrl+K). 'g h' is a timing-based chord (`if(e.key==='g'){lastG=Date.now();return}` chrome.js:81) with zero in-UI affordan\u2026",
    "location": "src/ui/panel/shell.html:55; src/ui/panel/chrome.js:37-42,80-82",
    "problem": "Shortcuts are a power feature for exactly the audience a self-hosted proxy panel has, and here they are undiscoverable and half-broken. The '?'-as-label pattern also reads as a help button, not a keyboard map.",
    "recommendation": "Bind '?' (and Ctrl+/) to open the modal; add kbd hints to the apply bar and subtabs' tooltips; fix or drop the Ctrl+K row.",
    "severity": "low",
    "source": "src/ui/panel/shell.html",
    "title": "Keyboard discoverability: the shortcut affordance is a bare '?' button and a 5-row modal with a dead row"
   },
   {
    "effort": "S",
    "evidence": "lib.js:31: `const list=[...modal.querySelectorAll('button,input,[href],[tabindex]:not([tabindex=\"-1\"])')].filter(x=>!x.disabled&&x.offsetParent!==null);` \u2014 `select` and `textarea` are missing from the selector, so any future modal containing one lets Tab escape to the page behind (openModal's return-focus also only fires from closeModal). No current modal contains a select/textarea, so this is lat\u2026",
    "location": "src/ui/panel/lib.js:31-36; src/ui/panel/shell.html modal markup",
    "problem": "The focus trap is otherwise exemplary (Tab wrap, Escape, return-focus) \u2014 but one new field type breaks the invariant silently, and the WARP preset modal already contains textareas in spirit (wp-endpoints is a TEXTAREA inside m-warp-preset \u2014 wait, verified: shell.html m-warp-preset has `<textarea class=\"input textarea textarea--mono\" id=\"wp-endpoints\"...>` \u2014 so the trap is broken TODAY in m-warp-pr\u2026",
    "recommendation": "Add `select,textarea` to the trapFocus querySelectorAll in openModal/trapFocus and in the per-modal Tab wiring (actions.js:440-442).",
    "severity": "low",
    "source": "src/ui/panel/lib.js",
    "title": "Modal focus trap omits SELECT and TEXTAREA \u2014 tab can escape the modal"
   }
  ],
  "removals": [
   "chrome.js:79 \u2014 the `#settings-search` dead branch (and its row in the shortcuts modal, chrome.js:40 + dict 'shortcuts.search'): remove, or actually build the search field the id promises (owner decision).",
   "dict.js:3 and dict.js:20 \u2014 'nav.warp' and 'nav.checker' i18n strings: zero usages outside dict.js; vestiges of the pre-collapse navigation.",
   "users.js:12 \u2014 the `data-qr` attribute on the user-row COPY button: no handler reads data-qr from that button (the separate QR button next to it does); dead payload."
  ],
  "summary": "The interaction layer is a tale of two codebases: optimistic toggles with rollback, per-field server-error mapping, focus trapping, and pause-on-hover toasts show real craft, while busy states, loading feedback, and undo degrade to bare \"Loading\u2026\" text, one lone spinner, and a keyboard combo that refuses to work inside the inputs admins actually type in. Advertised features are dead code \u2014 Ctrl+K focuses a #settings-search element that exists nowhere in the DOM, and the shortcuts modal sells it anyway. Half the destructive mutations (token regen, deletes, WARP saves, settings import, per-section Save) lack double-submit protection, Discard wipes every dirty section with no confirmation, and 60s KV / 30s sessionStorage staleness ships with zero conflict affordance."
 },
 {
  "consolidations": [
   "Sources tab should merge into the Egress subtab (its only unique field is dead and its pool-fetch button duplicates the Egress pool card); a single relay-pool surface replaces three.",
   "routingRules.* should move out of the camouflage card into their own 'Routing rules' card (or subtab), reusing the already-present-but-dead routing.title i18n key.",
   "Panel-access security controls should consolidate: allowedIps belongs with the Security (password) card or a titled 'Panel access' card alongside securePath, not in the untitled General card next to profileTitle/debugLogging.",
   "Language selection should consolidate to the topbar segment only, removing the Settings\u2192General language select (or vice versa).",
   "credential-bearing URI fields should consolidate on the 'secret' renderer with reveal/copy: chainProxy.uri and camouflage.url join securePath/vlessUuid/telegram.botToken.",
   "addresses.card.hint and addresses.card.help duplicate each other \u2014 merge into one help text and fix the stale 'One entry per line' wording to describe the card grid.",
   "The three behavior numbers (urlTestIntervalSec, subUpdateIntervalHours, maxNodesPerFormat) plus the DoH trio should collapse under one 'Advanced' disclosure with per-field hints instead of sitting as raw always-visible inputs.",
   "ECH trio (echEnabled/echAuto/echServerName) plus alpn/sniCase/fingerprint should group under one 'Advanced TLS' collapsible in protocols.common using the existing showIf mechanism proven in the egress section.",
   "The panel-wide hint standard already demonstrated by advanced.tg.hint and remote.nodes.help should be applied to the ~12 hint-less fields listed, rather than inventing new copy patterns per field."
  ],
  "dimension": "settings-field-by-field-audit",
  "findings": [
   {
    "effort": "S",
    "evidence": "UI renders it: settings.js:102 `FL('localDns','str','advanced.localDns.label',null,{mono:true})`. It is validated at validate.ts:525-529 (`case \"localDns\"` \u2026 `out.localDns = v`) and declared at fields.ts:110. `grep -rn localDns src` finds no other reference \u2014 no emitter, handler, or tunnel path consumes it (remoteDns feeds singbox-json.ts:285-286 and egress.ts:91; localDns feeds nothing).",
    "location": "src/ui/panel/settings.js:102",
    "problem": "Every minute an admin spends on 'Local DNS fallback' is wasted: the value is stored and validated but has zero runtime effect. This is the exact class of dead control that erodes trust in the rest of the panel.",
    "recommendation": "Delete the field, the descriptor row, and its dict keys (en+fa), or wire it into the sing-box DNS config where a 'local' resolver is semantically meaningful. Until decided, at minimum hide it behind an 'unwired' Advanced disclosure.",
    "severity": "high",
    "source": "src/ui/panel/settings.js",
    "title": "localDns is a write-only setting: rendered, validated, saved \u2014 consumed by nothing"
   },
   {
    "effort": "M",
    "evidence": "The tab's only editable field is bound at warp.js:12 (`data-bind=\"sourceUrls\" data-validate=\"url\"`), saved via fields.ts:117 (`{ path: \"sourceUrls\", spec: { kind: \"urlList\", maxItems: 16 } }`). But `grep -rn sourceUrls src` shows no consumer; the relay pool resolves exclusively from proxyIps/proxyIpPoolUrl/built-in DoH per proxyip-pool.ts:196-203 (`source = 'list' | 'doh' | 'url'`). Sources.desc p\u2026",
    "location": "src/ui/panel/warp.js:10-13",
    "problem": "An admin pastes scanner URLs, presses Save, gets toast 'Settings saved' \u2014 and nothing ever happens. The tab also duplicates the Egress pool card's Fetch button and is residue of the removed IP Checker view (dead checker.* dict keys, dead `checker-targets` special-case in settings.js:564).",
    "recommendation": "Merge Sources into the Egress subtab (or delete it). Either wire sourceUrls into collectProxyPoolDetailed as a fetchable source or remove the field, descriptor, dict keys, and the checker-targets remnant.",
    "severity": "high",
    "source": "src/ui/panel/warp.js",
    "title": "sourceUrls / entire Sources subtab saves a list nothing consumes"
   },
   {
    "effort": "S",
    "evidence": "Input rendered at settings.js:153 (`field('city',t('addresses.field.city'),'Frankfurt')`), read by readBind (L437), validated and stored at validate.ts:276-290 (`entry.city = trimmed`). `grep -rn city src/nodes src/subscription` returns nothing \u2014 node naming (naming.ts) and generate.ts:88-90 use only `entry.country`.",
    "location": "src/ui/panel/settings.js:153",
    "problem": "City is collected from the admin, persisted, counted against the 64-entry limit's dedupe key \u2014 and never influences a node name, flag, or emitter output. Dead weight in the most information-dense card of the panel.",
    "recommendation": "Remove the city input from addrCardHtml and its validation branch, or wire it into renderName as an optional token. If kept, drop it into the per-card Advanced disclosure.",
    "severity": "medium",
    "source": "src/ui/panel/settings.js",
    "title": "addresses[].city is collected and validated but never used in generation"
   },
   {
    "effort": "S",
    "evidence": "PRESETS is a 4-tuple (home.js:6: `{low:[100,200,1,1],medium:[50,100,1,5],high:[10,20,10,20],severe:[1,5,1,5]}`); applyFragmentPresetUi (settings.js:346-356) disables only `['lengthMin','lengthMax','delayMin','delayMax']` and the packets select only when `mode==='off'` (L353-355). But fragmentQuery emits fsplit only in custom mode (fragments.ts:13-20: `if (f.mode === 'custom') \u2026 &fsplit=\u2026`; else `f\u2026",
    "location": "src/ui/panel/settings.js:346-356",
    "problem": "An admin selecting Low/Medium/High/Severe can edit 'Max split min\u2013max' and 'Packets pattern' and press Save; the values persist but are silently ignored. The help text lies about split. Also the fpreset renderer (settings.js:237-243) hardcodes its own label/hint keys, so the passed `fragment.enable` label never renders \u2014 the chip row is titled just 'Preset' while 'Off' actually means 'disabled'.",
    "recommendation": "Hide (not merely disable) packets/length/delay/split unless mode==='custom'; make the preset help text accurate; render the 'Enable fragmentation' label or drop the dead label argument.",
    "severity": "medium",
    "source": "src/ui/panel/settings.js",
    "title": "Fragment presets leave split/packets editable but silently ignored"
   },
   {
    "effort": "S",
    "evidence": "showFieldError matches `[data-bind=\"CSS.escape(path)\"]` (settings.js:478-490). Server fails nested custom fields under leaf keys: validate.ts:538-540 `fail(fields, \"url\", \u2026)` for camouflage, validate.ts:496-498 `fail(fields, \"uri\", \u2026)` for chainProxy, validate.ts:564/572 `fail(fields, \"botToken\"/\"chatId\", \u2026)`. UI binds are `camouflage.url`, `chainProxy.uri`, `telegram.botToken`, `telegram.chatId` \u2026",
    "location": "src/settings/validate.ts:533-575",
    "problem": "For these four fields the admin only sees the 'Fix N error(s)' toast with no indication which field failed; the error's text is also raw English (server messages are sentences, so the DICT lookup path in showFieldError never translates them).",
    "recommendation": "Either emit fully-qualified keys from validate.ts (\"camouflage.url\", \"chainProxy.uri\", \u2026) or map leaf keys to qualified paths in the client before calling showFieldError. Add a regression test pinning the contract.",
    "severity": "medium",
    "source": "src/settings/validate.ts",
    "title": "Server validation errors for camouflage.url / chainProxy.uri / telegram.* can never render inline"
   },
   {
    "effort": "M",
    "evidence": "`FL('fingerprint','select','protocols.fingerprint.label',null,{opts:FPS})` \u2014 hint null, no help key; FPS at home.js:4 includes '360' and 'qq'. `FL('fragment.packets','select','fragment.packets',null,{opts:PACKETS})` with PACKETS=['tlshello','1-1','1-2','1-3','1-5'] (home.js:5). `FL('alpn','list',\u2026)` has only the format hint 'One protocol per line, e.g. http/1.1 or h2.' while KNOWN_ALPN silently re\u2026",
    "location": "src/ui/panel/settings.js:65",
    "problem": "The panel's target admin cannot know what 'TLS fingerprint' does (uTLS client impersonation), why 360/qq exist, what '1-3' means as a packets pattern, or that ALPN tokens outside the allowed set fail server-side after save.",
    "recommendation": "Add help popovers for fingerprint (what/why, 'random is fine'), packets patterns, and ALPN; add an ALPN preset chip row (http/1.1 \u00b7 h2,http/1.1 \u00b7 h2,http/1.1 as default-locked choices) instead of a free textarea.",
    "severity": "medium",
    "source": "src/ui/panel/settings.js",
    "title": "Developer jargon with zero explanation: fingerprint (10 raw uTLS options incl. 360/qq), fragment.packets raw wire tokens, ALPN tokens"
   },
   {
    "effort": "S",
    "evidence": "First card is `{title:null,fields:[securePath, profileTitle, debugLogging, allowedIps]}` (settings.js:31-36) \u2014 no heading at all. allowedIps' hint carries the panel-lockout warning and KV incantation 'recover by deleting the qproxy:settings KV key' (dict.js security.allowlist.hint). Second card is titled 'Kill switch' (L37) yet contains the panel language select (L39). The password card is separat\u2026",
    "location": "src/ui/panel/settings.js:31-40",
    "problem": "The panel's most important general settings appear in a card with no title; a security control that can lock the admin out is visually lumped with profile naming and a debug toggle; language under 'Kill switch' is miscategorization. The KV-key recovery instruction is dev jargon with no help-popover explaining the Cloudflare dashboard steps.",
    "recommendation": "Give the first card a title ('Panel access'), move allowedIps next to the Security/2FA cluster or its own 'Panel security' card, move language out of the kill-switch card, and expand the allowlist hint into a help popover with concrete recovery steps.",
    "severity": "medium",
    "source": "src/ui/panel/settings.js",
    "title": "Untitled General card mixes securePath, profile title, debug logging and the panel IP allowlist"
   },
   {
    "effort": "S",
    "evidence": "Fourth advanced card: `{title:'advanced.camouflage.label',fields:[camouflage.mode, camouflage.url, speedtestIntercept, routingRules.bypassLan, blockQuic, blockAds, blockMalware, customBypass, customBlock]}` (settings.js:113-122). The dedicated `routing.title` key ('Routing rules') exists in dict.js:3 but is unreferenced anywhere (verified by static scan).",
    "location": "src/ui/panel/settings.js:113-122",
    "problem": "Block QUIC / block ads / block malware / custom bypass-block lists are client-config rules with nothing to do with the camouflage landing page; the card title actively misleads. All four booleans' hints admit they only affect 'Clash and sing-box' \u2014 a scope that should be a badge, not buried in prose.",
    "recommendation": "Give routing rules their own card titled 'Routing rules' (the dead key already exists), and consider surfacing blockAds/blockMalware/blockQuic higher \u2014 rivals (BPB, sing-box UIs) treat these as first-class toggles.",
    "severity": "medium",
    "source": "src/ui/panel/settings.js",
    "title": "Six routing rules live inside a card titled 'Camouflage page'; routing.title key orphaned"
   },
   {
    "effort": "M",
    "evidence": "poolCardHtml (warp.js:8-9, 'Fetch pool'/'Test') renders in the Egress subtab; sourcesCardHtml (warp.js:11-13) repeats the same `data-action=\"pool-fetch\"`; Home renders a third 'Source pool' card via loadHomePool\u2192api('api/proxy-pool?probe=1') (home.js:105-112, 158-171) with its own renderHomePool \u2014 a second, separate implementation of the probe table that already exists as renderPool in warp.js:21-\u2026",
    "location": "src/ui/panel/warp.js:8-13",
    "problem": "Three fetch paths, two table renderers, three URLs that conceptually promise the same data; state (S.pool vs S.poolAt) can diverge between Home and Settings views.",
    "recommendation": "Keep one pool card (Egress) and one probe renderer; have Home link to it or reuse the same render function. Remove the pool-fetch button from Sources if the tab survives at all.",
    "severity": "medium",
    "source": "src/ui/panel/warp.js",
    "title": "Relay-pool widget duplicated across three surfaces (Egress card, Sources card, Home card)"
   },
   {
    "effort": "S",
    "evidence": "`case 'ports':` renders a port-matrix fieldset (settings.js:222-229) but no FL entry in SECTIONS uses type 'ports'; updatePortMasters (L340-348) only queries `[data-port-master]` which only that dead branch creates (called anyway at L326/L506); TLS_PORTS/PLAIN_PORTS consts at home.js:2 exist solely for it. settings.js:564 special-cases `ta.id.indexOf('checker-targets')===0` for a MAX_TARGETS cap, \u2026",
    "location": "src/ui/panel/settings.js:222-229",
    "problem": "Dead render/binder code inflates the 48KB settings bundle and misleads maintainers into thinking a port matrix is wired to settings.",
    "recommendation": "Delete case 'ports', the ports arms in readBind/writeBind, updatePortMasters, and the checker-targets special-case; keep TLS_PORTS/PLAIN_PORTS only if Home still needs them.",
    "severity": "low",
    "source": "src/ui/panel/settings.js",
    "title": "Dead UI machinery: 'ports' binder branch, updatePortMasters, checker-targets MAX_TARGETS remnant"
   },
   {
    "effort": "S",
    "evidence": "Static scan of all panel JS/HTML for each key as a quoted string finds no usage outside the two dictionary lines for: ports.* (23 keys: domains, hostnameOverride, matrix.legend_tls/plain/hint, policy.always/workersdev/never/label, cleanAddrs.label/short/help, remoteSubs.label/hint, cdn.title/enabled.label/addresses.label/short/help/host.label/sni.label), proxyip.* (9), checker.* (20), nav.warp, na\u2026",
    "location": "src/ui/panel/dict.js:3",
    "problem": "~120 dead strings across EN+FA (\u22488KB per language of a 65KB file) ship to every panel load and actively mislead maintainers \u2014 e.g. `tabs.settings.ports` implies a Ports subtab that no longer exists.",
    "recommendation": "Add a dict-lint step (fail CI on unreferenced keys) and delete the orphaned namespaces; keep the genuinely dynamic ones (tabs.settings.*, accent.*, err.* used via SCALAR_RULES).",
    "severity": "medium",
    "source": "src/ui/panel/dict.js",
    "title": "~60 dead i18n keys (~12% of dict.js) shipped in both languages"
   },
   {
    "effort": "S",
    "evidence": "home.js:120 builds the stat grid with labels for version, colo, and `home.status.today`, then appends `<span class=\"mono\">\u2026requestsTotal\u2026</span>` with no label span; `home.status.total` ('Requests total', dict.js:3) is never referenced (verified count=2, the en+fa definitions only).",
    "location": "src/ui/panel/home.js:120",
    "problem": "The lifetime counter renders as a mystery number next to 'Requests today' \u2014 a visible bug corroborated by the orphaned i18n key.",
    "recommendation": "Either render the label `t('home.status.total')` for the second value or drop requestsTotal from the grid.",
    "severity": "low",
    "source": "src/ui/panel/home.js",
    "title": "requestsTotal rendered without its label in Worker status grid; home.status.total key dead"
   },
   {
    "effort": "S",
    "evidence": "settings.js:41 `'<tr><td colspan=\"6\"><span class=\"field__hint\">'+esc(t('users.empty'))+'</span></td></tr>'` \u2014 no icon, no explainer, no body CTA; the Add User button stays in the card head far above, and the hidden bulkbar (users.js:5) plus six-column table header are meaningless at n=0. Contrast home.js:100 which has the proper pattern (`empty-card`, `empty-icon`, `empty-title`, `empty-msg`, `emp\u2026",
    "location": "src/ui/panel/users.js:29",
    "problem": "Confirms the owner pain point: with zero users the page is a skeleton of table chrome around one grey hint line. Rival panels show an illustrated empty state with the primary action inline.",
    "recommendation": "When S.users.length===0 render an empty-card (reuse the home.js:100 pattern/classes) with users.desc plus an inline 'Add User' primary button; suppress the table header/bulk bar until at least one user exists.",
    "severity": "medium",
    "source": "src/ui/panel/users.js",
    "title": "Users empty state is a bare hint row inside table chrome (owner pain point 3 confirmed)"
   },
   {
    "effort": "L",
    "evidence": "SECTIONS order (settings.js:31-125) makes users the 9th and warp the 10th of 10 subtabs: `{key:'users',cards:[{title:'users.title',users:true,fields:[]}]}, {key:'warp',cards:[\u2026]}`. Top-level nav is exactly two anchors (shell.html:43-44: `id=\"tab-home\"` and `id=\"tab-settings\"`). dict.js still ships `'nav.warp':'WARP'` and `'nav.checker':'IP Checker'` \u2014 verbatim proof a previous three-tab IA was col\u2026",
    "location": "src/ui/panel/settings.js:124-125",
    "problem": "WARP is a first-class feature (17 output formats, its own accounts/presets/amnezia model) demoted behind nine settings groups; Users likewise is entity management, not a setting. The dead nav keys show this was a deliberate flattening that the dictionary was never updated for.",
    "recommendation": "This is an IA decision, not a field fix: restore a third top-level view (or dashboard cards) for WARP and Users, or at minimum add a Home shortcut card with inline Generate/Import actions. Decision belongs to the owner.",
    "severity": "high",
    "source": "src/ui/panel/settings.js",
    "title": "WARP management buried as the 10th settings subtab (owner pain point 1 confirmed); nav.warp dict key proves earlier top-level design"
   },
   {
    "effort": "S",
    "evidence": "`FL('remoteDns','str','advanced.remoteDns.label',null,{mono:true})` \u2014 hint is null while its siblings dohUpstream has `.short`+`.help`. Yet remoteDns silently feeds sing-box DNS servers (singbox-json.ts:285-286) and egress hostname resolution (egress.ts:91 `resolveIpv4(target.host, settings.remoteDns)`). validate.ts:511-524 additionally rewrites a bare hostname/IP into `https://${bracketIpv6(hp.ho\u2026",
    "location": "src/ui/panel/settings.js:101",
    "problem": "A wrong remoteDns breaks all egress hostname resolution, and the admin gets no feedback until tunnel failures; the silent URL synthesis is undocumented magic.",
    "recommendation": "Add short+help ('Used by the worker to resolve targets and by sing-box DNS; wrong values break hostname dialing'), surface the normalized https://\u2026/dns-query form in a preview line (the __privateDoh copy-field pattern already exists), and offer preset chips (cloudflare / google / quad9).",
    "severity": "medium",
    "source": "src/ui/panel/settings.js",
    "title": "remoteDns has no hint despite silently driving egress DNS; hostname input is silently rewritten to a DoH URL"
   },
   {
    "effort": "S",
    "evidence": "`FL('chainProxy.uri','str','chain.uri.label','chain.uri.hint',{mono:true})` (settings.js:97) renders `<input type=\"text\" \u2026>` (bindHtml 'str' case, L137-142) while credentials sit in the URI per the field's own hint 'socks5://user:pass@host:port or http://host:port' (dict.js). The card has `title:null` (L95) and no help explaining the use case (egress via another VPS).",
    "location": "src/ui/panel/settings.js:97",
    "problem": "Secret-bearing value is shoulder-surfable and may leak into screenshots/screen-shares; the two-field card gives the admin no context for when or why to enable chaining.",
    "recommendation": "Render as type 'secret' with reveal (same pattern as securePath/telegram.botToken), and give the card a title plus one sentence of use-case help.",
    "severity": "medium",
    "source": "src/ui/panel/settings.js",
    "title": "chainProxy.uri holds credentials but renders as a plain text input (unlike telegram.botToken's secret type)"
   },
   {
    "effort": "S",
    "evidence": "settings.js:115 `FL('camouflage.url','str','advanced.camouflage.url',null,{mono:true})` \u2014 hint null, no help key, no `showIf` predicate even though the mechanism exists (used for proxyIps/nat64Prefixes at L83-84). Server-side the URL is only meaningful for proxy mode: validate.ts:533-540 validates http(s) and SSRF-guards it only `if (out.camouflage.mode === 'proxy')`.",
    "location": "src/ui/panel/settings.js:115",
    "problem": "An admin in 'None (plain 404)' or 'Static landing page' mode sees an empty URL field with no explanation of what belongs there (it is reverse-proxied and must not target private addresses).",
    "recommendation": "Add showIf(v=>v['camouflage.mode']==='proxy'), a hint with an example, and mention the SSRF constraint; consider secret-type rendering since the URL can embed credentials.",
    "severity": "low",
    "source": "src/ui/panel/settings.js",
    "title": "camouflage.url always visible regardless of mode; no hint on what it is or its SSRF constraint"
   },
   {
    "effort": "S",
    "evidence": "settings.js:67-69 render all three unconditionally (no showIf), while ech.ts:10-20 defines the real precedence: manual name > regex-valid SNI > (if echAuto) no ECH with a warning > (if !echAuto) the raw candidate. The live preview (updateEchPreview, settings.js:461-473) partially compensates with 'Effective ECH name: \u2026' text.",
    "location": "src/ui/panel/settings.js:67-69",
    "problem": "The admin must read code-level precedence to know which of the three controls matters; the auto toggle's failure mode (nodes emitted without ECH) is only visible in the preview line, not as state on the toggle.",
    "recommendation": "Apply showIf (manual name field and auto toggle visible only when echEnabled; auto toggle dimmed when a manual name is set), and keep the preview line \u2014 it is the right pattern.",
    "severity": "low",
    "source": "src/ui/panel/settings.js",
    "title": "ECH tri-state (echEnabled / echAuto / echServerName) rendered flat with no linkage"
   },
   {
    "effort": "S",
    "evidence": "Three bare `num` fields with no hints: `FL('urlTestIntervalSec','num','advanced.urlTest.label',null,{min:60,max:86400})`, `FL('subUpdateIntervalHours',\u2026)`, `FL('maxNodesPerFormat',\u2026)`. generate.ts:276 applies `const limit = Math.max(0, Math.floor(s.maxNodesPerFormat))` to cap every emitter's output; the URL-test value feeds clash-yaml/singbox-json/surge-conf/loon interval fields. Card title is the\u2026",
    "location": "src/ui/panel/settings.js:110-112",
    "problem": "The three numbers most likely to be blindly tweaked are the least explained; a high maxNodesPerFormat silently bloats subscriptions, a low one silently truncates them \u2014 no warning either way.",
    "recommendation": "One hint per field (what consumes it, what changes when raised/lowered) and an inline note when maxNodesPerFormat is set below the current generated-node count.",
    "severity": "medium",
    "source": "src/ui/panel/settings.js",
    "title": "Behavior numbers (urlTest / sub interval / max nodes) have no hints; maxNodesPerFormat silently truncates every subscription format"
   },
   {
    "effort": "S",
    "evidence": "The field's hint lists ten placeholders (dict.js addresses.nameTemplate.hint: '{IP} {PORT} {PROTOCOL} {PROTOCOL_LABEL} {IP_NAME} {LABEL} {HOST} {FLAG} {COUNTRY} {WORKER}'), but naming.ts:72-77 maps `IP_NAME: String(ctx.label ?? \"\")` and `LABEL: String(ctx.label ?? \"\")` to the identical value, and `HOST` / `WORKER` (naming.ts:76,77) both to `ctx.host`. No live preview of the rendered name exists de\u2026",
    "location": "src/nodes/naming.ts:72-77",
    "problem": "Half the documented vocabulary is redundant; an admin cannot tell IP_NAME from LABEL or WORKER from HOST because they are the same bytes.",
    "recommendation": "Drop IP_NAME and WORKER (keep LABEL/HOST) with a migration alias in expandTemplate, and add a live example preview under the input showing one rendered node name.",
    "severity": "low",
    "source": "src/nodes/naming.ts",
    "title": "nameTemplate exposes two byte-identical placeholder pairs ({IP_NAME}/{LABEL}, {HOST}/{WORKER}) with no live preview"
   },
   {
    "effort": "S",
    "evidence": "addresses.card.hint (dict.js en): 'One entry per line; use your worker hostname, custom domains or clean Cloudflare IPs. An ip:port pins that port\u2026' \u2014 but the control is a structured card grid (settings.js:127-160) with separate address/port/label/host/sni/country/city inputs and explicit Add/Test buttons; the longer addresses.card.help then restates the same port-pinning rules.",
    "location": "src/ui/panel/dict.js:3",
    "problem": "The first sentence describes a textarea that no longer exists, and hint vs help duplicate each other \u2014 the admin reads the port-pinning rule twice in two lengths.",
    "recommendation": "Rewrite the hint for the card UI ('Cards below create connect endpoints\u2026') and let help carry the detailed port-pinning semantics only.",
    "severity": "low",
    "source": "src/ui/panel/dict.js",
    "title": "addresses.card.hint is stale textarea-era copy ('One entry per line') for a card-grid control; hint and help duplicate content"
   },
   {
    "effort": "S",
    "evidence": "The range renderer (settings.js:233-236) just draws two independent number inputs with a dash between them; SCALAR_RULES only validate each bound against its own min/max. Server-side inverted ranges are caught only on save: validate.ts:783-790 `if (out.fragment.lengthMin > out.fragment.lengthMax) fail(\u2026)`, same for delay and maxSplit. fragmentQuery would emit `flen=500-100` if reached (fragments.t\u2026",
    "location": "src/ui/panel/settings.js:233-236",
    "problem": "Inverted min\u2013max ranges pass client-side and surface only as a post-save toast error, keyed server-side under fragment.* which the inline mapper may not attach to the right pair.",
    "recommendation": "Add an inline min\u2264max check in the range binder (mark the pair invalid on blur) mirroring the server rule.",
    "severity": "low",
    "source": "src/ui/panel/settings.js",
    "title": "Fragment min\u2013max inversion only caught server-side after a failed save; range inputs have no inline cross-check"
   },
   {
    "effort": "S",
    "evidence": "`general.backup.import_hint`: 'Paste an exported settings JSON (secrets and URLs are never included).' (dict.js:3) \u2014 but the card renders a hidden file input and the action opens a file picker (actions.js:46-61: `'settings-import'(){ const fileInput=$('settings-import-file'); \u2026 fileInput.click() }`).",
    "location": "src/ui/panel/actions.js:46-61",
    "problem": "The wording promises a paste flow that does not exist; an admin who copied JSON to the clipboard has no way to import it.",
    "recommendation": "Fix the copy to 'Choose an exported settings JSON file' (or actually support paste), and mirror in FA ('\u062f\u0631\u0648\u0646\u200c\u0631\u06cc\u0632\u06cc' copy says \u0628\u0686\u0633\u0628\u0627\u0646\u06cc\u062f 'paste').",
    "severity": "low",
    "source": "src/ui/panel/actions.js",
    "title": "Backup import copy says 'Paste an exported settings JSON' but the control is a file picker"
   },
   {
    "effort": "S",
    "evidence": "DEFAULT_SETTINGS: `language: \"fa\"` (types/settings.ts:214) \u2014 the panel boots RTL/Persian for a fresh English deploy unless the qp_lang cookie is set. `nat64Prefixes: [\"[2a02:898:146:64::]\", \"[2602:fc59:b0:64::]\", \"[2602:fc59:11:64::]\"]` (types/settings.ts:219) are public community prefixes shipped as defaults, invisible in the UI unless the NAT64 mode chip is selected.",
    "location": "src/types/settings.ts:214",
    "problem": "Language default is intentional (documented) but surprising for non-FA admins and should be an explicit first-run choice; the NAT64 defaults are operational data that can rot and are never surfaced as 'replaceable community defaults'.",
    "recommendation": "Surface language in the setup wizard as an explicit step; annotate the nat64 list (when visible) with 'community defaults \u2014 replace with your own' copy.",
    "severity": "low",
    "source": "src/types/settings.ts",
    "title": "Defaults sanity: language 'fa' boots non-Persian admins into RTL; NAT64 prefixes ship hardcoded public defaults"
   },
   {
    "effort": "S",
    "evidence": "Pain point 2 is largely addressed: home.js:88-99 lists S.subs sorted base64-first via copyFieldHtml (copy + QR per URL), with a Normal/Fragment mode segment (subUrlWithMode) and an empty-state CTA linking to protocols (L100). What is missing is symmetry inside Settings: the only copyable URL there is the DoH private endpoint (`__privateDoh` copyonly, settings.js:243-245), and user/WARP links live \u2026",
    "location": "src/ui/panel/home.js:88-99",
    "problem": "An admin editing Addresses/Protocols cannot see or copy the resulting subscription links without leaving Settings; there is no 'links' surface tied to the sections that generate them.",
    "recommendation": "Add a compact 'Your links' card at the top of the Addresses subtab (or a global sidebar) reusing copyFieldHtml, sourced from the existing S.subs bootstrap data.",
    "severity": "low",
    "source": "src/ui/panel/home.js",
    "title": "Main subscription links: pain point 2 partially refuted \u2014 Home is the link surface, but Settings has none"
   },
   {
    "effort": "S",
    "evidence": "`FL('telegram.chatId','str','advanced.tg.chat_id',null,{mono:true})` \u2014 no hint. The card's long help (dict.js advanced.tg.hint) covers bot creation and webhook but never says how to find the chat ID (e.g. message the bot, then read getUpdates / use @userinfobot); validation is opaque: TG_CHAT_ID_RE 'numeric chat id or @channelname' (validate.ts:568-575).",
    "location": "src/ui/panel/settings.js:108",
    "problem": "The single most common Telegram setup failure is not knowing your chat ID; the panel's best hint stops one step short.",
    "recommendation": "Add a hint: 'Send any message to your bot, then open https://api.telegram.org/bot<token>/getUpdates \u2014 the id field is your chat ID (or use @userinfobot). @name works for public channels.'",
    "severity": "low",
    "source": "src/ui/panel/settings.js",
    "title": "telegram.chatId has no hint on how to obtain it; only the card-level hint partially covers setup"
   }
  ],
  "removals": [
   "settings.localDns \u2014 no runtime consumer anywhere (only fields.ts:110 descriptor, validate.ts:525-529, types/settings.ts); delete field+descriptor+dict keys or explicitly wire it before it renders again.",
   "settings.sourceUrls \u2014 validated and saved but read by nothing (proxyip-pool resolves from proxyIps/proxyIpPoolUrl/DoH only); delete or wire into the pool collector.",
   "addresses[].city \u2014 collected in addrCardHtml (settings.js:153), validated (validate.ts:276-290), never used by naming/emitters (only country feeds flags); delete the input or wire it into renderName.",
   "Dead UI machinery: case 'ports' render branch (settings.js:222-229), the ports arms in readBind/writeBind, updatePortMasters (settings.js:340-348 plus calls at 326/506) \u2014 no FL field uses type 'ports' since the Ports & Domains subtab was removed.",
   "The `ta.id.indexOf('checker-targets')===0` special-case in validateOneEditor (settings.js:564) plus MAX_TARGETS \u2014 references the removed IP Checker view.",
   "Dead dict keys (~60, each \u00d72 languages): ports.* (23), checker.* (20), proxyip.* (9), nav.warp, nav.checker, warp.settings, warp.view.accounts, warp.view.settings, warp.presets.inuse, routing.title, home.status.total, wizard.s1_title, toast.saveFailed, toast.langChanged, common.theme, app.name, err.url, err.ip_or_host, err.ipv6_prefix, err.port \u2014 plus the ignored fragment.enable label argument passed to FL (settings.js:89) which the fpreset renderer never reads.",
   "Duplicate nameTemplate placeholders {IP_NAME} and {WORKER} (byte-identical to {LABEL} and {HOST} in naming.ts:72-77) \u2014 remove from the map and the hint, or keep only as deprecated aliases."
  ],
  "summary": "The settings registry is drift-free where the tests look (fields.ts \u2194 FL/SECTIONS \u2194 referenced dict keys), which makes the failures elsewhere more damning: three settings are write-only dead weight (localDns, sourceUrls, addresses[].city), the Sources tab exists only to collect one of them, and ~60 orphaned i18n keys plus the dead ports/checker binder code document an old IA that was removed but never excised. Label quality is bimodal \u2014 telegram/ECH/egress hints are genuinely good, while fingerprint, ALPN, remoteDns, the three behavior numbers, and the entire Amnezia grid ship raw protocol jargon with no hints, and the fragment preset UI leaves split/packets editable while silently ignoring them. The two IA decisions that most need an owner ruling: WARP and Users are demoted to subtabs 9 and 10 of a two-tab shell (the dead nav.warp/nav.checker keys prove a richer layout existed), and routing rules are mislabeled inside the \"Camouflage page\" card."
 },
 {
  "consolidations": [
   "AMZ (warp.js:66) and AMZK (warp.js:81) \u2014 identical 11-row Amnezia parameter arrays duplicated 40 lines apart in the same file \u2014 merge into one module-level AMZ_FIELDS constant.",
   "renderPool (warp.js:21) and renderHomePool (home.js:159) \u2014 same probe-map + same '\u2713 latency' markup \u2014 merge into one renderPoolInto(box, {limit, showMeta}).",
   "checker.* dict keys + dead 'ports' field renderer + TLS_PORTS/PLAIN_PORTS + MAX_TARGETS/checker-targets reference + #settings-search shortcut + shortcuts.search dict row \u2014 all six are artifacts of the same deleted IP-checker/ports refactor; remove as one dead-feature cleanup pass.",
   "markDirty/diffSection/collectSection/undo stacks \u2014 one section-scoped dirty module keyed by panel element would replace four overlapping snapshot mechanisms (S.snap, UR stacks, S.dirty, scheduleDirtyPush)."
  ],
  "dimension": "UI Code Organization & Performance (src/ui/panel/*)",
  "findings": [
   {
    "effort": "M",
    "evidence": "const js = PANEL_JS_ORDER.map((name) => panelPart(name)).join(eol); const html = panelSplice(...PANEL_CSS_MARKER, panelPart(\"app.css\")), PANEL_JS_MARKER, js);",
    "location": "scripts/build-single-file.mjs:24-29",
    "problem": "panel.html ships 258,107 B raw / 65,458 B gzip with zero minification, while esbuild (minify:true) is already used in the same build file for the worker bundle. BPB-class rival minifies+embeds panel assets (docs/research/01-bpb-panel.md:10: 'HTML/JS/CSS panel assets are minified, gzipped and base64-embedded'). Minifying the 202,336 B of JS parts would take the whole page to roughly 40 KB gzipped (\u2026",
    "recommendation": "Add a minify step to assemblePanel(): run the concatenated JS and CSS through esbuild.transformSync({minify:true}) (already a dependency) before splicing into shell.html. Keep the readable sources untouched; only the assembled panel.html changes. Keep node --check on the minified output.",
    "severity": "high",
    "source": "scripts/build-single-file.mjs; docs/research/01-bpb-panel.md:10",
    "title": "Panel ships raw, unminified \u2014 ~190 KB wasted per deploy; the compressor is already in the build script"
   },
   {
    "effort": "S",
    "evidence": "return htmlResponse(ASSETS.panel, 200, { \"Cache-Control\": \"no-store\", \"Content-Security-Policy\": PANEL_CSP });",
    "location": "src/handlers/panel-page.ts:9",
    "problem": "The panel is a compile-time constant (src/ui/assets.ts:6: panel: panelHtml) that changes only on deploy, yet every visit re-downloads all 258 KB (65 KB gz). The API layer one directory over does conditional requests correctly (src/handlers/api/bootstrap.ts:12 returns 304 with ETag) and the client implements 304 handling (lib.js:22-24) \u2014 the heaviest resource is the only one that cannot use it.",
    "recommendation": "Compute a deploy-time hash of panel.html, serve it as ETag, and switch Cache-Control from no-store to no-cache so the browser revalidates and receives 304. Same treatment for login.html (panel-page.ts:13).",
    "severity": "medium",
    "source": "src/handlers/panel-page.ts:9; src/handlers/api/bootstrap.ts:12",
    "title": "Cache-Control: no-store on a byte-identical 258 KB page \u2014 no ETag, no 304"
   },
   {
    "effort": "M",
    "evidence": "DICT={en:{...523 keys...},fa:{...523 keys...}} \u2014 verified 0 missing/extra between languages; t() fallback (dict.js:26): let s=(DICT[LANG]&&DICT[LANG][k])||(DICT.en[k]||k)",
    "location": "src/ui/panel/dict.js:2-21",
    "problem": "dict.js is 65,245 B source / 18,178 B gzip, always both languages, inlined into the page; nothing lazy-loads or splits per lang. The runtime only needs the active language plus the EN fallback. Fair counterpoint: gzip halves it and inline avoids an extra request \u2014 this is far from the worst offender \u2014 but the panel/README.md documents concat order and never discusses weight, so the trade-off is un\u2026",
    "recommendation": "Either (a) split per-language in the string-splice builder: emit only the cookie-selected language block plus the EN fallback inline, or (b) keep both and document the decision (inline beats a lazy request on Workers) in panel/README.md so it stops looking like an accident.",
    "severity": "medium",
    "source": "src/ui/panel/dict.js; src/ui/panel/README.md",
    "title": "dict.js ships both languages always, unsplit, non-lazy \u2014 52 KB source / 18 KB gzip with half unused at any moment"
   },
   {
    "effort": "M",
    "evidence": "'nav.checker':'IP Checker','checker.title','checker.desc','checker.run','checker.summary','checker.tooMany' (18 keys); 'ports.domains.label','ports.matrix.*','ports.policy.*','ports.cdn.*','proxyip.mode.label','proxyip.list.label'; 'home.status.total','common.theme_toggle','toast.saveFailed'",
    "location": "src/ui/panel/dict.js:3 (en) and :20 (fa)",
    "problem": "Programmatic scan of all 523 EN keys against every panel part + shell: 68 are referenced nowhere (excluding dynamic tabs.settings.*/accent.* prefixes and server-error err.* lookups). Highlights: a complete ghost 'IP Checker' feature (18 keys for UI that does not exist), entire dead settings sections (old Ports & Domains / Proxy IP tabs superseded by addresses/egress whose live keys sit beside them\u2026",
    "recommendation": "Delete the 68 dead keys from both languages (exact list produced during audit; ~9 KB source each). Add a build-time drift check (node script in build-single-file.mjs) that fails when a dict key is unreferenced, mirroring the existing marker/script checks.",
    "severity": "medium",
    "source": "src/ui/panel/dict.js:3,20",
    "title": "68 dead dictionary keys \u2014 a graveyard of deleted features, including a complete fake 'IP Checker'"
   },
   {
    "effort": "M",
    "evidence": "panel/README.md:16 \u2014 \"settings.js holds the user modal, settings registry, field renderers, TOTP card and section IO with validation\"; settings.js:291-300 totpHotp/totpCheck do raw crypto.subtle HMAC-SHA1 bit-slicing",
    "location": "src/ui/panel/settings.js:30 (SECTIONS), 186-260 (bindHtml renderers), 4-30 (openUserModal), 287-300 (TOTP), 441-497 (diff/save), 536-570 (validators)",
    "problem": "The file is 'one view' only by the accident of tab layout, not cohesion: the SECTIONS registry, 13 field-type renderers, address/remote-node card HTML builders, the users modal, the settings-diff/save engine, client validators, and a complete Base32/HMAC-SHA1 TOTP implementation with client-side code verification (~60 lines with zero dependency on the rest of the file). At 48 KB it is 74% the size\u2026",
    "recommendation": "Extract the TOTP stack into its own part (e.g. totp.js) inserted in PANEL_JS_ORDER, and split the field renderers/registry from the users modal. The splice-based build makes this a mechanical, order-preserving move.",
    "severity": "medium",
    "source": "src/ui/panel/settings.js; src/ui/panel/README.md:16",
    "title": "settings.js is a 621-line grab bag: field registry, renderers, users modal, AND a full TOTP crypto stack"
   },
   {
    "effort": "S",
    "evidence": "case 'ports':{ const fam=f.family==='tls'?TLS_PORTS:PLAIN_PORTS; ... \u2014 implemented but no SECTIONS field ever uses type 'ports' (verified: the 48 FL() rows use only secret/str/bool/list/instantbool/lang/select/num/addrList/remoteList/chips/fpreset/copyonly); settings.js:564: ta.id.indexOf('checker-targets')===0?' / '+MAX_TARGETS:''",
    "location": "src/ui/panel/settings.js:222-228 (case 'ports'), :405,:429 (readBind/writeBind ports branches), :564 (checker-targets ref)",
    "problem": "The 'ports' field renderer with its tri-state master checkbox is unreachable, and its inputs TLS_PORTS/PLAIN_PORTS (home.js:1) are read only by this dead branch and the corresponding readBind/writeBind data-type=\"ports\" branches. The same file carries MAX_TARGETS support for the nonexistent checker via an id-prefix condition that can never be true (no element id starting with checker-targets is ev\u2026",
    "recommendation": "Delete case 'ports' plus the two readBind/writeBind ports branches plus TLS_PORTS/PLAIN_PORTS; delete the checker-targets conditional and MAX_TARGETS usage. Guard with the dead-key drift check from the dict cleanup so it cannot regrow.",
    "severity": "low",
    "source": "src/ui/panel/settings.js:222,405,429,564",
    "title": "Dead code inside settings.js: unused field renderer, orphaned port constants, ghost checker reference"
   },
   {
    "effort": "S",
    "evidence": "warp.js:66: const AMZ=[['Jc','Jc (0-128)'],...] ... warp.js:81: const AMZK=[['Jc','Jc (0-128)'],...] \u2014 identical 11-row arrays 40 lines apart; warp.js:21-37 renderPool vs home.js:159-167 renderHomePool \u2014 same probe map, same '\u2713 latency' markup, differing only in slice(0,16) vs slice(0,8) and a meta header",
    "location": "src/ui/panel/warp.js:66 vs :81 (AMZ/AMZK); warp.js:21-37 (renderPool) vs home.js:159-167 (renderHomePool)",
    "problem": "Two verbatim-duplicate Amnezia parameter tables in one file, and two near-identical pool renderers across files \u2014 the classic string-splice part layout gives no shared-module pressure, so copy-paste accretes.",
    "recommendation": "Hoist one module-level AMZ_FIELDS constant in warp.js; extract a shared renderPoolInto(box, {limit, showMeta}) used by both call sites. Also keep the justified head.js/dict.js theme-lang duplication (FOUC prevention) but add a one-line comment cross-referencing the two files.",
    "severity": "low",
    "source": "src/ui/panel/warp.js:66,81,21; src/ui/panel/home.js:159",
    "title": "Duplicate helpers across parts: Amnezia tables twice in one file, pool renderer twins across files"
   },
   {
    "effort": "L",
    "evidence": "function markDirty(){ S.dirty.clear(); SECTIONS.forEach(s=>{ try{if(JSON.stringify(collectSection(s.key))!==S.snap[s.key])S.dirty.add(s.key)}catch(e){}}); scheduleDirtyPush(); updateApplyBar()}",
    "location": "src/ui/panel/settings.js:456-461 (markDirty), :441-447 (collectSection), :301-325 (renderSettings builds all panels), :462-467 (updateApplyBar)",
    "problem": "Every input event ends in markDirty() (actions.js:412-424). collectSection does panel.querySelectorAll('[data-bind]') + readBind + JSON.stringify for ALL ten panels \u2014 including the nine hidden (renderSettings built them all up front at boot, so hidden means fully-populated DOM). updateApplyBar then runs three more document.querySelectorAll passes per keystroke, plus undo pushes on a 1 s debounce (\u2026",
    "recommendation": "Maintain dirty state O(1) per event: every [data-bind] element sits inside a panel whose key is recoverable via el.closest('[id^=\"sp-\"]'); on each input, collect+diff only that one section. Only on section-save/apply-bar open do a global sweep. This is the actual answer to 'render diffing vs full re-render': the diff engine exists but full re-collect is paid anyway.",
    "severity": "high",
    "source": "src/ui/panel/settings.js:441-467; src/ui/panel/actions.js:412-424",
    "title": "Dirty-tracking is O(all sections) on every keystroke \u2014 10 hidden panels re-collected and JSON-stringified per input event"
   },
   {
    "effort": "M",
    "evidence": "if(!S.warp)loadWarpIfNeeded().then(()=>{if(S.warp)renderHome()}); \u2014 renderHome rebuilds every card; lib.js:18: const isGet=(!o.method||o.method==='GET')&&!o.fresh; \u2014 both home calls use {fresh:true} (home.js:156,168)",
    "location": "src/ui/panel/home.js:103 (re-render trigger), :85-131 (full innerHTML rebuild), :156,:168 (fresh:true calls)",
    "problem": "The API layer explicitly excludes fresh GETs from in-flight dedup (lib.js:18), so on a normal boot GET api/proxy-pool?probe=1 fires twice and each probe makes the Worker tcpProbe up to 8 endpoints (src/handlers/api/proxy-pool.ts:12,37-42: for (const ep of endpoints.slice(0, MAX_PROBES)) ... tcpProbe). Same for api/users (twice, even when the user list is empty \u2014 which is exactly the owner's 'Users\u2026",
    "recommendation": "Replace the wholesale renderHome() re-entry with a targeted update of just the WARP subscriptions block (or a flag parameter); drop fresh:true on the boot path (the 30 s sessionStorage cache already prevents dupes once fresh is off).",
    "severity": "medium",
    "source": "src/ui/panel/home.js:103,156,168; src/handlers/api/proxy-pool.ts:37-42",
    "title": "Home view re-renders wholesale and double-fires probe APIs because fresh:true bypasses the in-flight dedup layer"
   },
   {
    "effort": "S",
    "evidence": "if(mod&&e.key.toLowerCase()==='k'){e.preventDefault();const s=document.querySelector('#settings-search');if(s)s.focus();else location.hash='#/home';return} \u2014 and chrome.js:53 advertises ['Ctrl / \u2318 + K','shortcuts.search']",
    "location": "src/ui/panel/chrome.js:79 (handler), :53 (shortcuts modal row); shell.html (no #settings-search)",
    "problem": "No #settings-search exists anywhere (grep: 0 hits in shell.html or any part) \u2014 there is no search box in the panel at all. Every Ctrl+K silently degrades to 'go home' while the help dialog promises a search feature. Dead feature plus wrong help text is exactly the drift the dead-code pass (dict keys) would have caught.",
    "recommendation": "Either ship the search box (it is genuinely useful across 66 settings fields) or remove the shortcut row, its dict keys ('shortcuts.search' en+fa), and the Ctrl+K branch.",
    "severity": "low",
    "source": "src/ui/panel/chrome.js:53,79",
    "title": "Ghost keyboard shortcut: Ctrl+K focuses an element that does not exist, while the shortcuts modal advertises it"
   },
   {
    "effort": "S",
    "evidence": ".blob{position:absolute;border-radius:50%;filter:blur(130px)} .blob-a{...width:900px;height:500px;...animation:drift-a 44s var(--ease) infinite alternate} @keyframes spinring{to{rotate:360deg}}",
    "location": "src/ui/panel/app.css:33-37 (blobs), :43-44 (spinring), :32 (feTurbulence noise), shell.html:23 (.texture-wrap)",
    "problem": "Two 900\u00d7500 elements animated with infinite translate plus a 130px filter blur repaint on a fixed full-viewport layer, a rotating conic-gradient ring behind the logo, and an SVG feTurbulence noise tile run continuously on a settings page full of text inputs. There is a prefers-reduced-motion kill switch (app.css:301) \u2014 good \u2014 but the default path burns compositing budget on the low-end Android pho\u2026",
    "recommendation": "Pause blob animations when the settings view is active (body[data-view=\"settings\"] .blob{animation-play-state:paused}) or render them once to a static gradient; drop the per-frame blur in favor of a pre-blurred radial-gradient background (visually identical at this opacity).",
    "severity": "low",
    "source": "src/ui/panel/app.css:32-37,43-44,301",
    "title": "Always-on decorative GPU work: 130px blur blobs, conic-gradient spin, turbulence noise"
   },
   {
    "effort": "M",
    "evidence": "const esc=s=>String(s==null?'':s).replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',\"'\":'&#39;'}[c])); \u2014 traced ~50 interpolation sites incl. users.js:17-22 (names), warp.js:95-97 (tokens/URLs), settings.js:203 (values), settings.js:486 (textContent for raw server messages), actions.js:482 (QR modal)",
    "location": "src/ui/panel/lib.js:2 (esc); ~500 esc() call sites across settings.js/warp.js/users.js/home.js/actions.js",
    "problem": "All dynamic values pass through esc() before attribute or text interpolation; the raw-message path in showFieldError is safe because it uses textContent. Verdict: no XSS finding today. But correctness rests on hundreds of hand-placed esc() calls in concatenation soup with no lint rule (repo runs no eslint by design; build's node --check is syntax only). One future contributor forgetting one esc() \u2026",
    "recommendation": "Introduce a tiny h(tag, attrs, children) helper (or at minimum a build-time grep gate banning un-escaped '+ concatenation into attribute positions) so the invariant is enforced instead of remembered.",
    "severity": "low",
    "source": "src/ui/panel/lib.js:2",
    "title": "innerHTML/esc discipline: consistently applied \u2014 and that is a convention, not a guarantee"
   },
   {
    "effort": "S",
    "evidence": "return RE_HOST.test(s)&&s.includes('.')||(RE_HOST.test(s)&&!s.includes('.')) \u2014 reduces to RE_HOST.test(s) by precedence; randomPass: let s='';a.forEach(b=>{s+=cs[b%cs.length]})",
    "location": "src/ui/panel/settings.js:541-545 (validIpOrHost), :601-604 (randomPass)",
    "problem": "validIpOrHost's dot-checks are dead logic that misleads readers into thinking it distinguishes IPv4 from bare hostnames. randomPass generates Trojan/SS passwords with modulo bias over a 62-char alphabet (24 chars \u2248 142 bits ideal, slightly less actual) \u2014 a trivial rejection-sampling fix.",
    "recommendation": "Collapse validIpOrHost to the intended explicit branches (or just RE_HOST.test(s)); replace b%cs.length with rejection sampling: re-draw when b >= 248 (4*62) to make the distribution uniform.",
    "severity": "low",
    "source": "src/ui/panel/settings.js:541,601",
    "title": "Minor correctness nits found during the pass: no-op dot checks in validIpOrHost, modulo-biased password generator"
   }
  ],
  "removals": [
   "68 dead dictionary keys in both en and fa blocks of dict.js (full list enumerated in audit): nav.checker + all 18 checker.* keys, all ports.domains/ports.matrix/ports.policy/ports.cdn/proxyip.* section keys, home.status.total, common.theme/common.theme_light/common.theme_dark/common.theme_toggle, wizard.s1_title, toast.saveFailed, toast.langChanged, warp.settings, warp.presets.inuse, warp.view.accounts, warp.view.settings, routing.title, onboarding.setup.expired (panel-side), app.name, nav.warp.",
   "Dead 'ports' field renderer: case 'ports' in settings.js bindHtml, the data-type=\"ports\" branches in readBind (settings.js:405) and writeBind (settings.js:429), and the TLS_PORTS/PLAIN_PORTS constants in home.js:1.",
   "The checker-targets conditional in settings.js:564 and the MAX_TARGETS constant if the IP checker stays deleted (PROBE_TIMEOUT/PROBE_TRIES in lib.js:4 are likewise referenced nowhere else).",
   "The no-op includes('.') conditions in settings.js:541-545 validIpOrHost (collapse to the single RE_HOST.test they reduce to).",
   "The 'shortcuts.search' row from the shortcuts modal (chrome.js:53) and its dict keys \u2014 or ship the search box it promises."
  ],
  "summary": "The panel's architecture is genuinely better than BPB-class rivals \u2014 documented 9-part splice build, perfect 523/523 EN/FA dict parity, consistent esc() discipline, JSON-diff section saves \u2014 but no dead-weight or performance pass has ever been run on it. It ships 258 KB unminified (65 KB gzip) under Cache-Control: no-store with no ETag, the dictionary carries 68 dead keys memorializing deleted features including an entire fake 'IP Checker', and settings.js is a 48 KB grab bag containing an unreachable field renderer and a ghost Ctrl+K search target. Meanwhile dirty-tracking re-collects all ten hidden panels' DOM and JSON-stringifies them on every keystroke, and the home view re-renders wholesale and double-fires probe APIs on boot because fresh:true bypasses the client's own dedup layer."
 }
]