# U1..U5 — UX functional details
Type: task (AFK) · Phase: UX (functional only — styling waits for V1)

## Questions
- U1 Empty states: dedicated icon+title+msg+CTA for subscriptions/checker/line-editors (EN/FA).
- U2 Help popovers: `ⓘ` hover/click for policy fields (Fragment, Chain proxy, CDN, NAT64, plainPortPolicy, early-data); keep `.field__hint` for format rules.
- U3 Validation timing: on-blur scalar fields; clear-on-valid.
- U4 Char counters: profileTitle, securePath (`0/64`), red near limit.
- U5 Toast progress bar + backdrop-click modal close.

## Answer
(resolved on ticket close)
